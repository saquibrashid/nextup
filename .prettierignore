dist/
build/
coverage/
node_modules/
*.min.*
docs/
specs/
CHANGELOG.md
# Generated by npm; reformatting it would fight `npm install` on every run.
package-lock.json
# Generated by `npm run notices`. `check:licences --check` compares it BYTE FOR
# BYTE against a fresh render, so letting Prettier reformat it puts two CI gates
# in permanent conflict: format:check rewrites the file, then check:licences
# reports drift, and regenerating undoes the formatting again. Keep it ignored.
THIRD-PARTY-NOTICES.md
# Generated by `npm run status`, and `check:status --check` compares it byte for
# byte. `docs/` is already ignored above, so this line is belt and braces: it
# keeps the exclusion true if `docs/` is ever narrowed.
docs/status.md
# Generated by `npm run infra:build` from infra/main.bicep, and `check:infra`
# compares it against a fresh compile. Prettier reformatting it would put the
# two gates in permanent conflict, exactly as for the files above.
infra/main.json
playwright-report/
test-results/
.lane-briefs/