// `/about` - attribution and retention statements (specs/ui.md §8). STUB:
// TASK-026/TASK-120 add IMAGE_RETENTION_STATEMENT and the never-delete and
// no-analytics copy.

import type { JSX } from 'react';

export function AboutPage(): JSX.Element {
  return <h1>About nextup</h1>;
}
