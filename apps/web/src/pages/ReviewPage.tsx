// `/batches/:batchId/review` - the review pass, the SAFETY GATE (ui.md §5). STUB.

import type { JSX } from 'react';

export function ReviewPage(): JSX.Element {
  return <h1>Review this batch</h1>;
}
