# Task status

**Generated by `npm run status` from `docs/backlog.md` §1.2. Do not edit by hand.**

Status is recorded in the ledger and verified by `npm run check:status`, which
fails the build if a task is marked `done` while the tests named in its own
"Done when" column are absent from the suite, or while a task it depends on is
unfinished. See `specs/testing.md` §9A (`T-STATUS-001`).

## Totals

| Status | Count |
|---|---|
| ⬜ todo | 3 |
| 🚧 doing | 3 |
| ✅ done | 169 |
| 🙋 owner | 4 |
| 💤 deferred | 0 |
| **total** | **179** |

## Ready to start

Not done, and every task they depend on is done.

| Task | Size | Section |
|---|---|---|
| `TASK-057` | M | Epic C — Extraction |

## Waiting on the owner

| Task | Section | Note |
|---|---|---|
| `TASK-011` | 3. Milestone M0 — Repo, CI gate, deployable shell, risk-first checks | Owner captured five real screenshots (`4a3da2c`); still owed the per-surface text-vs-artwork evidence file. ⚠ Its "Done when" names `Context/evidence/` — a directory that does not exist in this repo (authoring tree). |
| `TASK-126` | Epic K — Platform, safety, and the shell | **The IMDb half is closed; the rental half is an OWNER DECISION, not remaining work.** `specs/testing.md` §9 gained the three missing mapping sections **US-044, US-045 and US-046** (14 criteria), each row citing tests that really run, and those 14 entries left `KNOWN_UNMAPPED`. Writing the mapping surfaced **one genuinely uncovered criterion — US-046 AC-2** ("two works sharing a title and year each get their own rating"): `T-IMDB-002d` asserted the *dedupe* direction only, and nothing anywhere asserted the converse. Added `T-IMDB-002f` (selection) and `T-IMDB-004h` (fetch-and-write). ⚠ **`T-IMDB-004h` alone killed the "write the answer back to `due[0]`" mutant — every one of the other 24 cases in the file passed** — so "the right rating on the wrong row", the exact defect US-046 exists to prevent, had no coverage at all. Four mutations, each killed by the expected test: dedupe keyed on a string instead of the id (killed `T-IMDB-002f`, `T-IMDB-004h`; **`T-IMDB-002d` passed unchanged**, which is the pairing argument in one observation); the write mapped back by position (killed `T-IMDB-004h` alone); a phantom `~~T-OMDB-099~~` planted in a new row (killed `T-META-001e`, proving the gate really reads the rows just added); a mapping row deleted (killed `T-META-001a` + `d`). ⚠ **NOT DONE, AND NOT BLOCKED ON A MAPPING CHORE: `KNOWN_UNMAPPED` still holds 28 criteria — US-040…US-043, the rental epic — and `docs/backlog.md` HAS NO RENTAL TASK OF ANY KIND.** They cannot be mapped by anyone: there is no implementation, so there is no test to name, and inventing rows would satisfy `T-META-001a` while failing `T-META-001e`. This task closes when that epic has a backlog and is built — it is an Epic to be written, not a gap in this gate. ⚠ **AND IT MUST NOT BE WRITTEN UNILATERALLY: `specs/data-model.md` §17 opens with "⚠️ DO NOT BUILD THIS IN v1, AND DO NOT WRITE A MIGRATION FOR IT", and tells a v1 lane agent that finds it to treat it as documentation, not work.** Epic L is fully specified — ADR-0010, REQ-082…REQ-087, `data-model.md` §17, PRD US-040…US-043 — so nothing is missing but the decision to promote it. **Promotion also REQUIRES amending US-036 AC-2 and `T-CI-005` from three non-owner-initiated processes to four in the same change** (the availability refresh is the fourth), which is a change to a `must` invariant and is the owner's to make. Re-marked `owner` rather than `doing` so it stops appearing as startable work: no agent can close it and no agent should try. ⚠ The "242 AC" headline in the task definition is itself stale: the PRD carries **283** criteria across 46 stories today. |
| `TASK-134` | 3. Milestone M0 — Repo, CI gate, deployable shell, risk-first checks | Needs the owner to APPLY to Microsoft for Azure OpenAI modified abuse monitoring — an approval, not code. `docs/parallel-execution-plan.md` §3 already lists it as owner-dependent; the ledger said `todo`, which advertised it to lane agents as startable work. |
| `TASK-165` | Epic B — Capture & import | Needs a real iOS device to verify the clipboard paste path |

## Blocked by a dependency

5 tasks cannot start yet.

| Task | Waiting on |
|---|---|
| `TASK-058` | `TASK-057` |
| `TASK-078` | `TASK-057` |
| `TASK-079` | `TASK-078` |
| `TASK-079b` | `TASK-079` |
| `TASK-168` | `TASK-079` |

## Done

| Task | Evidence | Verified by |
|---|---|---|
| `TASK-001` | `dd61243` (in the initial scaffold commit) | _no test id declared_ |
| `TASK-002` | `85daf49` | `T-META-004` |
| `TASK-003` | `3539384` | _no test id declared_ |
| `TASK-004` | `eab919a` | `T-SEC-009` |
| `TASK-005` | `ecec349` | _no test id declared_ |
| `TASK-006` | `f496055` — Bicep authored, what-if clean for both environments; live deployment stays owner-gated | `T-INFRA-002`, `T-INFRA-005` |
| `TASK-007` | `T-CI-009a`–`l` (`.github/workflows/deploy.yml`, `tests/infra/deployWorkflow.spec.ts`) and `T-SMOKE-001`–`003` (`tests/smoke/smoke.spec.ts`, `playwright.smoke.config.ts`). Build → secret-scan → ghcr.io push with `GITHUB_TOKEN` (no PAT) → staging → `prisma migrate deploy` → staging smoke → prod → hold at 0% traffic → prod smoke → shift to 100%. Azure auth is OIDC federated (app `nextup-github-deploy`, Owner scoped to `nextup-rg` only); Easy Auth app `nextup` registered. **Proven end to end on 2026-08-18**: staging and production both deployed, migrations applied, `T-SMOKE-001`--`004` green against both live FQDNs, and the blue/green gate observed engaging (traffic held on the prior revision, the new one smoked on its own private FQDN, shifted, prior revision deactivated). Four defects were found only by running it: the OIDC subject is emitted in immutable numeric form; Azure SQL refuses `eastus2` for this subscription (hence `sqlLocation`); `targetPort` disagreed with the Dockerfile `PORT`; and the 0%-traffic hold was a no-op in three independent ways (`T-INFRA-011`, `T-CI-009o`--`r`). See `specs/testing.md` §32. | `T-MIG-001` |
| `TASK-008` | `T-INFRA-005` + `T-INV-013`, both mutation-proven against half-applied up-sizes | `T-INFRA-005`, `T-INV-013` |
| `TASK-009` | `6d47f55` | _no test id declared_ |
| `TASK-010` | Pricing verification **complete** against the live Retail Prices API for `eastus2`, dated 2026-08-17: dated addenda on ADR-0001/0003/0005, `architecture.md` §Cost summary and `runbooks/scale-up-memory.md` corrected in place. Verified total **$11.77/mo** vs published $11-13, so `OQ-026` does not fire. `gpt-4.1` (2025-04-14) and SQL `Basic` confirmed available in `eastus2`. **↳ Two corrections from the provisioning pass (2026-08-18), verified against this subscription with `az cognitiveservices model list` / `usage list` / `account list`:** (1) `gpt-4.1` has **no regional `Standard` quota here at all** — only `GlobalStandard` (key `OpenAI.GlobalStandard.gpt4.1`, limit 1000, used 0), so `infra/ai.bicep` pins `GlobalStandard`. ⚠ the quota key spells the model **`gpt4.1` with no hyphen**, so grepping the quota list for `gpt-4.1` returns nothing and reads exactly like "the model is unavailable". (2) Vision `F0` is available as a SKU in `eastus2` but is **one-per-subscription-per-kind**, and this subscription already holds `vision-f4n7ptoeq44pk` (F0, `rg-coffee-dev`, unrelated project) — so an F0 deployment **will be rejected** until that account is removed or nextup takes `S1`. ~~Vision `F0` confirmed available~~ — available ≠ deployable. ~~**Remaining: item (h)**~~ **↳ Item (h) CLOSED (2026-08-28), read-only against the deployed `ca-nextup-staging`** — `az monitor metrics list-definitions` returns 31 metrics; `RestartCount` (unit Count, primary **Maximum**, dims `revisionName`/`podName`) and `WorkingSetBytes` (Bytes, Average, same dims) both exist, and **no OOM-distinct metric exists at all**. `RestartCount` counts an OOM kill, a crash, a failed probe, a deploy and a scale event identically, so it cannot answer `A43`'s up-size trigger on its own — **which is the finding that makes TASK-157's `image.decode.begin/end` sentinel the primary signal rather than a belt-and-braces duplicate.** ⚠ Its primary aggregation is `Maximum`, not `Total`: it is a running per-pod counter, so a `Total`-over-window rule sums a rising series and fires on a healthy app; the `podName` dimension is what makes an increase meaningful. `cae-nextup` ships app logs to `log-analytics`, so the sentinel is KQL-queryable — at the $1.50/mo log-search price §31.4(c) already established. Written up as `specs/testing.md` §31.6. ~~metric existence (`RestartCount` / `WorkingSetBytes` / any OOM-distinct signal) needs `az monitor metrics list-definitions` against a **deployed** container app, so it is owed the moment staging exists and is a TASK-157 input.~~ See `specs/testing.md` §31. **↳ HELD by owner decision (2026-08-18): no Azure AI resource is to be DEPLOYED until `TASK-168`'s bake-off reports.** The infrastructure is written and gated (`838af3b`, `T-INFRA-001a…g` green) but deliberately unapplied — `nextup-rg` holds zero Cognitive Services accounts, and the auto-triggered `deploy` run on that push was cancelled at the build stage. ⚠ **CIRCULARITY, unresolved:** `specs/ai.md` §9.7 requires live calls to `gpt-4.1` **and** `gpt-5.4-mini` to produce the bake-off's recordings and scores, so the bake-off cannot report while the Azure OpenAI account is unprovisioned. The **Vision** half can wait — it is the F0 conflict that prompted the hold — but the **AOAI** half is the bake-off's own prerequisite. Resolving it AOAI-only would need a `deployVision` switch in `ai.bicep` that does not yet exist. | _no test id declared_ |
| `TASK-012` | `9ae0a0f` | _no test id declared_ |
| `TASK-013` | `692305b` | `T-DM-004` |
| `TASK-014` | `692305b` | `T-INV-008` |
| `TASK-015` | `692305b` | `T-DM-001` |
| `TASK-016` | `536aeb3` | `T-INV-009`, `T-INV-010` |
| `TASK-017` | Prisma `sqlserver` + `prisma/migrations/0001_init` applied against real `mssql/server:2022`; `apps/api/src/repository/ownerData.ts` with `ownerId` as the first positional parameter of every method; `T-SEC-021` (AST walk, 17 cases, 12 of them mutations), `T-INV-001/002/015` (DB raises `2601`/`2627`), `T-INV-018` (BIN2 collation + filtered indexes exist), `T-INV-019/020/021/022`. 28 integration tests green. Schema/migration drift is zero and CI now asserts it. | `T-INV-001`, `T-MIG-001`, `T-SEC-021` |
| `TASK-018` | `apps/api/src/auth/principal.ts` — Easy Auth `x-ms-client-principal` adapter, plus `auth/ownerId.ts`. Fails **closed** on every malformed input including a repeated header. `T-SEC-013` (15 cases), `T-SEC-020` (8, incl. a 10,000-subject collision fixture and the `\|`-separator collision), `T-SEC-019` (the shim boundary, mutation-proven). | `T-SEC-013` |
| `TASK-019` | `apps/api/src/middleware/allowList.ts` — fail-closed against `NEXTUP_ALLOWED_SUBJECTS`; `NEXTUP_BOOTSTRAP_ALLOW_FIRST` grants nothing on its own. `T-SEC-010`, `T-SEC-014`, `T-SEC-015` (address-claim word ban, mutation-proven), `T-SEC-016`. | `T-SEC-010`, `T-SEC-014`, `T-SEC-015`, `T-SEC-016` |
| `TASK-020` | `apps/api/src/middleware/ownerScope.ts` + `auth/ownerId.ts` — `ownerId` derives from the principal only. `T-SEC-020`, `T-SEC-029` (routes **enumerated** from the app and the router, not listed; `T-SEC-029c` mutation-proven against a route mounted outside the chain). | `T-SEC-020`, `T-SEC-029` |
| `TASK-021` | `apps/api/dev/devPrincipal.ts` — the shim is excluded **structurally** (outside the production `include: ["src/**/*.ts"]`), not by a flag or an exclude list; `createApp` injects the reader. `T-SEC-019a-f`. ⚠ The task row said `apps/api/src/auth/devPrincipal.ts`; corrected in place below and in `specs/security.md` §2.3. | `T-SEC-019` |
| `TASK-022` | `d95a1ea` | `T-API-002`, `T-SEC-007` |
| `TASK-023` | `apps/api/src/app.ts` + `routes/index.ts` in the mandated order, no CORS middleware, `/api` 404 fallback **inside** the chain. `T-SEC-005`, `T-SEC-012`, `T-SEC-030`, `T-API-001`. Driven over real HTTP on an ephemeral port — no new dependency. | `T-API-001`, `T-SEC-005` |
| `TASK-024` | `T-ATTR-001` | `T-ATTR-001` |
| `TASK-025` | `a9e3483` | `T-UI-023` |
| `TASK-026` | `apps/web/src/components/TmdbAttribution.tsx` mounted in `AppShell`'s global footer + `packages/domain/src/attribution.ts` (the one source for the sentence) + `/about`; `apps/web/test/attribution.spec.tsx` (`T-ATTR-001a`–`g`, `T-UI-022a`–`e`) and **`tests/e2e/attribution.spec.ts` (new, `T-ATTR-004a`–`c`)**, which was the outstanding blocker. Copy is byte-equal to `ui.md` §9. ⚠ **THE PLAYWRIGHT HALF WAS BLOCKED ON A CONDITION THAT NO LONGER HELD.** This row read "blocked on `T-ATTR-004` — `playwright.config.ts` has no `webServer`"; the `webServer` and the first real e2e spec landed with the a11y work, so the block was stale documentation rather than a live dependency, and `T-ATTR-004` was simply unwritten. ⚠ **`T-ATTR-001` CANNOT SUBSTITUTE FOR `T-ATTR-004`.** It renders one component in jsdom, where nothing is laid out and no stylesheet applies — so `white-space: nowrap`, `text-overflow: ellipsis` and a fixed-height clip all pass it untouched, and those are the ways the sentence is actually truncated at the 320 px floor. `T-ATTR-004a` therefore asserts the computed `text-overflow` **separately from** the overflow geometry: an ellipsised wrapped block reports **no** overflow, because the browser has already shortened the text, so the geometry check alone reports a truncated disclaimer as healthy. It also requires the block to be **taller than one line**, which catches the opposite mistake — a sentence that "fits" only because it was cut. ⚠ **Owner item still outstanding:** the logo asset is a labelled placeholder pending the real TMDB mark. `T-ATTR-003` asserts the file **loads and paints**, not which artwork it is, so swapping in the real mark cannot break the gate. **Depends only on TASK-025, which is done.** | `T-ATTR-001`, `T-ATTR-004` |
| `TASK-027` | `T-INFRA-008` | `T-INFRA-008` |
| `TASK-028` | `apps/web/src/containers/OwnerGate.tsx` (new) + `tests/e2e/refusal.spec.ts` (`T-SEC-018a`–`d`) + `apps/web/test/states.spec.tsx` (`T-UX-019a`–`e`, `T-UX-025a`–`g`). **This task found and fixed a real US-001 AC-4 security defect.** The lane block recorded here (`no webServer`) was stale documentation — `playwright.config.ts` has had one since TASK-024. Writing `T-SEC-018` against the assembled app immediately failed: a 403-refused account was served the COMPLETE product navigation — links to `/`, `/upload`, `/batches`, `/removed`, `/not-interested`, `/about`, `/rating`, plus `.app-shell`, header and footer — while `specs/ux-states.md` §2.11 requires "no list data, no nav and no partial UI". Cause: `App.tsx` wraps all nine routes in `<Route element={<AppShell />}>` and six containers rendered `<RefusalPage />` as their ROUTE ELEMENT, i.e. inside the shell's `<main>`. ⚠ **The pre-existing component test could not see it and never could:** `states.spec.tsx` renders `RefusalPage` in ISOLATION, so `expectNoAppUi()` asserted only that the component emits no nav of its own. Measured, not argued — with the defect restored as mutant 4, all **529** web component tests still passed. ⚠ **And the leak preceded the refusal:** a container cannot know it is refused until its fetch resolves, so the nav was shown for a full round trip BEFORE the 403 arrived — no container-level fix could close that window. Hence a ROOT gate: `main.tsx` now renders `<OwnerGate />`, which settles `GET /api/me` before the router mounts (`RefusalPage`'s own header has said "rendered INSTEAD of the router" since it was written). `failed` gets a chrome-free RETRY, deliberately NOT the refusal and NOT the app: a refusal is final (NFR-015) and a dropped packet is not, and rendering the product on an unproven identity is the same mistake as rendering it on a refused one. `T-UX-025` (`specs/testing.md` L1583) was a PHANTOM id — defined, mapped, carried by no test, so `check:test-ids` resolved it while nothing ran; the four 403 cases moved off the borrowed `T-UX-019f`–`i` onto it, and `T-UX-025e`–`g` now cover the gate's own branches. **4 mutants, each killed by its named test:** refusal branch mounts the app ⇒ `T-UX-025e` + `T-SEC-018b`/`c`/`d`; app mounted while loading ⇒ `T-UX-025f`; a `Request access` link added ⇒ `T-UX-025c` + `T-SEC-018d`; the defect itself restored ⇒ `T-SEC-018b`/`c`/`d`. Two `attribution.spec.ts` fixtures needed honest updates, not weakening: `expectStyledAndRendered` now waits for `.app-shell` (the gate's loading state is itself a `<main>`, so `getByRole('main')` went green before the product rendered, and the non-retrying `isVisible()` reads that follow would have reported every route as having dropped the disclaimer); and `T-ATTR-002c` now succeeds `/api/me` and fails only the data endpoints — the gate screens carry NO TMDB attribution ON PURPOSE, since they render before the router, show no TMDB-derived field of any kind, and putting TMDB's mark in front of a stranger mid-refusal is not what the licence asks for. ⚠ **FINDINGS, reported not resolved:** the id for the 403 refusal is inconsistent ACROSS the two specs, and `T-META-005a` is what forced this to be got right. `specs/ux-states.md` §2.11 assigns the state ~~`T-UX-020`~~, and `specs/testing.md`'s own TASK-024 correction note says that systematic allocation "is the original and wins" — yet the row `testing.md` actually DEFINES for this state is `T-UX-025`, and ~~`T-UX-020`~~ has no definition row anywhere, so citing it fails the phantom-id gate outright. `T-UX-025` is used here because it is the id `testing.md` defines and NFR-003 makes `testing.md` authoritative for the AC-to-test mapping; `ux-states.md` §2.11 should be re-pointed to it. (An earlier note in this repo described these as two live tests under one id — that was wrong: the offline pair was renumbered to `T-UX-023`/`T-UX-024` and the old id struck through at `specs/testing.md` L1600.); the SPA still mounts NO error boundary, so any container that throws unmounts the whole tree including the TMDB attribution; and `GATE_CHECKING`/`GATE_FAILED_TITLE`/`GATE_FAILED_BODY` are invented copy pending owner review, like `IDP_FAILURE_TITLE`/`_BODY`. | `T-SEC-018`, `T-UX-019`, `T-UX-025` |
| `TASK-029` | `apps/api/test/integration/security.spec.ts` — delivered with the auth chain and never recorded. The route walk is enumerated from the live Express router, so `GET /api/titles`, `/api/titles/:titleId` and the `/api/batches` routes are covered without editing it. `T-SEC-002` (a–h), `T-SEC-017` (a–d), `T-SEC-028` (a–c), `T-SEC-030` (d–g). `T-SEC-002f` is the mutation: a deliberate `/api/leak/:id` answering 403 for a foreign id, which the walk catches. | `T-SEC-002`, `T-SEC-017`, `T-SEC-028`, `T-SEC-030` |
| `TASK-030` | `tools/check-no-credentials.mjs` + `tests/infra/noCredentials.spec.ts` — `T-SEC-011` (a–k) and `T-SEC-001` (a–m). Mutation-proven against `passport`, `bcrypt`, a `puppeteer` runtime dependency, a streaming host literal and a schema credential field; with false-positive cases (`passwordless`, the SQL connection string, the `netflix`/`max` enum values, and `@playwright/test` in devDependencies staying legal). Wired as `npm run check:credentials`. | `T-SEC-001`, `T-SEC-011` |
| `TASK-031` | **Verified, not written — `TASK-007` had already delivered it.** `tests/smoke/smoke.spec.ts` carries `T-SMOKE-001`–`004`, and `.github/workflows/deploy.yml` already runs `npm run test:smoke` against **the new revision's own per-revision FQDN at 0% traffic** (steps "Resolve the new revision and its private FQDN" → "Production smoke suite"), with an explicit `::error::` guard refusing to smoke a URL that might be the old revision — which is the whole substance of this task. **Proven live rather than assumed:** 4/4 pass against `ca-nextup-staging`, and **4/4 fail against `https://example.com`** — a mutation check, because a smoke suite that passes against an unrelated host asserts nothing. ⚠ **Two findings in `specs/testing.md` (hard stop, report only):** (1) §11's authoritative layout names `tests/smoke/deployed.spec.ts` but the delivered file is `smoke.spec.ts`, and §18.2 line 3441 names `smoke.spec.ts` — the binding layout block disagrees with both reality and the same document; (2) **the second smoke case is implemented but has no definition** in `specs/testing.md` — 001/003/004 each have one, ~~`T-SMOKE-002`~~ does not. It is struck through here only so this gate stops counting it as a done-when; it is **undefined, not superseded**, which is the finding. `check:test-ids` could not have caught it on its own, because it validates only ids *cited by the backlog*, and an implemented-but-unspecified test is cited by nobody. Not renamed and not defined here: fixing either requires editing `specs/testing.md`. | `T-SMOKE-001`, `T-SMOKE-003` |
| `TASK-032` | `tests/fixtures/seed.ts` (fixture) + `tests/infra/seedFixture.spec.ts` — a seed fixture with an injected clock, a pure `planSeed()`, `asOwner()` and owner-scoped writes under the DERIVED `ownerId`. `T-SEED-001` (a–e) determinism, `T-SEED-002` (a–d) derived identity, `T-SEED-003` (a–e) the clock as the only time source, per `specs/testing.md` §14.1. The original done-when `~~T-META-003~~` was a mis-citation (`A48`); it is delivered separately as `tools/check-decision-verifiability.mjs` + `tests/meta/decisionVerifiability.spec.ts`. | `T-SEED-001`, `T-SEED-002`, `T-SEED-003` |
| `TASK-033` | `T-LIST-010`, `T-LIST-011`, `T-API-017` | `T-API-017`, `T-LIST-010`, `T-LIST-011`, `T-LIST-012`, `T-LIST-013`, `T-LIST-019` |
| `TASK-034` | `T-LIST-028` — `apps/api/test/integration/titleDetail.spec.ts` (8 cases): owner scoping, and the foreign-id refusal asserted byte-identical to the unknown-id refusal with `T-LIST-028g` as its non-vacuity guard. `T-LIST-035` — `apps/api/test/unit/titlesShape.spec.ts` (8 cases) for the §6.3 shaping, where coverage is measured. The active/removed badge split is mutation-proven at both layers. | `T-LIST-028`, `T-LIST-035` |
| `TASK-035` | `tools/check-write-once-date-added.mjs` + `tests/infra/writeOnceDateAdded.spec.ts` — `T-INV-006` (a–n), a static gate proving no assignment to `.dateAdded` and no `dateAdded` key in a Prisma `update`/`updateMany`/`upsert` exists outside `createServiceListing()`. Three mutations of the checker itself caught (loosened `dateAddedEdited` lookahead, exemption failing open, `create` treated as mutating). Server-side `dateAddedLabel` was already delivered and is asserted by `T-LIST-011c`. `~~T-LIST-018~~` relocated to TASK-038 — see `specs/testing.md` §19. | `T-INV-006` |
| `TASK-036` | `packages/domain/src/ordering.ts` + `packages/domain/test/ordering.spec.ts` (19 cases) + `apps/api/test/integration/titleOrdering.spec.ts` (16 cases) — `T-LIST-014`, `T-LIST-015`, `T-LIST-016`, `T-LIST-017` (U) and `T-LIST-025`, `T-LIST-026`, `T-LIST-027` (I); `T-LIST-010` was already delivered by TASK-033. **Two live ordering bugs fixed**, both silent: the tie-breaker was `{ id: dir }`, so tie order flipped when the owner reversed the sort AND the keyset predicate stopped mirroring the `ORDER BY`, dropping rows between pages; and `nulls` placement relied on SQL Server's default, which is last on `desc` and **first** on `asc`. Five mutations caught — three of the query, two of the comparator. Four orphaned ids adopted; see `specs/testing.md` §20. | `T-LIST-010`, `T-LIST-014`, `T-LIST-015`, `T-LIST-016`, `T-LIST-017`, `T-LIST-025`, `T-LIST-026`, `T-LIST-027` |
| `TASK-037` | `apps/api/test/integration/titleFilters.spec.ts` (26 cases) — `T-LIST-020` (a–e), `T-LIST-021` (a–d), `T-LIST-022` (a–h), `T-LIST-023` (a–e), `T-LIST-024` (a–d). **The genre filter did not exist**: `genre` was parsed, validated, and then never passed to the query, so `?genre=Comedy` returned 200 and listed everything. Implemented as a quoted-token match inside the JSON column, which makes `genres: []` exclusion (AC-6) fall out by construction. Four mutations caught, including the sibling-`OR` hazard that would have dropped the filter on page 2 only. `T-LIST-021`/`T-LIST-022` were orphaned ids, adopted here — `specs/testing.md` §20.1. | `T-LIST-020`, `T-LIST-021`, `T-LIST-022`, `T-LIST-023`, `T-LIST-024` |
| `TASK-038` | `components/TitleRow.tsx` + `components/TitleList.tsx` + `pages/ListPage.tsx` — `T-UI-010` (a–k), `T-LIST-018` (a–d), `T-UI-012` (a–d). Three mutations caught: a component-built date label (fails `T-LIST-018a/b/c` + `T-UI-010a`), one row per listing instead of per work (fails `T-UI-010e`, `T-LIST-018b/c`, `T-UI-012d`), a credentialed `netflix.com` anchor in a badge (fails all four `T-UI-012`). ⚠ **Deep links out to the service are NOT built** — this row and `T-UI-012`/US-038 AC-3 presume one, but `specs/ui.md` §2.2 lists no such element and `specs/api.md` §6.2 carries no URL field, so a URL scheme would have to be invented. Built the conservative reading instead: no row addresses a streaming host at all, asserted universally by `T-UI-012b`. Needs an owner decision. | `T-LIST-018`, `T-UI-010`, `T-UI-012` |
| `TASK-039` | `components/FilterBar.tsx` — `T-UI-016` (a–n), `T-UX-013` (a–e). The query string is the **only** store of filter state: there is no `useState` mirror, so the two directions `T-UI-016` demands are one direction of data flow rather than two that can drift. `T-UI-016b` is the case a write-only implementation passes today and fails on a shared link; `T-UI-016g`/`l` prove `sort`/`dir` survive a filter change, because rebuilding the query string from the filters alone would silently reset REQ-038's oldest-first escape hatch on the first checkbox click. Values repeat (`service=netflix&service=max`) rather than comma-joining, which the API would reject; unknown values are dropped rather than forwarded, so a stale link degrades to a wider list instead of a 400. `T-UX-013b` asserts the zero-match copy cannot read as data loss. Seven mutations caught. **The sort control is deliberately NOT stubbed here** — it is TASK-166 and `ui.md` gives it the opposite persistence rule (client view state, not re-derived from the URL). New copy constants `CLEAR_FILTERS_LABEL`, `ZERO_MATCH_TITLE`, quoted from `ux-states.md` §2.4, which `ui.md` §9 has no rows for. | `T-UI-016`, `T-UX-013` |
| `TASK-040` | `components/ListEmptyState.tsx` — `T-UX-012` (a–d), `T-UX-014` (a–d), `T-UX-018` (a–e). The three "nothing to show" states are chosen by `listEmptyKind()` **from the facts**, not by a caller passing a message — the tempting shape (one `EmptyState` with a `message` prop) is exactly the shape that lets the wrong one through, and all three render fine while only one is true. Order is load-bearing and tested: the filter check runs **first**, so an owner who filtered everything out never reads the first-run message; and a single removed or suppressed title makes "Nothing here yet" false (`T-UX-012b`). `T-UX-018a` asserts the failure message keeps *"Nothing has changed"* — a failed read is the one moment the owner cannot verify the no-silent-loss promise themselves — and `T-UX-018e` proves the filter bar is not rendered over it, since its live count would have to invent numbers the API never returned. Six mutations caught, including collapsing all-gone into never-uploaded, dropping the filter check, pointing both recovery links at `/removed`, and downgrading `role="alert"`. New copy constants `LIST_EMPTY_NEVER_UPLOADED_TITLE`/`_BODY`, `UPLOAD_SCREENSHOTS_LABEL`, `LIST_EMPTY_ALL_GONE_TITLE`, `LIST_LOAD_FAILED_BODY`, `RETRY_LABEL` from `ux-states.md` §2.3/§2.5/§2.9 prose; `ui.md` §9 has no rows for any of them. | `T-UX-012`, `T-UX-014`, `T-UX-018` |
| `TASK-041` | `apps/api/src/routes/serviceState.ts` + `packages/domain/src/freshness.ts` — `GET /api/service-state`, one entry per service in `SERVICES` so a never-captured service reads "has never been updated" rather than vanishing. `ageInDays` counts UTC calendar days, not 24-hour blocks, and clamps clock skew. `T-FRESH-010` (a–h), `T-FRESH-012` (a–f), `T-FRESH-015` (a–c, the A46 no-nudge regression guard). All four mutations caught; see `specs/testing.md` §16. | `T-FRESH-010`, `T-FRESH-012`, `T-FRESH-015` |
| `TASK-042` | `components/FreshnessStrip.tsx` + `pages/ListPage.tsx` wiring — `T-FRESH-014` (a–i), 100% covered. Degrades **visibly**: a `role="status"` notice plus per-chip unknown labels, and a partial payload degrades only the missing service. Three mutations caught: rendering nothing when the payload is missing (fails `T-FRESH-014b/c/e/g` + `014i`), reporting missing data as "never updated" (fails `T-FRESH-014c`), nag wording in the degraded copy (fails `T-FRESH-014h`, the `A46` guard). ⚠ **`FRESHNESS_UNAVAILABLE` is invented copy pending owner review** — `T-FRESH-014` requires a visible degraded state but `specs/ui.md` §9 and `specs/ux-states.md` §2 supply no wording. ⚠ **`/upload` does NOT yet consume `?service=`** — the chips link there (asserted by `T-FRESH-014i`) but REQ-039's "pre-selecting that service" has no test id in `specs/testing.md` §9's US-022 table (AC-1/3/4/5 only) and `T-META-004` forbids an unnamed test; **a new id is requested from the coordinator — see the FINDING in `apps/web/test/freshnessStrip.spec.tsx`** (not cited here: `check:test-ids` rightly rejects an id `specs/testing.md` does not define). | `T-FRESH-014` |
| `TASK-043` | `apps/api/src/services/tmdbRefresh.ts` + `updateTitleMetadata` in `apps/api/src/repository/ownerData.ts` + both read handlers in `apps/api/src/routes/titles.ts`; `apps/api/test/integration/tmdbRefresh.spec.ts` — `T-TMDB-004` (a–f), `T-TMDB-014` (a–b), `T-TMDB-015` (a), `T-TMDB-016` (a–b), 11 tests, against a real engine and `msw`-recorded TMDB. `TMDB_REFRESH_BUDGET_MS = 5_000`, `TMDB_REFRESH_CONCURRENCY = 4`, horizon from `TMDB_METADATA_MAX_AGE_DAYS`. Triggered by the read of the rows it refreshes and scoped to the page returned (REQ-076), so it stays inside the `T-CI-005` exemption — the budget is a "stop starting new work" check before each item, deliberately **not** a timer. Six mutations applied and each caught by its named test (null `fetchedAt` treated as fresh, horizon widened, refreshed values not served on the fetching render, unmatched rows swept, the write widened to `sortDateAdded`, `imdbId` written unconditionally). ⚠ **The refresh is awaited BEFORE the response, unlike `beginRatingRefresh`** — `metadataStale` has to be on the item being served, so the success/failure decision must precede the JSON; the asymmetry is commented in both files. ⚠ **`tmdbFetchedAt === null` counts as STALE** and that is load-bearing: `closeBatch`'s `insertTitle` writes name/year/poster but no runtime, no genres and no `fetchedAt`, so treating null as fresh would leave every closed batch's rows permanently without a runtime or genre. ⚠ **A `null` `imdb_id` from TMDB means UNCHANGED, never cleared** (the committed `movie.438631.json` fixture carries none) — `T-TMDB-014b` guards it. ⚠ **FINDING — `T-TMDB-011` is not satisfied at CLOSE time**: `insertTitle` never writes runtime, genres or `fetchedAt`, so "stores exactly type, year, runtime, genres, poster path, tmdbId, fetchedAt" holds only after the first display repairs the row. Reported, not silently fixed — the write set at close is TASK-057/073 territory. ⚠ `findTitlesWithStaleMetadata` in `ownerData.ts` is now redundant and still has zero callers. **Was ahead of TASK-045 (now `done`)** — was deliberate: TASK-045's client (`apps/api/src/clients/tmdbClient.ts`) and its route are delivered and registered (`routes/index.ts:136`), and `getWork` is the entire surface this refresh consumes; TASK-045's last open piece, `T-AI-017`, is now closed (`T-AI-017a`) and the refresh never touched it. | `T-CI-005`, `T-TMDB-004`, `T-TMDB-016` |
| `TASK-044` | `tests/infra/no-scheduler.spec.ts` — `T-CI-005` (a–i), 9 tests. Static gate over `apps/api/src/**` (timers, scheduler/job-queue packages, five-field cron expressions, timer/queue/event triggers), `.github/workflows/**` (`schedule:`), `infra/**` (`Microsoft.Sql/servers/jobAgents`, `Microsoft.Scheduler`, `Microsoft.Logic/workflows`, `Recurrence`) and `prisma/migrations/**` (`sp_add_job`, `sp_add_jobschedule`, `WAITFOR DELAY`), plus the three-process count read from `PERMITTED_BACKGROUND_PROCESSES`. Five mutations applied and each caught by its named test (a real `setInterval` in `config.ts`, a `schedule:` trigger on `ci.yml`, a `jobAgents` resource in `aca.bicep`, an `sp_add_job` in migration `0005`, a fourth permitted background process). ⚠ **THE ID WAS BEING CITED BEFORE IT EXISTED.** `specs/testing.md` defines `T-CI-005` in three places (US-010 AC-5, US-036 AC-2/AC-5) and several rows cited it as delivered evidence, but nothing in the suite implemented it — TASK-171 already recorded the discovery and this row closes it; the reported gate is now a real one. ⚠ **`setTimeout` is deliberately NOT banned** (`T-CI-005i` asserts the gate stays precise): backoff and the TMDB rate gate both need it, and a blanket ban on a construct the codebase legitimately requires produces a gate that is switched off rather than obeyed. What makes something a scheduler is that it *recurs on its own*. ⚠ **Scoped to `apps/api/src`, not `apps/web/src`** — a browser poll only runs while the owner is looking at the page, so it is owner-initiated by construction; invariant 5 is about processes the *server* runs without the owner. ⚠ Comments are stripped before matching, never suppressed by filename: `jobs/runExtraction.ts` discusses the ban in as many words, and a gate that fired on its own documentation would be deleted within the week. ⚠ Hand-rolled `readdirSync` walker rather than a `glob` dependency — NFR-004 and the allow-list make a new package the expensive option. | `T-CI-005` |
| `TASK-045` | Client, route handler and `msw`-served recorded fixtures delivered; `T-TMDB-010`…`010z` pass (client `a`–`o`, `v`–`z`; route `p`–`u`, in `apps/api/test/unit/clients/tmdbSearchRoute.spec.ts` + `tmdbClient.spec.ts`). The client suite runs on the REAL `fetch` at the real TMDB origin through `msw`, so request construction and API-key placement are actually asserted. `registerTmdbRoutes(...)` is registered in `apps/api/src/routes/index.ts` (inside the auth chain, so within `T-SEC-029`'s scope); `TMDB_UNAVAILABLE` → 502 is asserted by `T-TMDB-010q`. **`T-AI-017` CLOSED (verified 2026-08-30):** it exists as `T-AI-017a` in `apps/api/test/integration/batchCandidates.spec.ts` (delivered by commit `ac5137c`, #139), an integration assertion over the *matching* pipeline — a chrome-suspected candidate whose `reclassifyAsTitle` search hits a TMDB 503 (via the `__tmdb_unavailable__` fixture token) leaves the candidate unmatched (`resolvedWorkIdentity` null, `matchCandidates` null), the batch still reaches `in-review`, the review banner is shown and no failure surfaces; `calls` length 3 pins the 503 + two retries. Run green against a real `mssql/server:2022`. The earlier "cannot exist until the cross-check merge lands" note was stale — the merge landed and the test was written. **Verified mechanically, not trusted:** two load-bearing branches mutation-tested — (1) `tmdbClient.ts` 5xx-retry condition `response.status >= 500` broken to `>= 5000` → killed by `expect(calls).toHaveLength(3)` (retry path exercised); (2) `batchCandidates.ts` `applyReclassify` outage-swallow `if (error instanceof TmdbUnavailableError) return;` flipped to re-throw → killed by `expect(res.status).toBe(200)` (got 500; the swallow is load-bearing). Both mutants restored by edit and confirmed clean (`git diff --stat` empty), test re-run green. No production code changed — TASK-045 was already complete on `main`; this row corrects a stale `doing`. ⚠ **FINDING (reported, not fixed):** a thrown `TmdbUnavailableError` on the PATCH candidates route surfaces as **500**, not the 502 `TMDB_UNAVAILABLE` the `/tmdb/search` route maps it to — harmless today because `applyReclassify` swallows it by design, but if that swallow is ever removed the candidate route would leak a bare 500 rather than the closed error code; the mapping lives in `apps/api/src/routes/batchCandidates.ts` (another lane's file), so left for its owner. | `T-AI-017`, `T-TMDB-010` |
| `TASK-046` | `tests/e2e/attribution.spec.ts` (**new**, shared with TASK-026) — `T-ATTR-002a`–`c` and `T-ATTR-003a`–`b`, 26 cases per engine, **52 green across Chromium and Mobile Safari**. Four mutants applied and each killed **by its named test** (`<TmdbAttribution />` removed from the footer → 25 of 26; `text-overflow: ellipsis` + `white-space: nowrap` on the disclaimer → `T-ATTR-004a` alone; `TMDB_LOGO_PATH` pointed at a missing asset → exactly the ten `T-ATTR-003a` cases, with every disclaimer assertion still passing; the disclaimer wrapped in a `<details>` expander → the ten `T-ATTR-002b` cases plus `T-ATTR-002c` and `T-ATTR-004a`, with the logo assertions untouched). ⚠ **THE ROUTE LIST IS ENUMERATED FROM `ROUTES`, NEVER RETYPED.** `specs/testing.md` §9A is explicit that these suites keep passing over a route they no longer cover if they hold their own literal list; `T-UI-023a` pins `ROUTES` itself and `T-ATTR-002a` pins the shape here (nine routes plus the catch-all), so the two per-route loops cannot become self-fulfilling. The catch-all is covered too: the obligation is not conditional on a route being a "real" one. ⚠ **`toBeVisible` ALONE PASSES OVER A 404'd LOGO.** A broken `<img>` still has a bounding box (its alt text), still matches the locator and still reports visible — so `T-ATTR-003a` asserts `naturalWidth > 0` and `complete`, and `T-ATTR-003b` exists because US-011 AC-5's "a build without it fails" is a claim about the **test**: it serves the asset as a 404 and requires `naturalWidth` to be 0, so the check cannot later be weakened back to a visibility test without something noticing. ⚠ **A GENERIC `{}` STUB BLANKED TWO ROUTES AND LOOKED LIKE A PRODUCT DEFECT.** `/batches/:batchId` and `/batches/:batchId/review` read fields off the response, throw on a malformed 200, and — because **the SPA mounts no error boundary** — React unmounts the entire tree including the footer; the suite reported a missing attribution on two of nine routes when the fault was the fixture. Those two paths are now served a real `BATCH_NOT_FOUND` envelope, which is what they would genuinely meet at that id. **FINDING, reported not fixed: there is no error boundary anywhere in the SPA**, so any container that throws takes the whole application down to a blank page — including the licensing attribution, whose failure `ui.md` §8 notes is invisible from inside the product. ⚠ **`T-ATTR-002c` asserts the disclaimer survives an API 500**, because a compliance statement gated on a fetch is absent exactly when the owner is most likely to be looking at the screen. **Depends on TASK-026 and TASK-038, both done.** | `T-ATTR-002`, `T-ATTR-003` |
| `TASK-047` | `prisma/migrations/0002_perf_indexes/` (the five §16.6 indexes) + `apps/api/test/integration/queryPlan.spec.ts` (12 cases) — `T-PERF-001` (a-i), `T-PERF-003` (a-c), plus `listRemovedListingPage` / `searchRemovedListings` / `escapeLikeTerm` in the repository. Plans are read from `sys.dm_exec_query_plan`, NOT `SET SHOWPLAN_XML ON`, because Prisma pools connections and a session-scoped plan setting captures the wrong statement. Two of my own errors caught by the harness: a plan-cache clear placed before its own measurement manufactured a phantom 296-vs-50 regression and sent one round of optimisation after nothing, and the resulting "sargable leading predicate" was measured at depth 15,000, found to make no difference, and removed. Search is deliberately NOT index-backed (no `pg_trgm` on Basic); `escapeLikeTerm` escapes its own escape character first. See `specs/testing.md` §23. | `T-PERF-001`, `T-PERF-003` |
| `TASK-048` | `T-BATCH-010`, `T-BATCH-015` | `T-API-003`, `T-BATCH-010`, `T-BATCH-015` |
| `TASK-049` | `T-UI-003a`…`j` green in `apps/web/test/uploadStep1.spec.tsx`; mutation-proven against a defaulted mode and against revealing the consequence on selection | `T-UI-003` |
| `TASK-050` | `T-IMG-002`, `T-IMG-006`, `T-IMG-010`, `T-IMG-012`, `T-IMG-018`, `T-IMG-023`, `T-PASTE-003`, `T-PASTE-005`, `T-PASTE-006`, `T-PASTE-007`, `T-SEC-003`, `T-RET-014`; ~~`T-IMG-013`~~ ⚠ **DEFECT FOUND AND FIXED LATER: AN OVERSIZED IMAGE RETURNED 500 `INTERNAL_ERROR`.** `multer` is configured with `limits.fileSize = MAX_IMAGE_BYTES + 1`, and the comment above it claimed the extra byte existed so that "multer must let the file through for the pipeline to judge it" and emit a per-file `rejected[]` entry. **That window is exactly ONE BYTE WIDE.** Anything genuinely oversized — an 11 MB paste, which is what `T-PASTE-007` requires — aborted the multipart stream, and the resulting `MulterError` was mapped by nothing, so `errorEnvelope`'s catch-all turned it into **500 `INTERNAL_ERROR`** where `specs/api.md` §5.3.1 requires **413 `IMAGE_TOO_LARGE`**. The owner was told that nextup had broken when in fact their file was too big — a message that is both false and unactionable, on the one screen where they are adding data. Fixed by `mapMulterError` + the `acceptUploads` wrapper in `apps/api/src/routes/batchImages.ts`, which translates `LIMIT_FILE_SIZE` → 413 `IMAGE_TOO_LARGE`, `LIMIT_FILE_COUNT` → 400 `TOO_MANY_FILES_IN_REQUEST` and `LIMIT_UNEXPECTED_FILE` → 400 `VALIDATION_FAILED`; every other error is returned unchanged so nothing real is swallowed. `T-IMG-010k`/`T-IMG-010l` in `apps/api/test/unit/batchImagesRoute.spec.ts`. 2/2 mutants killed: restoring the bare `upload.array('files')` failed both cases with **expected 500 to be 413**, reproducing the shipped defect exactly, and disabling only the `LIMIT_FILE_SIZE` arm failed both. ⚠ **`T-IMG-010l` RE-RUNS THE ASSERTION WITH `ingestSource=paste`** — a fix applied on the upload path only would leave paste returning 500 while every upload-path test stayed green (product invariant 17). ⚠ **THE MISLEADING COMMENT WAS CORRECTED IN PLACE AND THE OLD TEXT STRUCK THROUGH, NOT DELETED** — it described a behaviour the code did not have, which is the most expensive kind of comment to leave standing. ⚠ **KNOWN NARROWING, REPORTED NOT HIDDEN:** the 413 is whole-request, so valid files attached alongside an oversized one are refused with it rather than partially accepted per US-004 AC-6. Multer aborts the stream on the offending part, and recovering the others means buffering past the ceiling — precisely what the 0.5 GiB budget (product invariant 14, REQ-079) forbids. The pipeline's per-file `IMAGE_TOO_LARGE` still covers every file under the backstop. ⚠ **FOUND BY THE TASK-163 INGEST-PARITY LANE, WHICH HARD-STOPPED RATHER THAN REACHING INTO `apps/api/src/**`** — the lane discipline worked exactly as intended: the finding was reported, and the production fix was made by the owner of that path. | `T-IMG-002`, `T-IMG-006`, `T-IMG-010`, `T-IMG-012`, `T-PASTE-003`, `T-PASTE-005`, `T-PASTE-006`, `T-PASTE-007` |
| `TASK-051` | `T-IMG-006g`-`i`, `T-INV-012a`-`e` (`apps/api/test/unit/deleteBatchImage.spec.ts`, 12 cases), `T-INV-012g`-`j` (`tests/infra/hardDelete.spec.ts`, 4 cases). Route in `apps/api/src/routes/batchImages.ts`, repository in `ownerData.ts`. 4 mutants, all caught. Spec §28.1a. | `T-IMG-006`, `T-INV-012` |
| `TASK-052` | `apps/api/src/routes/images.ts` (`GET /api/images/:imageId`, `specs/api.md` §6.27) + `findUploadedImageById` and `contentTypeFor`. `T-IMG-002i`-`m`, `T-IMG-004a`-`d`, `T-IMG-005a`-`b`, `T-SEC-002g`-`h`, `T-SEC-003h`-`i` (`apps/api/test/unit/imagesRoute.spec.ts`, 15 cases); `T-IMG-002n`-`o`, `T-IMG-004e`-`g`, `T-IMG-005c`, `T-SEC-002i`-`j`, `T-SEC-003j` (`apps/api/test/integration/imageServe.spec.ts`, 9 cases). ⚠ **Three absences, two answers, and never a 500.** A purged image's NORMAL steady state is a live row pointing at bytes that are gone — the lifecycle rule writes nothing back (REQ-028 keeps the row forever) — so `T-IMG-005` treats a missing blob as 410 `IMAGE_EXPIRED`, identical to a passed `retainUntil`; a 500 there would fire an alert every day from day 31. `T-IMG-004e` deliberately leaves the blob IN PLACE while moving `retainUntil` into the past, so the assertion is the APPLICATION boundary and cannot pass for `T-IMG-005`'s reason instead. `isExpired` is exported and its `<=` boundary asserted directly (US-035 AC-1 is "**at** `retainUntil`"), because no HTTP request can pin the clock to the millisecond. Lookup is by id alone but keeps `ownerId` in the predicate — a `findUnique({ id })` would serve another owner's screenshot and look identical in any single-owner test. `res.end`, not `res.send`, so no `ETag` invites a conditional request for bytes marked `no-store`. | `T-IMG-004`, `T-SEC-003` |
| `TASK-053` | `components/ImageDropzone.tsx` — `T-UI-004` (a–e), `T-UX-041` (a–e), `T-UX-042` (a–f). All three affordances render at once (paste button when `navigator.clipboard.read` exists, `Choose files` always, drop target); one `addFiles` path with the source reported but never branched on. Validation is lenient — empty **and** `application/octet-stream` types are accepted and left to the server sniff. Four mutations caught: HEIC dropped from `accept` (`T-UI-004a`), hard-filtering on `File.type` (`T-UI-004d`), the paste affordance replacing rather than joining file selection (`T-UX-041a/b/c`), rejections hiding the accepted list (`T-UX-042a/e`). New copy constants `UNSUPPORTED_FORMAT_REJECTION`, `IMAGE_ACCEPT_ATTRIBUTE`, `CHOOSE_FILES_LABEL`, `HEIC_PREVIEW_PLACEHOLDER` — quoted from `specs/ui.md` §3.2 prose, which §9 has no rows for. `PasteCapture` (TASK-160) and the drop target's full behaviour (TASK-162, `T-UI-014`) fill the slots; the ceiling messages are rendered here but the ceiling-copy test id named on TASK-162 is not defined in specs/testing.md, so it is reported as a spec defect rather than cited here. | `T-UI-004`, `T-UX-041`, `T-UX-042` |
| `TASK-054` | `apps/api/src/services/batchLifecycle.ts` — the transition table, `submitBatch`, `discardBatch`, `assertBatchMutable`, plus `transitionUploadBatchStatus` (the conditional write). Routes `POST /api/batches/:batchId/submit` (§6.14) and `/discard` (§6.23). `T-BATCH-017a`…`h` (table totality, one-way-ness, discardable set), `T-BATCH-019a`…`d` (the submit endpoint's status codes), `T-BATCH-018a`…`c` (atomic transition — `018a` mutation-verified after its first form proved vacuous), `T-BATCH-013a`…`d` (immutability: the guard **and** the absence of a mutating route), `T-BATCH-006a`…`f` (a discarded batch writes nothing, retains images, releases the ceiling). ⚠ **The three close/reconciliation ids this row originally cited are NOT delivered here** — they belong to TASK-072 and no close endpoint exists yet; see `specs/testing.md` §24.3. Two spec gaps reported in §24.2. | `T-BATCH-006`, `T-BATCH-013`, `T-BATCH-017`, `T-BATCH-018`, `T-BATCH-019` |
| `TASK-055` | `packages/domain/src/extraction/` (contract + degraded projections) + `apps/api/src/extraction/` (recordings, `StubExtractor`, factory). `T-STUB-001a`…`r`, incl. the three fault tokens and byte-identical output over three runs. | `T-STUB-001` |
| `TASK-056` | `AzureVisionExtractor` (`apps/api/src/extraction/azureVisionExtractor.ts`) + offline `msw` contract suite (`T-AI-033a`–`s`, recordings in `tests/fixtures/msw/vision/`) + static boundary gates (`T-AI-009a`–`j`, `T-AI-010a`–`d`). Retry/timeout policy is implemented LOCALLY with an injectable `sleep` and the SDK's own retry pipeline disabled, because two retry layers compose multiplicatively against a 5,000/month free tier. ⚠ In standalone `azure-vision-read` mode `extract()` always reports `crossCheck: 'llm-unavailable'` — the primary reader is deliberately not called, so the read genuinely was never corroborated, and reporting `ok` would let a strictly-worse read propose mass removals. The ADR-0001 Rev 1 revert path therefore runs in degraded mode (§2.2a) and withholds removals. `T-AI-009` request half + `T-AI-010` land here; the `LlmVisionExtractor` half of `T-AI-033` is TASK-056b. | `T-AI-009`, `T-AI-010`, `T-AI-033` |
| `TASK-056b` | `LlmVisionExtractor` (`apps/api/src/extraction/llmVisionExtractor.ts`) + committed prompt/schema (`prompts.ts`) + offline `msw` contract suite (`T-AI-033t`–`an`, recordings in `tests/fixtures/msw/aoai/`) and the `openai` half of the boundary gate (`T-AI-010e`–`g`). ⚠ Three judgement calls, all load-bearing. (1) `finish_reason: 'length'` is checked BEFORE the body is parsed — the truncation fixture carries *valid JSON with one complete tile*, so a parse-first implementation returns one title and looks entirely successful. (2) A schema-invalid or non-JSON body is TERMINAL, not retried: §2.2's retry set is explicit and exclusive (429/5xx/network only), and at `temperature: 0` with a fixed seed and strict Structured Outputs a repeat is near-deterministic, so retrying would spend the batch ceiling to get the same answer. (3) In standalone `llm-vision` mode `extract()` reports `crossCheck: 'ocr-unavailable'`, which — deliberately unlike TASK-056's `llm-unavailable` — still PERMITS removals: the primary, higher-quality reader did run, so a title's absence is evidence. SDK retry is disabled (`maxRetries: 0`) and §2.2's policy implemented locally with an injectable `sleep`, for the same multiplicative reason as TASK-056. | `T-AI-011b`, `T-AI-033`, `T-AI-040`, `T-AI-044` |
| `TASK-056c` | `packages/domain/src/extraction/crossCheck.ts` + `apps/api/src/extraction/hybridExtractor.ts`: deterministic cross-check merge with geometry-scoped `exact`/`partial`/`none`, OCR orphan recovery, OCR-vs-LLM box selection and total ordering; hybrid extractor issues the LLM and OCR legs in parallel and carries asymmetric degraded states. Tests: `packages/domain/test/crossCheck.spec.ts` (`T-AI-034` a–l, `T-AI-039` a–b), `packages/domain/test/cleanup.spec.ts` (`T-AI-032` a–b), `apps/api/test/unit/extraction/hybridExtractor.spec.ts` (`T-AI-036` a–h), plus runner-level `T-AI-036` already present in `apps/api/test/unit/runExtraction.spec.ts`. Four mutants applied and each killed by its named test: exact threshold made unreachable → `T-AI-034f`; OCR orphan loop skipped all unconsumed lines → `T-AI-039a/b`; unsupported-LLM predicate inverted → `T-AI-032a/b`; hybrid legs made sequential → `T-AI-036b`. ⚠ **Spec defects reported, not edited:** `specs/ai.md` §2.1c still named ~~`apps/api/src/extraction/crossCheck.ts`~~ (**since corrected in place**, along with three sibling paths and the §2.1c "survives the §3.2 gates" wording, and `T-INFRA-014` now fails CI on the whole class), but the work order and implementation place the pure merge in `packages/domain`; §2.1c step 2 says OCR orphans must "survive" cleanup gates even though §3.1/§3.2 define cleanup as classify-and-surface, never filter, so the implementation emits every unconsumed OCR line and lets `cleanup.ts` classify it. | `T-AI-032`, `T-AI-034`, `T-AI-036`, `T-AI-039` |
| `TASK-059` | `ahead-of:TASK-058` — TASK-058 owns the extraction **runner** and its progress reporting; this is the level-`C` page that renders progress, and it takes its state as props, so nothing here waits on the runner. `apps/web/src/pages/BatchStatusPage.tsx` — `T-UX-007` (a–t, 20 cases) and `T-UX-008` (a–j, 10 cases) in `apps/web/test/batchStatusPage.spec.tsx`. Props-driven like every other page here: the poll interval, its pause while `offline`, and §5.4's auto-navigation belong to the container, because both ids are level `C` and **`GET /api/batches/:batchId` (api.md §6.15) has since been implemented by TASK-076** in `apps/api/src/routes/batchDetail.ts` (`T-BATCH-017` pins the SPA-calls-a-registered-route pairing) ~~"is still unimplemented — no route in `apps/api/src/routes/batches.ts` serves it, and no task owns it explicitly (it is implied by TASK-058's progress reporting)"~~ *(corrected in place: the route exists, and it is in `batchDetail.ts`, not `batches.ts` — a reader grepping the file this row names finds nothing and concludes the hole is still open)*. All nine §5 states render: queued vs running (chosen by `status`, **never** by `imagesDone === 0` — an extracting batch with nothing finished is not queued, `T-UX-007c`), per-image tiles that **name** the file as well as thumbnail it (US-006 AC-3), the §5.3 zero-yield count where `candidateCount === null` means *unread* and is deliberately not counted as a failure (`T-UX-007j`), and the three error states with their distinct actions — §5.5 `Try again` + `Discard batch`, §5.6 retry only (the batch is still good), §5.7 **no retry at all** because the blobs are gone under the 30-day purge and a retry could only loop (`T-UX-007p`). **`T-UX-008`: the degraded banner is now ONE shared string.** `ux-states.md` §5.9 requires the same banner on this page and on review; `reviewBanner` carried a second, differently-worded copy, so `DEGRADED_EXTRACTION_BANNER` was added to `packages/domain/src/copy.ts` and both surfaces import it (`T-UX-008h`), with `T-UX-008j` pinning it verbatim to §5.9 — without that pin the shared constant is invisible to every test that imports it. `ocr-unavailable` raises the banner too (§5.10, same wording, milder consequence — `T-UX-008b`), and it is non-dismissible (`T-UX-008g`) and survives completion (`T-UX-008f`). ⚠ **Two spec findings.** (1) ~~**§6.15's response shape carries neither `degradedExtraction` nor `crossCheck`**, so as documented the server sends the client nothing it could raise the banner from~~ **RESOLVED** — §6.15 now states both are *"REQUIRED, not optional"* and `batchDetail.ts` sends them. They were declared **optional** on `BatchStatus` so the page was correct the moment the fields arrived and merely silent until then; they have now arrived. (2) §5 specifies no page heading, so `STATUS_TITLE` is invented and flagged in-file. Mutation-tested: **28 mutants, 28 killed**; two survivors were fixed by adding `T-UX-007t` (a load failure **over a stale batch** — with no batch the two branches coincidentally agree, so the original case could not distinguish a frozen progress count presented as live) and correcting a bad anchor. | `T-UX-007`, `T-UX-008` |
| `TASK-059b` | `ahead-of:TASK-058` — TASK-058 is `doing` only because its golden-corpus half needs recorded provider responses that do not exist; the crop reads `boundingBoxes`/`boxSource` as they are ALREADY written by the extraction pipeline and served by `GET /api/batches/:batchId/review`, so nothing here waits on that. The **§5.3a CROP**. `T-AI-041a`–`e` predate this row and already proved a tile image is rendered beside an `inferred-unverified`/`unreadable-tile` candidate and wired to `GET /api/images` — but the image they asserted was **the whole screenshot**, while `CandidateCardProps.thumbnailUrl`'s own doc comment already claimed "the cropped tile": a contract that lied about itself. Adds `ReviewTileCrop` + `tileCropFor()` + `TILE_CROP_PADDING = 0.08` (`packages/domain/src/review.ts`), `tileCrop` on `ReviewCandidate`, a validating `parseBoundingBoxes()` in `apps/api/src/routes/batchReview.ts`, and a clipping wrapper in `CandidateCard.tsx` + `.candidate-card__thumb--cropped` in `index.css`. Tests: `T-AI-041f`–`n` (domain), `T-AI-041o`–`r` (render), `T-AI-041s`–`u` (stylesheet), `T-AI-041v`–`z` (integration), `T-AI-041aa`–`af` (`apps/api/test/unit/parseBoundingBoxes.spec.ts`). ⚠ **THE PARSER NEEDED A UNIT FILE AS WELL AS `T-AI-041y`, AND CI IS WHAT SAID SO.** `npm run coverage` runs `--project unit --project web` ONLY, so the parser's branches — reached exclusively from the integration project — read as uncovered and took `apps/api/src/**` branches to **84.88 % against the 85 % floor**. The floor was NOT lowered and the file was NOT excluded: the enumeration of malformed shapes is cheap in a unit file and costs a database round trip each in an integration one, so both tests earn their place. ⚠ **FINDING — TWO LATENT GATE DEFECTS, FOUND BY CI AND FIXED HERE.** `TEST_ID_RE` matches `[a-z]{0,2}`, but `baseId` (`tools/check-test-ids.mjs`) stripped a **single** trailing letter, so `T-AI-041aa` resolved to `T-AI-041a` — which the spec does not define — and `T-META-005a` reported a correctly-cited case as a phantom citation while its single-letter siblings passed. `isTestIdPresent` (`tools/check-status.mjs`) carried the same assumption in a different shape (`defined.length === testId.length + 1`), contradicting its own doc comment that *"a base id is satisfied by any of its variants"*: a base whose only delivered cases were double-lettered read as UNFINISHED. Both now accept one **or two** letters, and `T-META-005d` / `T-STATUS-001q` were extended to hold them there. ⚠ The second fix nearly broke the documented digit-mistype guard: `T-UI-023a` is a two-digit-truncated base plus the extra characters `3a`, so an **unanchored** `/[a-z]{1,2}$/` resolved a mistyped digit to a different criterion, and the existing case could not catch it because its extra is the bare digit `3`. Anchored at both ends, and `T-STATUS-001q` now asserts the lettered form of the mistype too. ⚠ **FINDING — A TEST ID WRITTEN AS PROSE IN THIS LEDGER IS INDISTINGUISHABLE FROM A CITATION.** The sentence above originally spelled that truncated base out literally, and `T-META-005a` correctly reported it as a phantom citation from `TASK-059`: the gate reads every `T-…` token in a row, and it must, because a citation is exactly a `T-…` token in a row. **When a row needs to discuss an id that is deliberately invalid, describe it — never write it.** ⚠ Bad boxes are **filtered, not rejected wholesale** (`T-AI-041ae`): the crop is the union of the boxes, so silently dropping the valid siblings of one bad entry narrows the region instead of falling back to the whole image — the failure mode that looks like it worked. ⚠ **THE CROP IS PRESENTATIONAL, DELIBERATELY.** The byte route serves whole screenshots and cropping server-side would put image processing on the request path of a 0.25 vCPU / 0.5 GiB container (REQ-079); the image is fetched whole either way, so the crop is a scaled, offset `<img>` inside an `overflow: hidden` box. ⚠ **A CROP IS OFFERED ONLY FOR `boxSource === 'llm'`.** An OCR box is a text-**line** strip, so cropping to it fills the thumbnail with the caption the owner is already shown as `rawText` and shows **none of the artwork** that is the entire reason §5.3a makes the thumbnail mandatory — and that failure is invisible to any test that only asserts "a crop was produced" (`T-AI-041h`/`x`). ⚠ **`tileCrop.imageId` beats `sourceImageIds[0]`**: after an SD-02 collapse a candidate's source images are not in the order its boxes were recorded, and cropping image A's rectangle out of image B shows a confidently-wrong region of the wrong screenshot (`T-AI-041q`). ⚠ **FINDING — MUTATION 4 SURVIVED FIRST TIME, AND IT WAS THE IMPORTANT ONE.** With the exhaustive domain suite and the render suite both green, replacing the route's `tileCropFor(...)` call with a hard `null` broke **nothing**: a perfect domain function that no route invokes is a feature that does not exist. `T-AI-041v`–`z` were written to close that seam — the same dead-wiring defect `thumbnailUrl` already shipped with once. ⚠ **FINDING — MUTATION 5 SURVIVED TOO, FOR A DIFFERENT REASON.** The padding assertions were written in terms of `TILE_CROP_PADDING`, so setting the constant to `0` still satisfied them; expressing an expectation in terms of the implementation's own constant makes the test recompute the implementation. Rewritten as literals. Also fixed: `CandidateCard` reads `candidate.tileCrop ?? null`, not `=== null`, because a response from the previous revision during a rolling deploy carries no such key and strict equality would take the crop branch and throw on `undefined.w`, blanking the review page; and the `candidate()` fixture builder now pins `tileCrop` explicitly, because a spread of `Partial<ReviewCandidate>` lets TypeScript accept a **missing required field** that then arrives as `undefined` at runtime. Five mutations, five killed after the two fixes. | `T-AI-041` |
| `TASK-060` | `packages/domain/src/matching/tmdbMatcher.ts` + `packages/domain/test/tmdbMatcher.spec.ts` (20 tests) — deterministic TMDB scoring on the SHARED `jaroWinkler` (`extraction/jaroWinkler.ts`), `MATCH_AUTO_THRESHOLD = 0.92`, `MATCH_REVIEW_FLOOR = 0.70`, `MATCH_AMBIGUITY_MARGIN = 0.05`, `YEAR_HINT_BONUS = 0.05`, `YEAR_HINT_PENALTY = 0.15`, `MATCH_ALTERNATIVES_LIMIT = 5`. `T-TMDB-010` (a–o), `T-TMDB-012` (a–e). Four mutations applied and each caught by its named test (year penalty dropped, tie-break reversed to the higher `tmdbId`, ambiguity-margin comparison flipped, alternatives cap removed). No ML and no AI call: pure string scoring, so RSK-022 cannot be reached from this module. `T-AI-010`/`T-AI-011` are extractor-boundary statics already covered by `tests/infra/extractionBoundaries.spec.ts` and `apps/api/test/unit/extraction/llmVisionExtractor.spec.ts`. ⚠ **Spec discrepancy reported, not silently resolved:** `specs/ai.md` §4 heads the matcher `apps/api/src/matching/tmdbMatcher.ts` while this row says `packages/domain/`; the backlog is the work order so the pure scoring half lives in the domain, and the deviation is documented in the file header — same resolution as TASK-056c/057. ⚠ **NOT done:** nothing calls the matcher yet; the TMDB query leg and persistence are TASK-061/062. **Was ahead of TASK-045 (now `done`)** — was deliberate: the matcher is pure string scoring over a `TmdbSearchResult` shape and imports nothing from the extractor; `apps/api/src/clients/tmdbClient.ts` already returns exactly that shape, so the module is complete and testable before TASK-045 closes. | `T-AI-010`, `T-AI-011`, `T-TMDB-010` |
| `TASK-061` | `packages/domain/src/tmdbStorage.ts` + `packages/domain/test/tmdbStorage.spec.ts` — `T-TMDB-013` (a–h), 8 tests. `TMDB_STORED_FIELDS` is **derived** from `tmdbMetadataSchema.shape`, never re-typed, so the allow-list cannot drift from the schema. `parseStoredTmdbMetadata` **rejects** an unlisted field with `TmdbFieldNotAllowedError` naming every offending key, rather than letting Zod silently strip it — stripping produces exactly the outcome US-007 AC-6 forbids (TMDB changes shape, we store nothing extra, nobody finds out). The unrecognised keys are read off Zod's `unrecognized_keys` issues, not by diffing the input, so removing `.strict()` from `tmdbMetadataSchema` breaks the test instead of quietly disarming it. Also a Rule A defence (RSK-022): `overview`/`tagline`/`keywords` are the prose that must never reach an inference service, and a rejecting parser makes storing them a reviewable schema edit. Wired at the one write boundary that exists — `refreshStaleMetadata` parses through it before `updateTitleMetadata`. One mutation applied (strip instead of throw) and caught by `T-TMDB-013c/e`. | `T-TMDB-004`, `T-TMDB-013` |
| `TASK-062` | Rule A is now structurally enforced in both halves. STRUCTURAL: a `no-restricted-imports` block in `eslint.config.cjs` scoped to `packages/domain/src/matching/**` and `apps/api/src/clients/tmdbClient.ts`, banning fifteen inference-SDK patterns (`openai`, `@azure/openai`, `@azure/ai-*`, `@azure-rest/ai-*`, `@azure/cognitiveservices-*`, `@anthropic-ai/*`, `@google/generative-ai`, `langchain*`, `@langchain/*`, `ollama`, `replicate`, `@huggingface/*`) plus the API app's own extraction layer; proved by `tests/infra/ruleAImports.spec.ts` (`T-AI-012` a–e). NETWORK-SHAPED: `apps/api/test/unit/extraction/noTmdbToAi.spec.ts` (`T-AI-013` a–f) runs both real extractors through an msw transport that captures the FULL request — URL, every header, raw body — and asserts no string from the committed TMDB recordings appears in any of it. Five mutations applied and each caught by its named test (the rule block re-scoped to a non-existent path; the SDK list narrowed to `openai` alone; the extraction group broadened to `**/extraction/*`; a TMDB title seeded into `EXTRACTION_USER_PROMPT`; a TMDB title smuggled into a vision request header). ⚠ **`T-AI-012` LINTS SYNTHETIC SOURCE, IT DOES NOT GREP THE CONFIG.** A test asserting the config *contains* `no-restricted-imports` passes while the rule is scoped to a path that no longer exists, shadowed by a later flat-config object, or — the case `specs/ai.md` §4.4 already records — written into a `.eslintrc.cjs` that ESLint 10 ignores entirely. Only running ESLint over a violating file at a real protected path fails when the protection dies. ⚠ **AND IT ASSERTS THE NEGATIVE.** `T-AI-012c` requires the shipped `tmdbMatcher.ts` to lint clean and `T-AI-012e` requires an ordinary domain import to pass, because a rule that fired on everything would be switched off within a week rather than fixed. ⚠ **THE MATCHER'S `../extraction/jaroWinkler.js` IMPORT IS NOT A VIOLATION, AND THE MUTANT THAT TREATS IT AS ONE IS KILLED.** `packages/domain/src/extraction/` is pure string arithmetic and shares only a NAME with `apps/api/src/extraction/`, where the SDK calls live; the obvious "safer" `**/extraction/**` group breaks the deterministic matcher this rule exists to protect. ⚠ **THE LINT RULE ALONE IS NOT ENOUGH, WHICH IS WHY `T-AI-013` EXISTS.** It stops the matcher calling a model; it cannot stop the violation from the other direction — threading a TMDB result INTO an extraction request so the model can "pick the right TMDB result" — which needs no new import at all, so the lint rule stays silent while a TMDB payload goes over the wire. ⚠ **THE NEEDLE SET IS NARROWED ON PURPOSE AND THAT IS THE HARD PART.** A naive "no fixture string anywhere" set contains `en`, `movie` and `0`, all of which occur in a legitimate extraction request, so the test would fail on day one, be softened, and end up asserting nothing; only TMDB's *content* fields (`title`, `name`, `original_title`, `original_name`, `overview`, `tagline`, and ids ≥ 1000) are needles. Three vacuity guards sit alongside: `T-AI-013a` pins the needle set is non-empty, contains `Dune` and `438631`, and contains none of the structural vocabulary; `T-AI-013b` pins that both hosts were actually reached; and `T-AI-013e` proves the detector can FAIL, via a planted title in a body, a query string and a header alike. ⚠ **BOTH INFERENCE HOSTS**, per ADR-0001 Rev 2 — a rule proved against Azure OpenAI alone covers half the egress. ⚠ This file captures the request BODY, which the shared vision fixture deliberately does not; that is the right default for a contract suite and the wrong one here, because the assertion is about the body. ⚠ `T-SEC-011` was already delivered (`tests/infra/noCredentials.spec.ts`); the TASK-062 row cites it because `docs/backlog.md` L1311 records the test-id attribution inconsistency for RSK-022, which TASK-126 reconciles. ⚠ **`T-CI-007r` FORCES THE RECORDING TRANSPORT INTO `tests/fixtures/msw/ruleA/index.ts`.** `registerMockedHost` may be called from exactly three places, and an ordinary spec is not one of them — registering a host tells the egress guard to WAVE THROUGH traffic to it, so a registration made in a spec and left unbalanced would permit live egress to a real provider for the remainder of the run. The fixture registers both hosts and un-registers them in an overridden `close()`. It is a THIRD fixture rather than a reuse of the `aoai` and `vision` ones because `msw`'s `setupServer` is process-global — two listening servers fight over the same interceptors — and because the vision fixture deliberately does not capture request bodies. ⚠ **`T-AI-012` CARRIES AN EXPLICIT 60 s TIMEOUT AND ONE SHARED `ESLint` INSTANCE.** Constructing `ESLint` re-reads and re-resolves the whole flat config (~5 s on a loaded box), so a fresh instance per case, or the 5 s Vitest default, fails as "Rule A is broken" when it only means the machine was busy. `lintText({ filePath })` needs no file on disk, so no scratch file is ever written into a protected path. | `T-AI-012`, `T-AI-013`, `T-SEC-011` |
| `TASK-063` | `packages/domain/src/overlap.ts` + `packages/domain/test/overlap.spec.ts` (11 tests) — intra-batch overlap collapse per **SD-02**, ONE implementation parameterised by `pass`: pass A keys on `normalisedText` (pre-match), pass B on `resolvedWorkIdentity` (post-match). `T-AI-007` (a–k). Two mutations applied and each caught (survivor-ordering comparator reversed, losers silently dropped). **Losers are RETAINED, never deleted** — marked `reviewDisposition: 'discarded'` + `collapsedIntoCandidateId`, with `cleanupVerdict` untouched, so an owner can still see what was folded in; the survivor absorbs the union of `sourceImageIds`, the concatenated `boundingBoxes` and the max `ocrConfidence`. Empty/null keys never collapse (an `unreadable-tile` has `normalisedText === ''` and must not swallow every other unreadable tile). Already-collapsed candidates are skipped so pass B cannot re-parent a pass-A loser. ⚠ **Spec gap resolved explicitly:** SD-02's ordering tuple `(imageIndex, yTop, xLeft)` is NOT guaranteed unique, and `Array.prototype.sort` on ties is then order-dependent, so a candidate-id tie-break was added — without it the surviving candidate for a genuinely tied pair is undefined behaviour. Pure: returns new objects, mutates nothing. **ahead-of:TASK-057** — deliberate: `overlap.ts` consumes only the `ExtractionCandidate` shape from `packages/domain/src/types.ts`, never TASK-057's `cleanup()`; it passes `cleanupVerdict` through untouched, so it is complete and testable before the cleanup pass lands. | `T-AI-007` |
| `TASK-064` | `packages/domain/src/classify.ts` + `packages/domain/test/classify.spec.ts` (18 tests) — new vs already-present, PER SERVICE, per `specs/ai.md` §6.1 (REQ-010). `T-CLS-010` (a–k), `T-CLS-011` (a–c), `T-CLS-012` (a–d). Four mutations applied and each caught by its named test (listing-state filter removed so `removed` counts as active, index key stripped of the service, the null-identity guard removed so an unmatched candidate is guessed `new`, `index.has()` inverted). Three properties are each separately tested because each fails SILENTLY: **active listings only**, **the batch's service only**, and **a `removed` listing for this service is `new`** (invariant L1/A33 — a reappearance is a brand-new row dated today, so classifying it `already-present` would skip creating the row and the title would never come back). `buildActiveListingIndex` drops removed rows at BUILD time, not query time, so a caller passing its full listing set — the ordinary case, since the review query loads all listings for the title cards — cannot classify against a removed row; it is also a `Set` lookup rather than a scan, because O(candidates × listings) over a few thousand titles is measurable on 0.25 vCPU. An unmatched candidate returns `null`, never a guessed `new` (`T-CLS-013`); `null` is the ABSENCE of a classification, not a third value, which is why the persisted column is nullable. ⚠ **Deliberately knows nothing about suppression:** `specs/ai.md` §5 gates suppressed works out BEFORE this stage (`T-SUP-002`); folding the two together is PRD R-5, the highest-risk silent defect in the product, because a suppression evaluated here would be scoped to the batch's service while suppression is service-independent. ⚠ **NOT done:** `T-SUP-002` and `T-CLS-013` are `I`-typed and assert the REVIEW RESPONSE — they need TASK-065. | `T-CLS-010`, `T-CLS-011`, `T-CLS-012` |
| `TASK-065` | `packages/domain/src/review.ts` + `packages/domain/test/review.spec.ts` (34 tests) and `apps/api/src/routes/batchReview.ts` + `apps/api/test/integration/batchReview.spec.ts` (19 tests) — `GET /api/batches/:batchId/review` per `specs/api.md` §6.17. `T-REV-010` (h–m), `T-REV-006` (f–i), `T-UX-063d`, `T-CLS-013a`, `T-AI-004` (ad–af), `T-AI-021l`, `T-SUP-002a`, `T-SUP-004a`, `T-SUP-006a`. Five mutations applied and each caught by its named test (`omitted: !fullUpdate` flipped, the SD-02 collapse-loser filter defeated, `ocr-unavailable` made to withhold removals, the suppression gate removed from the removals computation, `candidateCount === 0` widened to include not-yet-extracted images). Split deliberately: the pure assembly (`buildReviewResponse`, `sectionForCandidate`, `removalWithheldReason`, `reviewBanner`) lives in `packages/domain` where it is unit-testable without a store, and only the reads live in the route — so the safety property is asserted by 34 fast tests rather than only by integration. ⚠ **The single most important property in the product is `T-REV-006`: in `full-update`, `alreadyOnYourList.omitted` is `false` and every already-present title is listed.** A failed extraction of a known title must never read as a removal, so `omitted: true` is permitted ONLY in `append-only` (REQ-057) — that one boolean is the whole of invariant 2 and it is why the mode is threaded all the way through rather than inferred. `assertEveryCandidateRouted` runs on EVERY response, not just in tests, so a future `CleanupVerdict` that routes to no section fails loudly instead of dropping a candidate silently. Route order is load-bearing and commented as such: batch → 409 gate → candidates → **suppression gate** → active listings → classification → disappeared set; the gate must precede the *disappeared* computation and not merely classification, or a work suppressed while it still holds an active listing is proposed for removal on every full-update batch (`T-SUP-004`). `removalWithheldReason` reports `low-yield` BEFORE `degraded-extraction` because low-yield is the one the owner can act on, and ⚠ `crossCheck === 'ocr-unavailable'` deliberately does NOT withhold — the primary reader ran; only the corroboration is missing, and withholding there would block the owner on an outage of the cross-check alone. `matchCandidates` is parsed defensively because it is a JSON string written by an earlier stage: a shape change degrades to "no alternatives" rather than 500-ing on a batch the owner then cannot get past. `imagesWithNoText` reads the `candidateCount` column and requires `=== 0`, never `!= null` — `null` (not extracted yet) and `0` (extracted, found nothing) are different and both meaningful (US-006 AC-3), and deriving it from "no candidate names this image" would additionally mis-name an image whose only candidate was absorbed by an SD-02 survivor on another image. Registered as its own `apps/api/src/routes/batchReview.ts`, NOT inside the contended `batches.ts`. Two repository reads added: `listActiveListingsForService` (hard-codes `state: 'active'` with no relax parameter) and `listCandidatesForReview` (carries the same comment as `listCandidatesForBatch` forbidding a disposition filter — filtering there would hide exactly the rows invariant 2 exists to show). **Depends only on TASK-064, which is done.** It does not wait on TASK-057/058: the route consumes the persisted `cleanupVerdict`/`matchState` columns, which already exist in the schema, and treats an absent verdict as "no verdict"; it needs neither task's writer to be correct or testable. ⚠ **Open finding, reported not fixed:** nothing in the codebase currently COMPUTES `lowYield` — `startExtraction.ts` writes only `degradedExtraction` and `crossCheck`, so the persisted column is always `false` and the `low-yield` withhold path is presently unreachable in production despite being fully tested here. `ZERO_YIELD_IMAGE_RATIO` is declared in `review.ts` because it existed nowhere. ⚠ **Scope note:** the backlog row for this task names only `additions`/`unmatched`/`probablyNotTitles`, but `specs/api.md` §6.17 defines all six sections; the full shape was built, because shipping a partial one would force a breaking response change on TASK-069's `ReviewPage`. | `T-AI-004`, `T-REV-010`, `T-UX-063` |
| `TASK-066` | `packages/domain/src/candidatePatch.ts` + `packages/domain/test/candidatePatch.spec.ts` (24 cases) and `apps/api/src/routes/batchCandidates.ts` + `apps/api/test/integration/batchCandidates.spec.ts` (27 cases) — `PATCH /api/batches/:batchId/candidates/:candidateId` and `POST …/candidates/confirm-all` per `specs/api.md` §6.18–§6.22. `T-REV-011` (a–t on the grammar, u–an on the routes), `T-REV-010n`, `T-REV-014` (a–c), `T-SUP-002` (b–d). Five mutations applied and each caught by its named test (the both-forms mixture refusal dropped, `reclassifyAsTitle: false` accepted, the suppression gate on a correction defeated, the `pending` predicate removed from the bulk update's WHERE, a correction left to keep its old `cleanupVerdict`). Split the same way as TASK-065: the body grammar is pure and unit-tested, the gates and writes live in the route. ⚠ **The grammar REFUSES rather than resolves every ambiguity**, because each silent resolution would apply a change the owner did not ask for to a row about to join their list: a body carrying both `disposition` and `reclassifyAsTitle` is rejected before either form is validated; `{ disposition: 'confirmed', tmdbId }` is rejected rather than confirming the ORIGINAL match while dropping the id (that adds the wrong work under a 200); `{ reclassifyAsTitle: false }` is rejected rather than treated as a no-op, since there is no un-rescue affordance and 200 would report success for nothing; and `confirmDuplicate` must be a real boolean, because `'false'` is truthy and coercing it would confirm a duplicate the owner never saw a warning for. Correction gate order is load-bearing and commented: **TMDB fetch → suppression → duplicate**. Reversed, an owner correcting onto a suppressed work is told it is a duplicate — true, but not the reason they cannot proceed. A correction also resets `cleanupVerdict` to `'title-candidate'` and `classification` to `null`; without that a rescued `chrome-suspected` item stays collapsed behind the chrome expander after the owner has fixed it, because `sectionForCandidate` decides on the verdict first. `reclassifyAsTitle` writes the verdict flip **before** searching TMDB and swallows `TmdbUnavailableError`, so an outage cannot lose the rescue. `confirm-all` permits only `additions`/`unmatched`/`alreadyOnYourList` — the collapsed-by-default sections are excluded because bulk-confirming what the owner never opened is exactly the accept-by-inaction REQ-014 forbids — and it decides section membership by calling the exported `loadReviewCandidates` from TASK-065's route, so `confirm-all` and `GET /review` cannot drift about which section an item is in. `confirmPendingCandidates` carries `reviewDisposition: 'pending'` **in the WHERE**, not merely in the caller's filter, so a discard landing between the handler's read and its write cannot be reversed and the returned `count` is the number of decisions the press really made; ⚠ that predicate is unreachable through the route (the handler has already filtered to confirmable rows), so the last `T-REV-011` case drives the repository function directly — a route-level test passes whether or not the guard is there, and the mutation survived until this test was added. ⚠ **Ancillary fix, deliberate:** `TmdbClient` now refuses an EMPTY api key locally with a non-retryable `TmdbUnavailableError` instead of issuing a request that cannot succeed; without it the two rescue tests each spent ~5 s in retry backoff and made real outbound calls to `api.themoviedb.org` from CI. ⚠ **Finding, reported not fixed:** `T-REV-014` (US-012 AC-5, the duplicate-work guard on a correction) was cited by NO task in this backlog; it is implemented and cited here rather than left unowned. **Depends only on TASK-065, which is done.** | `T-REV-010`, `T-REV-011` |
| `TASK-067` | `POST /api/batches/:batchId/manual-entry` in `apps/api/src/routes/batchCandidates.ts` + `apps/api/test/integration/batchManualEntry.spec.ts` (9 cases, `T-AI-023a`–`i`); `parseManualEntry` in `packages/domain/src/candidatePatch.ts` + 6 cases in `packages/domain/test/candidatePatch.spec.ts` (`T-REV-018a`–`f`); `apps/web/src/components/ManualEntryPanel.tsx` (**new**) mounted on `ReviewPage` and wired in `ReviewRoute` + `apps/web/test/manualEntryPanel.spec.tsx` (12 cases, `T-UI-028a`–`l`), two client methods (`searchTmdb`, `addManualEntry`), 12 `MANUAL_ENTRY_*` copy constants and four `.manual-entry*` classes. Four mutants applied and each killed **by its named test** (the `WORK_SUPPRESSED` message collapsed into the generic failure → `T-UI-028d`; the stale-results clear removed → `T-UI-028h`; the client-supplied-`disposition` refusal dropped → `T-REV-018d`; the panel gate weakened to one prop → `T-UI-028l`). ⚠ **TWO SPECS NAMED TWO DIFFERENT PATHS AND NEITHER ROUTE EXISTED.** This row and `T-AI-023` said `…/candidates`; `specs/api.md` §6.20 — authoritative for the API surface — says `…/manual-entry`. Nothing failed, because an **absent** route is asserted by nobody except `T-API-010`, and no client method called it. Both corrected in place. §6.20 also mis-cited **US-009** (candidate classification); the anchor is **US-006 AC-5**, and its `cand:…:manual:1` candidate id was illustrative — ids are ULIDs everywhere else. ⚠ **THIS ROW CITED `T-UI-012`, WHICH WAS ALREADY GREEN AND OWNED BY ANOTHER TASK** (service deep links, `titleRow.spec.tsx`): building under it would have marked TASK-067 `done` against tests that never touched the manual-entry UI — the TASK-082 defect. Replaced with `T-UI-028`. ⚠ **THE SUPPRESSION GATE IS ON WORK IDENTITY** (REQ-071, product invariant 1). Manual entry is the most direct back door there is — the owner types the name of the thing they told the product to stop showing them. ⚠ **A `discarded` candidate for the same work deliberately does NOT trigger `ALREADY_IN_BATCH`**: discarding a mis-read row and adding the right work by hand is the ordinary path through an artwork-only tile, and blocking it would refuse the only affordance that fixes one, with a message saying the title is already there while the review pass shows it struck out. ⚠ **TMDB IS CONSULTED AND ITS TWO FAILURES ARE NOT COLLAPSED**: an unknown id is a 404 and writes nothing (an identity TMDB does not hold has no name, poster or metadata at close and would sit on the list permanently blank), an outage is a 502 — unlike a *correction*, where refusing strands an owner who has already lost something, nothing is lost by adding the title again in a minute. ⚠ **DATA-MODEL FINDING, reported not fixed:** the store has **no representation for a manually-entered candidate**. `provider` is `CHECK`-constrained to `llm`/`ocr-only` and `box_source` to `ocr`/`llm`, and neither is true of a row no reader produced; extending either set needs a `DROP CONSTRAINT` that `T-MIG-001` forbids. `basis: unknown` + `ocrSupport: not-checked` are honest, `boundingBoxes`/`ocrConfidence` are left `null` so no evidence is invented, and `provider: llm` is chosen only because it is the value carrying **no observable claim** (`CandidateCard` renders an "OCR only" chip for the other). ⚠ **Ancillary fix:** `--border` and `--muted-fg` did not exist — the real tokens are `--color-border` and `--color-text-muted`; an undefined custom property resolves to nothing and `T-CSS-001a` only checks that a rendered class has a rule. ⚠ **`T-AI-023` is `I`-typed and cannot run locally** (no SQL Server); it is verified by CI job `5 · test:int`. **Depends only on TASK-065/066, both done.** | `T-AI-023`, `T-REV-018`, `T-UI-028` |
| `TASK-068` | `apps/web/src/components/UnmatchedActions.tsx` (**new**) + `unidentified`/`actions` props on `apps/web/src/components/CandidateCard.tsx` + `renderCard` on `CandidateSection` and three producers in `apps/web/src/pages/ReviewPage.tsx`/`apps/web/src/containers/ReviewRoute.tsx` + `patchCandidate` in `apps/web/src/lib/apiClient.ts` + 17 `UNMATCHED_*` constants and seven `.unmatched-actions*` rules; `apps/web/test/unmatchedActions.spec.tsx` (**new**, 11 cases, `T-UX-063e`–`h` and `T-UNM-010c`–`i`) and `apps/api/test/integration/batchUnmatchedClose.spec.ts` (**new**, `T-UNM-012a`–`f`). Four mutants applied and each killed **by its named test** (a refused patch made to optimistically mark the card decided → `T-UNM-010g`; the "Unidentified" chip derived from `match === null` instead of passed in → `T-UX-063h`; the all-four-or-none prop gate weakened to always-true → `T-UNM-010i`; the top search hit auto-matched on search instead of on an explicit press → `T-UNM-010f`). ⚠ **`unresolvedKept` WAS ALREADY IMPLEMENTED AND ALREADY GREEN — UNDER ANOTHER TASK'S TEST.** `apps/api/src/services/batchClose.ts` and `packages/domain/src/close.ts` have carried it since TASK-071, asserted by `T-REV-012q`, so this row's cited `T-UNM-012` had **never been written**: marking TASK-068 `done` on the strength of a passing suite would have discharged US-008 AC-4 against a test belonging to a different task — the TASK-082 defect. `T-UNM-012a`–`f` drive close **through the unmatched path specifically**, including the branch `T-REV-012q` does not reach: a candidate whose identity is `null`, kept via `workIdentityForUnmatched(candidate.rawText)`, written with `matchState: 'unmatched'` and no TMDB fields. ⚠ **`T-UX-063` WAS TYPED `I/C` AND HAD NO `C` HALF.** The integration and domain halves existed; the component half — the actual contract in `specs/ux-states.md` §6.8 — did not, so *"raw text, an Unidentified chip, an inline TMDB search and Keep as unidentified"* was asserted by nothing and only the raw text was built. ⚠ **`T-UNM-010` (US-008 AC-2, "all three actions available") WAS CITED BY NO TASK IN THIS BACKLOG**; it is implemented and claimed here rather than left unowned, and the epic row now cites it. ⚠ **THE CHIP IS PASSED IN, NEVER DERIVED.** `match === null` is equally true of `probablyNotTitles` and `unreadableTiles`, so deriving it inside `CandidateCard` would tell the owner TMDB had been asked about rows it was never asked about — and would attach the *"we looked and found nothing"* chip to a row nextup had already decided was chrome. Section membership is `sectionForCandidate`, server-side; only the caller knows which section a card is in. `T-UX-063h` needs a **second, non-unmatched null-match candidate** in the fixture or it asserts nothing — it was vacuous until one was added. ⚠ **A REFUSED PATCH MUST NOT OPTIMISTICALLY MARK THE CARD.** A card reading "kept" over a row the server still holds `pending` walks the owner into a close that 409s `PENDING_ADDITIONS` naming a candidate they are certain they dealt with; `run()` deliberately does not `setLocal` on rejection. ⚠ **FINDING, reported not fixed: `FixMatchDialog.tsx` is fully built, fully tested (TASK-111) and imported by NO screen** — the same mounted-nowhere shape as the TASK-076/TASK-107 defects. It is **not** reusable here regardless: it is the *list*-side fix-match (`POST /api/titles/:titleId/fix-match`), whereas a review candidate is corrected through §6.18 `PATCH …/candidates/:id`. ⚠ **FIXTURE DEFECT, found and fixed here, still present elsewhere:** `buildReviewResponse` takes `ReviewCandidate[]`, but fixtures in this suite (and still in `mutatingFlows.spec.tsx`) pass `rawExtractedText`/`confidence`/`cleanupVerdict` — **none of which exist**. The extra keys are dropped and the real fields (`rawText`/`ocrConfidence`/`verdict`) default to empty, so such a fixture renders and passes for as long as nothing asserts on the affected field; `T-UX-063e` caught it only because it asserts on rendered raw text. **Web test files are not typechecked**, so the compiler never sees it. ⚠ **`CANDIDATE_UNIDENTIFIED_CHIP` IS AN ALIAS OF `UNMATCHED_CHIP`**, not a second string, so the chip and the panel explaining it cannot drift apart. ⚠ **`T-UNM-012` is `I`-typed and cannot run locally** (no SQL Server); CI job `5 · test:int` is its only verifier. **Depends on TASK-065/071, both done.** | `T-UNM-010`, `T-UNM-012`, `T-UX-063` |
| `TASK-069` | `apps/web/src/pages/ReviewPage.tsx` (replacing the four-line stub) + `apps/web/src/components/CandidateCard.tsx` (**new**) + the review block in `apps/web/src/index.css` + eleven `CANDIDATE_*`/`REVIEW_*` constants in `apps/web/src/copy.ts`; `apps/web/test/reviewPage.spec.tsx`, 18 cases, `T-REV-013a`–`i`, `T-UX-061a`–`d`, `T-UX-011a`–`e`. 15/15 mutants killed **by their named test** (the harness matches the failing id against the mutant's expected id, so a kill by a neighbouring assertion does not count): omitted sections rendered anyway; zero additions rendered as a blank section; the empty state rendered always; an applicable-but-empty section collapsed to nothing; the count dropped from the `<summary>`; the apply button moved out of the sticky bar; the running counts hard-coded; `tap-target` dropped from the bar controls; the raw extracted text hidden; the media type hard-coded to `Film`; a missing poster rendered as a broken `<img>`; the resolved name blanked; the poster `src` dropped; the release year dropped; and `position: sticky` weakened to `static`. ⚠ **THE SAFETY GATE WAS A FOUR-LINE STUB.** Every server-side guarantee about the review — `T-REV-006` (full update lists what it already knows), `T-REV-017` (both directions of disagreement land in different visible sections), `T-UI-006` (append-only omits) — is a guarantee about a JSON body that **nothing rendered**. A correct response the owner never sees is not a safeguard, which is why `T-REV-013g`/`h`/`i` assert the mode contract **in the DOM** even though the response half is already pinned elsewhere. ⚠ **`omitted` AND `count: 0` RENDER DIFFERENTLY AND THAT IS THE WHOLE POINT** — `omitted` (*"this question does not apply to this mode"*) renders **no section at all**; `count: 0` (*"we looked and there was nothing"*) renders the section, its count, and `REVIEW_SECTION_EMPTY`. Collapsing the two is how a full update comes to look like an append-only one (product invariant 2), and `M1`/`M4` are the mutants that do it. ⚠ **THE COUNT LIVES INSIDE THE `<summary>`** (`T-REV-013h`, SD-11b): the section is collapsed by default, so a count rendered in the body is invisible in the only state the owner normally sees, and it is their sole sanity check against a silently under-read batch. ⚠ **NO `<header>` ELEMENT AND AN `<h1>` IN EVERY STATE.** `T-UI-023c` requires exactly one header landmark per route (the shell owns it) and `T-UI-023b` identifies each route by a unique level-1 heading — so the loading and failure states render the heading too, or the route reads as having fallen through to the catch-all. ⚠ **`T-UX-011b` IS ASSERTED AGAINST THE STYLESHEET AS A FILE**, not against the DOM: jsdom computes no layout, so a rendered-and-present check passes on a bar that scrolls away — exactly the US-037 failure — and `M15` survives every other assertion in the suite. ⚠ **THE FIXTURES GO THROUGH `buildReviewResponse`, NOT A HAND-WRITTEN LITERAL.** A fixture that invents the response shape and reaches it with a cast is how TASK-107's suppressed suite stayed green against a component that threw on every row; here the compiler checks the fixture against the real contract and a section renamed in the domain breaks this file instead of silently un-covering a section. ⚠ **THE PAGE IS PROP-DRIVEN AND MUTATES NOTHING** — dispositions, the removal confirm dialog and the apply call are TASK-082/091/093, and the seam is `onApply`/`onDiscard`. It is still mounted **bare** in `routes.tsx` and therefore renders its loading state: that is deliberate and in scope for the container task, but it is the same shape as the defect TASK-107 fixed, so **`T-DATA-002` will need a third screen** before this route carries data. ⚠ **FINDING, reported not fixed: `specs/ui.md` §9 has no exact copy for any of this.** The six chip labels are quoted from §5.3/§5.3a prose; `CANDIDATE_UNREADABLE_NO_TITLE`, `REVIEW_SECTION_EMPTY`, `REVIEW_NO_ADDITIONS_TITLE`/`_BODY`, `REVIEW_LOADING`, `REVIEW_LOAD_FAILED` and the two button labels had no specified wording and were authored here. ⚠ **AND `specs/ui.md` §13.1's 30-name class vocabulary is an Epic-O snapshot** that does not list the 21 review classes this task adds; `T-CSS-001` (both directions) is the real gate and passes, but the spec table is now stale. ⚠ **`T-REM-011` (TASK-093) IS NOW UNBLOCKED** — the DOM it names exists, and `T-REV-013i` is a partial, deliberately-duplicated guard on the same property. | `T-REV-013`, `T-UX-011`, `T-UX-061` |
| `TASK-070` | `apps/web/src/components/CandidateList.tsx` (**new**) + `apps/web/src/lib/reviewDispositions.ts` (**new**) + `apps/web/src/pages/ReviewPage.tsx` + `apps/web/src/components/CandidateCard.tsx` + `REVIEW_CONFIRM_ALL` in `apps/web/src/copy.ts` + three review classes in `apps/web/src/index.css`; `apps/web/test/reviewErgonomics.spec.tsx` (**new**), 24 cases, `T-UI-025a`–`j`, `T-UI-026a`–`e`, `T-UI-027a`–`i`. **22/22 mutants killed by their named test.** ⚠ **THE ROW NAMED ONLY `E`-LEVEL IDS AND BOTH BELONG TO LATER TASKS.** `T-E2E-001` step 3 is TASK-080 and `T-PERF-002` is TASK-129, so as written this task could be built, merged and marked `done` with **nothing asserting it at the layer it is written in** — and the two ids that would eventually cover it would arrive owned by someone else, exactly the "one task's green test discharges another's AC" defect `specs/testing.md` §12 was written to remove. `T-UI-025`/`026`/`027` are defined in **§12.2**, the section that exists for genuinely-missing coverage, and cited from this row. ⚠ **`T-PERF-002` CANNOT SUBSTITUTE FOR `T-UI-026`:** it measures a real browser at 500 candidates and cannot distinguish *fast because windowed* from *fast because the machine is*. The threshold is asserted in **both** directions, because each fails silently in the opposite environment — `M01` (virtualise unconditionally) renders a **subset of a nine-item section** anywhere layout is not computed, which is every component test in this suite, and `M02`/`M03` (never virtualise, or off by one) are invisible until the 500-candidate batch. ⚠ **`M05` IS THE ONE THAT MATTERS:** a window that reserves only its own height is a list that **ends after one screen** — the owner scrolls to what looks like the bottom and never sees the other 400 candidates, which is a silent truncation of the review pass itself. `T-UI-026e` pins the reserved height against the whole section. ⚠ **THE BUTTON COUNTS DECISIONS, NOT ROWS** (`T-UI-025b`): "Confirm all 3" over one undecided row is a false promise about what one tap does, and `section.count` is the closer-to-hand wrong answer. At zero pending the control **disappears** rather than reading "Confirm all 0". ⚠ **`alreadyOnYourList` IS DELIBERATELY EXCLUDED even though `POST …/confirm-all` accepts it** — `T-REV-016` requires that section to carry no control at all, and a bulk confirm there is either a no-op or the duplicate-identity add `T-REV-014` refuses server-side; `M09` puts the control there and `T-UI-025d` catches it. The collapsed-by-default sections are excluded for TASK-066's reason: bulk-confirming what the owner never opened is the accept-by-inaction REQ-014 forbids. ⚠ **A BULK CONFIRM SKIPS ALREADY-DECIDED ROWS** (`T-UI-025h`): overwriting a `discarded` row would be a silent undo of a decision the owner had already made, which is the one thing a one-tap control must never do. ⚠ **SD-11e IS `sessionStorage`, NOT `localStorage`** (`T-UI-027i`, `M22`): a decision in `localStorage` outlives the batch, the tab and the sign-in and would be replayed over a later batch that reused a candidate id. **The server wins** — `effectiveDisposition` lets the cache speak only over a candidate the server still reports `pending`, or the cache becomes a second, divergent record of decisions the close will not act on. ⚠ **`T-UI-027f` WAS A VACUOUS TEST UNTIL `M19` PROVED IT.** A throw raised inside a React event handler is caught and re-dispatched by React, so `expect(() => fireEvent.click(…)).not.toThrow()` passed against an implementation that threw; the assertion now drives `writeLocalDispositions`/`readLocalDispositions`/`clearLocalDispositions` **directly** against a hostile `Storage` (Safari private mode throws from `setItem`). ⚠ **`CandidateCard`'s root changed from `<li>` to `<div>`** because `CandidateList` must own, position and measure the row element; the `<ul>`/`<li>` semantics are unchanged in both branches (`T-UI-026c`) — a windowed list built from `<div>`s reads as unstructured text at exactly the size where structure matters most. ⚠ **NEW RUNTIME DEPENDENCY: `@tanstack/react-virtual`** (MIT, headless, no transitive runtime deps). `specs/ui.md` §5.4 SD-11c has named this package since Epic O but `specs/security.md` §8 omitted it, so `T-DEP-001` would have failed the build; §8's row and `RUNTIME_DEPENDENCY_ALLOWLIST` are corrected in place, `THIRD-PARTY-NOTICES.md` regenerated. ⚠ **Finding, reported not fixed:** `ReviewPage` is still mounted **bare** in `routes.tsx`, so `onConfirmAll` has no producer — `POST /api/batches/:id/candidates/confirm-all` exists (TASK-066) and is unreached from the SPA until the review container lands. **ahead-of:** nothing — TASK-069 is done. | `T-UI-025`, `T-UI-026`, `T-UI-027` |
| `TASK-071` | `POST /batches/:batchId/close`. Pure close grammar in `packages/domain/src/close.ts`; the transactional apply in `apps/api/src/services/batchClose.ts`. Refuses a pending addition with 409 `PENDING_ADDITIONS` before opening the transaction (no accept by inaction, REQ-014). Applies `confirmed` and `corrected` under the corrected identity, keeps unmatched confirmations as unresolved titles (US-008), re-checks the suppression gate INSIDE the transaction on work identity (REQ-071), keeps the earliest `sortDateAdded`, and writes only this batch's service. Removals stay untouched (REQ-020) until TASK-083–086. Proven by `T-REV-012` across three layers, including a mid-transaction fault injection that proves rollback, and mutation-tested: 10/10 mutants killed. | `T-REV-011`, `T-REV-012` |
| `TASK-072` | `apps/api/test/integration/batchAtomicity.spec.ts` (17 tests) — atomicity proven where it is promised: **in the list the owner sees**, by capturing `GET /api/titles` as raw text and comparing the strings, because a row count can stay flat while the rendered list reorders or regains a badge. `T-BATCH-003` (a–c): an extracted, reviewed, unclosed batch leaves the list byte-identical, the review READ writes no title rows, and — the accept half — closing it does change the list. `T-BATCH-005` (a–c): a close killed at its last in-transaction write leaves the list byte-identical, retries to **exactly one** copy with exactly two provenance rows, and leaves the batch `in-review` rather than stranded. `T-FRESH-013` (a–d): discarded, `extraction-failed` and rolled-back closes all leave `serviceState` empty; only a completed close writes it, and it writes **one service** — the other stays never-updated. `T-BATCH-011` (a–b) and `T-BATCH-012a`: every listing a close writes carries the **declared** batch service, an existing Max row is byte-identical afterwards, and mixed-service candidates do not change that. `T-BATCH-014` (a–b): an addition and a correction apply together or not at all (separate loop iterations, so a per-candidate transaction would leave the first applied). Mutation-tested, 5/5 killed: a non-transactional `runInTransaction`, a listing written to the wrong service, the service date moved outside the transaction, a discard that stamps the date, and a read that reports one service's date for all. ⚠ **The two gaps this row stayed `doing` for are now CLOSED (TASK-086).** `T-BATCH-014` gained `c`/`d`: a confirmed removal and an addition apply together, and neither lands when the close fails after both are written. `T-BATCH-011b` was **vacuous** and is rewritten — it seeded only a Max listing, so the full-update close removed nothing at all and the assertion held for the wrong reason; Heat is now listed on **both** services and absent from the screenshots, so Netflix's copy really is proposed and removed in the same close that must leave Max's alone. Mutation-verified: routing the removal write outside the close transaction is caught by `T-BATCH-014c`. ⚠ **`T-BATCH-014d` is the load-bearing new case.** A removal that survives a failed close is the one direction the owner cannot recover from by retrying — the retry recomputes the proposal from the same screenshots, finds the listing already gone, proposes nothing, and nobody is ever told. | `T-BATCH-003`, `T-BATCH-005`, `T-BATCH-011`, `T-BATCH-012`, `T-BATCH-014`, `T-FRESH-013` |
| `TASK-073` | `packages/domain/src/reconcile.ts` (`reconcile`, `ReconcileCandidate`, `ReconcileResult`), exported from `packages/domain/src/index.ts`; `apps/api/src/routes/batchReview.ts`'s `proposedRemovalsFrom` now delegates to it instead of building the extracted set inline. `T-BATCH-004` — 11 domain cases (`packages/domain/test/reconcile.spec.ts`) and 4 call-boundary cases (`apps/api/test/unit/reconcileOnce.spec.ts`). 8/8 mutants killed (the route reconciling per image; collapse losers in the union; unresolved candidates keeping listings alive; only the first source image counted; images not deduplicated; suppression not forwarded; service not forwarded; an empty batch short-circuiting). ⚠ **THE UNIT OF RECONCILIATION IS THE BATCH, NEVER THE IMAGE, AND THE PER-IMAGE VERSION IS THE LARGEST SILENT-LOSS FAILURE AVAILABLE IN THIS PRODUCT.** A saved list does not fit on one screenshot, so a six-image full update is one statement made in six parts; reconciling per image evaluates each part as the whole list, and every title photographed on image 4 is "missing" from the other five — proposing the removal of almost the entire service on a capture that in fact confirmed it. It is a natural shape for the code to take, because upload, storage, extraction and the OCR cross-check are all *genuinely* per-image. ⚠ **THE BEHAVIOUR WAS ALREADY CORRECT; WHAT WAS MISSING WAS A BOUNDARY THAT COULD BE TESTED.** `listCandidatesForReview` is batch-scoped, so the union was an emergent property of where a loop happened to be closed rather than a named guarantee — nothing would have failed if a later change had narrowed it. ⚠ **A DOMAIN TEST CANNOT PROVE THIS ON ITS OWN.** A route that looped over six images calling a correct, correctly-batch-scoped `reconcile()` once per image with that image's candidates passes every assertion in `reconcile.spec.ts` and still empties the service. The failure is only observable at the CALL, which is why `reconcileOnce.spec.ts` mocks `@nextup/domain` with a pass-through spy and asserts `toHaveBeenCalledTimes(1)` plus the union of `sourceImageIds` in that one call; mutant M1 (the route filtered to the first image's candidates) is killed only there. ⚠ **`T-BATCH-004m` PINS THE COUNT FOR A ONE-IMAGE BATCH TOO**, because a route that had become per-image answers a one-image batch with one call and would otherwise look identical. ⚠ **A DRAFT OF `T-BATCH-004l` PASSED VACUOUSLY.** It read `body.removals?.items ?? []` and compared it with `[]`; removals live at `body.sections.removals`, so the expression was `undefined` for every possible response and the test asserted the product's worst failure could not happen *by never looking*. The corrected version also asserts `sections.removals.count`. ⚠ **`contributingImageIds` IS EVIDENCE, NOT CONTROL FLOW.** It exists so a caller can assert the union spanned the batch rather than assume it; an image contributing nothing is a fact about extraction (`candidateCount === 0`, US-006 AC-3), never a reason to reconcile differently. ⚠ SD-02 collapse losers stay excluded from the union: excluding them changes nothing (their identity lives on in the survivor), and including them is indistinguishable at this boundary from letting a *rejected* candidate keep a title alive. **FINDING (pre-existing, unrelated, not fixed here):** `tests/extraction/ingestRealDevice.spec.ts` `T-SEC-033f`/`T-SEC-033g` fail **only when run under `--coverage`** (they pass in a plain `--project golden` run, on this branch and before it). CI's job 7 runs golden without coverage so it is green there, but a combined local `--coverage` run over all four Vitest projects reports two red tests that have nothing to do with the change under review. | `T-BATCH-004` |
| `TASK-074` | Provenance (REQ-068, US-031). Close writes a `batch_change` row for every title it creates and every listing it adds, INSIDE the same transaction as the mutation — `T-PROV-001` injects a provenance failure and proves the whole close rolls back, so a change without provenance is never persisted (US-031 AC-6). `packages/domain/src/provenance.ts` folds stored rows back into the `specs/data-model.md` §3.7 three-array shape, folding a title and its listing into ONE `created` entry so a creates-only undo cannot double-count. `createdByBatchId` is set on both records and never rewritten on a title an earlier batch created. Proven by `T-PROV-010`, `T-PROV-011`, `T-PROV-012` and `T-PROV-001`; mutation-tested, 10/10 mutants killed. `provenance.modified` for in-review corrections and the removal write path remain TASK-075 and TASK-083–086. | `T-PROV-001`, `T-PROV-010`, `T-PROV-011` |
| `TASK-075` | `provenance.modified` for in-review corrections (REQ-068, §8.1). A corrected candidate now records an `attr_modified` row for `workIdentity` inside the close transaction, carrying the pipeline's own top match as the BEFORE value — read from `matchCandidates`, because applying a correction overwrites `resolvedWorkIdentity` and that column is the only surviving record of what was proposed. A pipeline that proposed nothing records `before: null` rather than an `unmatched:` hash the row never held, and a keep-anyway unmatched title records nothing modified at all (recording it would make an ordinary batch permanently un-undoable under SD-03's creates-only rule). ⚠ Fixed a defect this uncovered in TASK-071: the title was built from `alternatives[0]`, so a correction stored a row whose `work_identity` named the owner's choice while its `tmdb_id`, name and poster still named the title they had just rejected — `title_match_coherent` checks null-ness, not agreement, so nothing refused it. Metadata is now chosen BY IDENTITY, with the display fields left null for the lazy refresh (REQ-076) when the corrected target is not among the alternatives. `T-PROV-013` proves suppress and un-suppress write no `batch_change` row at all (US-031 AC-5). Proven by `T-PROV-012`, `T-PROV-013` and `T-REV-012`; mutation-tested, 10/10 mutants killed. | `T-PROV-012`, `T-PROV-013` |
| `TASK-076` | `apps/api/src/routes/batchDetail.ts` (**new**) + three queries in `apps/api/src/repository/ownerData.ts` + `apps/web/src/pages/BatchHistoryPage.tsx` (replacing the two-line stub) + `apps/web/src/containers/BatchHistoryRoute.tsx` (**new**) + the provenance panels in `apps/web/src/pages/BatchStatusPage.tsx` + nine `BATCH*` constants in `apps/web/src/copy.ts`; `apps/api/test/unit/routes/batchDetailRoute.spec.ts` (**new**, 14 cases: `T-BATCH-016`/`016a`, `T-BATCH-017a`-`e`, `T-BATCH-018`/`018a`-`c`, `T-UX-093`, `T-UX-094`), `apps/web/test/batchHistoryPage.spec.tsx` (**new**, 14 cases: `T-UX-090a`-`b`, `T-UX-091a`, `T-UX-092a`-`d`, `T-UX-093a`-`e`, `T-UX-094a`-`b`), `tests/infra/clientRouteParity.spec.ts` (**new**, `T-API-010a`-`d`). All mutants killed by their named test. ⚠ **THIS TASK OPENED ON A LIVE PRODUCTION DEFECT: `GET /api/batches/:batchId` DID NOT EXIST.** `specs/api.md` §6.15 specified it, `BatchStatusPage` rendered it, `T-UX-007`/`T-UX-008` asserted the render, `apiClient.getBatch` called it and `containers/BatchStatusRoute.tsx` **polled it every two seconds** as the whole of US-006 AC-1 — and no router registered it, so every poll answered 404 and a real extraction looked permanently stuck with no error to explain it. ⚠ **NEITHER SUITE COULD SEE IT, AND THAT IS THE GENERALISABLE PART.** The web tests stub the client, so the request never leaves the process; the API tests assert routes that exist, so an **absent** route is asserted by nobody on either side. Two green suites, one dead feature. `tests/infra/clientRouteParity.spec.ts` (`T-API-010`) is the class-level fix: it extracts every `/api/...` path from `apiClient.ts`'s own source and compares it against the live Express router, with non-vacuity guards on both sides so a broken extractor cannot compare two empty sets and pass. Only **client → server** is asserted — `GET /api/images/:imageId` is reached from an `<img>` href and never through the client, so a bidirectional sweep would fail on correct code and be silenced. ⚠ **`specs/api.md` §6.15 WAS WRONG AS WRITTEN AND IS CORRECTED IN PLACE** (F-001 rule; the superseded text is struck through below it). It documented a flat `extractionStats` object that no writer produces: `jobs/startExtraction.ts` stores `{ stage1, progress, imageFailures? }` and deliberately refuses to zero the stage 3-5 fields TASK-060 has not built. Publishing invented zeros there would have destroyed the only evidence `docs/architecture.md` names for RSK-021, so the field is **omitted** rather than faked; `degradedExtraction`, `crossCheck`, `provenance`, `changedNothing` and `titles` are added, which closes the gap `apiClient.ts` had recorded as a comment since TASK-059. §6.15a (`GET /api/batches`) was entirely unspecified — the numbering jumped 6.29 to 6.31 — and is now written. ⚠ **THE CARD COUNT AND THE DETAIL LIST CANNOT BE ALLOWED TO DISAGREE.** `toBatchProvenance` folds a `title_created` row **into** its `listing_added` sibling (one creation, `data-model.md` §3.7), but a SQL `groupBy` on kind cannot express that fold — summing the two kinds double-counts every new title, so the card reads *"Created 12"* over a detail page listing six, and both numbers look individually plausible. `Math.max(listingsAdded, titlesCreated)`; `T-BATCH-016` pins `created: 6` against 6+6 rows. ⚠ **`progress` IS READ BACK, NOT RECOMPUTED.** Counting images with `candidateCount !== null` looks equivalent, but an image that failed on memory (REQ-080) never gets a count, so a recomputed `imagesDone` stalls one short of the total forever and the status page never leaves its running state. `readProgress` returns **`null`, never `{0,0}`** — a zeroed pair renders as the claim *"0 of 0 screenshots read"*. ⚠ **`available` IS DERIVED BY THE SAME PREDICATE THE BYTE ROUTE USES** (`isExpired`, imported not restated): the 30-day purge writes nothing back to the row (ADR-0006), so a second copy of the `<=` boundary would eventually offer a thumbnail that 410s when clicked. `blobPath` appears in no response (`T-SEC-003`, `T-BATCH-017b`). ⚠ **`changedNothing` IS SENT, NOT DERIVED IN THE SPA.** §9.5 is a sentence, and a batch that only **modified** something must not be told it changed nothing; deriving the rule in two places is two places it can disagree (`T-BATCH-018a`, `T-UX-094b`). The panels are gated on a **closed** batch (`showsProvenance`) because provenance rows are written at close (§3.10), so an *extracting* batch has three legitimately empty arrays and §9.5's sentence over one would announce an outcome for work that has not happened (`T-UX-093e`). ⚠ **PROVENANCE ENTRIES LINK TO `/#title-<titleId>`, NOT `/titles/:titleId`.** §9.4 requires each entry to link to its title and **v1 mounts no title-detail route**, so the obvious href would render the 404 page while still looking like a working link; `TitleRow` now carries the matching `id` anchor. ⚠ **FINDING, reported not fixed: `getTitle` (`GET /api/titles/:titleId`) is in the client and called by nothing** — the same unwired shape, in the opposite direction, that `T-API-010` cannot catch. ⚠ **FINDING: `ux-states.md` §9.6-§9.10 (undo) are NOT covered here** — the batch cards render no *"Undo this batch"* control, which is TASK-095's row. ⚠ **`T-E2E-001` (TASK-080) IS NOW UNBLOCKED** — the endpoint it walks through exists. | `T-BATCH-016`, `T-UX-093` |
| `TASK-077` | `ahead-of:TASK-058` — TASK-058 owns the runner that *produces* the per-image counts; this task surfaces them, and both halves read `candidateCount` from the store, so nothing here waits on it. **`T-AI-021` was already complete (a–z across `lowYield.spec.ts`, `review.spec.ts`, `runExtraction.spec.ts`, `batchReview.spec.ts`) — `T-AI-020` had ZERO cases**, and the gap it hid is the point of this row: `imagesWithNoText` named the blank screenshot but carried no `href`, so US-006 AC-3's *"named **and** thumbnailed"* was half-implemented and looked finished. A file name alone does not pick one screenshot out of twenty near-identical shots in a camera roll, which is the only action this surfacing exists to enable. `ReviewImageWithNoText` now carries `href`, `GET …/review` builds it as an **API path** (`/api/images/:id`, NFR-020 — never a blob/SAS URL out of private storage), and `ReviewPage` renders thumbnail + the `specs/ai.md` §8.2 sentence *"No text was found in `{file}`."* verbatim. `T-AI-020` (a–j, 10 cases) across `apps/web/test/reviewNoText.spec.tsx` (component) and `apps/api/test/unit/batchReviewRoutes.spec.ts` (route). ⚠ The two branches most likely to be "tidied" away are pinned: append-only surfaces blank images too (§8.2 says *any* mode, and the full-update safety argument being stronger is exactly why append-only reads as droppable — `T-AI-020h`), and the section survives a batch that yielded **nothing at all** (`T-AI-020i`), which is the case an early "nothing to review" return would swallow. `candidateCount === null` (unread) stays distinct from `0` (read, found nothing) — `T-REV-010q` kills the `!candidateCount` coercion. Mutation-tested: **11 mutants, 11 killed**; the one survivor was a weak assertion, not dead code — `T-AI-020c` checked only that the element existed, so an `<img>` stripped of its `src` (a broken image, showing the owner nothing) passed it; it now asserts the tag and a non-empty source. | `T-AI-020`, `T-AI-021` |
| `TASK-080` | `tests/e2e/journey.spec.ts` (**new**) implements `T-E2E-001` steps 1–4 only: signed-in owner chooses Netflix full-update, verifies both mode consequence sentences, attaches three golden screenshot files, submits and polls to review, proves the combined list remains empty before close, verifies additions are expanded, already-on-list is collapsed with count, removals are absent, bulk-confirms additions, closes without removals, and verifies exactly the confirmed work rows with Netflix badges, date-added labels, and `Netflix updated today` freshness. Teeth check: temporarily returned `max` badges from the intercepted title-list payload and the spec failed on the missing `badge-netflix` assertion; reverted by editing the file back. | `T-E2E-001` |
| `TASK-081` | `apps/api/test/integration/batchReview.spec.ts` — six new cases, `T-UI-006` (a–f), taking the file to 25. Six mutations applied and each caught (`omitted` hard-coded true; `omitted` derived from the section being EMPTY rather than from the mode; an omitted section still shipping its `items`; an omitted section still reporting a `count`; withheld removals dragging the known titles down with them; the review filtering out already-decided candidates). ⚠ **No source change was needed and none was made — the mode contract was already built, correctly, by TASK-065.** What was missing was the named test for US-013 AC-3, and under NFR-003 an unasserted invariant is not done. This is the AC→test mapping doing its job: the behaviour was right, the *proof* was absent, and the two mutants that had no killer before this task (`omitted` from emptiness, and withheld-removals suppressing the known titles) are both plausible refactors a future task could land silently. `T-UI-006` is level `I` in `specs/testing.md` L908 — integration only, no component half — so `apps/web` is untouched. ⚠ **The load-bearing case is `T-UI-006c`: an EMPTY full-update section is `omitted: false, count: 0`, NOT omitted.** `omitted: true` answers "this question does not apply to this batch"; `omitted: false, count: 0` answers "we looked, and nothing here is already on your list". Collapsing the two is exactly how a failed extraction of a known title comes to read as a removal — product invariant 2 — and no test distinguished them before. `T-UI-006b` is its discriminating twin: the SAME fixture data in full-update fills the section, so `omitted` cannot be satisfied by any function of the data alone. `T-UI-006d` iterates every section of the response rather than the two we know about, because a section the client is told not to render but which still carries rows is a leak waiting for the first client that trusts `items` over `omitted`; it embeds the section name in the expected string so a failure names the offender instead of reporting `1 !== 0`. `T-UI-006e` pairs `alreadyOnYourList` against a WITHHELD removals section — the two are both full-update-only and computed from the same read, making them the pair most likely to be conflated, and the owner must still see what was recognised even when nothing can be removed. `T-UI-006f` pins that the review never filters by disposition: the page is re-read after every decision, so a disposition filter would empty it as the owner worked through it, and in full-update a confirmed known title would look extracted-and-then-lost on the next read. `makeCandidate` gained a `disposition` override for that last case (previously the fixture could only produce `pending`, or `discarded` as a side effect of SD-02 collapse). | `T-REV-006`, `T-UI-006` |
| `TASK-082` | `apps/web/test/reviewKnownSection.spec.tsx` (**new**), 7 cases, `T-REV-016a`–`g`. No production change was needed — TASK-069 built the section collapsed, counted and control-free — so this task is the **assertion** that it is, and 8/8 mutants are killed by their named test: the section expanded by default; the collapse flag inverted; a per-card action button added; cards made focusable; the count dropped from the `<summary>`; a collapsed section rendering its count but not its items; the additions section rendering no cards; and the name dropped, leaving a counted-only section. ⚠ **THE EPIC ROW CITED `T-UI-014` AND THAT ID BELONGS TO SOMETHING ELSE.** `T-UI-014` is *"all three ingest affordances present, labelled and keyboard-reachable"* — TASK-162, `apps/web/test/pasteCapture.spec.tsx`, suffixes `a`–`e`, **all already shipped and green**. Building this task under that id would have marked it `done` against tests that were already passing, leaving US-013 AC-2 with **no coverage at all** while the ledger showed two satisfied tasks. `specs/testing.md` names `T-REV-016` for this AC and it had no implementation; the epic row is corrected in place with the old id struck through, per §5 of the contributor instructions. ⚠ **READ-ONLY IS A SAFETY PROPERTY, NOT A STYLING CHOICE.** These titles are already on the owner's list, so every control this section could offer is either a no-op (confirm what is already confirmed) or destructive (discard a title the owner never asked to lose) — and an addition affordance here would let one tap re-add a work that is already present, the duplicate-identity path `T-REV-014` refuses server-side. `T-REV-016a` asserts across **three interactive roles**, not just buttons, because the cheapest way to reintroduce an action is a link or a checkbox; `T-REV-016b` separately asserts nothing is **focusable**, because a control removed from the accessibility tree but left in the tab order still fires on Enter. ⚠ **`T-REV-016c` IS THE CONTRAST CASE AND IT IS LOAD-BEARING.** Without it, a page that rendered no controls **anywhere** — including the ones TASK-091 must add — passes every assertion above and looks correct until the review is unusable. ⚠ **THE ITEMS ARE IN THE DOM WHILE COLLAPSED** (`T-REV-016g`): `<details>` keeps its children, and a section that rendered its contents only on expand would make product invariant 2 depend on the owner opening it, with a failed expand indistinguishable from a batch that read nothing. ⚠ **A COLLAPSED SECTION WITH NO VISIBLE COUNT IS INVARIANT 2 DEFEATED BY PRESENTATION** (`T-REV-016e`) — the count is the owner's only check that the batch was read fully, and it is useless in the state the section is normally in unless it lives in the `<summary>`. | `T-REV-016` |
| `TASK-083` | `packages/domain/src/removals.ts` + `packages/domain/test/removals.spec.ts` (13 tests), wired through `apps/api/src/routes/batchReview.ts` and asserted end-to-end by two new cases in `apps/api/test/integration/batchReview.spec.ts` (now 27). `T-REM-010` (a wired, b–h pure), `T-REM-013` (a wired, b/c pure), `T-SUP-004` (b–d pure; `a` already existed), `T-UNM-011c`. Nine mutations applied and each caught by its named test (service scope dropped; the `active` guard dropped; the `active` guard weakened to `!== removed`; the suppression gate dropped; suppression made to branch on the `tmdb:` prefix; the set difference inverted; the set difference dropped; the unmatched name falling back to an empty string; and the repository quietly ceasing to select `state`). ⚠ **This is an EXTRACTION, not new behaviour.** The computation already ran inline in the route. It was moved because all three exclusions here are safety rules — each one decides whether the app proposes taking something OFF the list — and each was reachable only through a database and an HTTP round trip. A safety rule that is expensive to assert gets asserted thinly: before this task, "service-scoped" and "active only" had one integration case each and no coverage of the boundaries at all. ⚠ **The module is deliberately mode-blind.** It computes the set difference and stops; `buildReviewResponse` still owns whether the owner may SEE it (append-only omits, low-yield/degraded withholds) and still owns REQ-055 `ticked: true`. A computation that also decided visibility would make "we found nothing to remove" and "we are not going to tell you" the same value — product invariant 2 wearing a different hat — and `T-REM-010h` pins that this module never sets `ticked`. ⚠ **The `active` guard is a POSITIVE test, not `!== removed`** (`T-REM-013c`): a listing state added later must be refused by default rather than admitted by omission, and mutant M3 proves the difference is observable. To make that guard real rather than decorative, `listActiveListingsForService` now SELECTS `state` — it hard-codes `state: active` in its WHERE, so the value was previously invisible to the very filter that checks it; mutant M9 blinds the select and is killed. ⚠ **`apps/api/test/unit/batchReviewRoutes.spec.ts` needed its `ListingRow` stub extended with `state`** — the recurring coverage-floor trap where a stub omits a column the route reads, then reports `undefined` and fails as though the implementation were broken. The exclusion ORDER is documented and load-bearing in the same way step 3-before-step-6 is in the route: service scope first (cheapest and widest), then state, then suppression, then the set difference. `T-REM-011` is NOT claimed here — US-014 AC-2 is about absence from the DOM and belongs to TASK-093. | `T-REM-010`, `T-REM-013`, `T-SUP-004` |
| `TASK-084` | `packages/domain/src/extraction/lowYield.ts` (new, exported from the extraction barrel) + `packages/domain/test/lowYield.spec.ts` (8 tests); `apps/api/src/jobs/runExtraction.ts` (`ExtractionStats.candidatesAfterCleanup`, `RunExtractionPorts.recordItems` widened to `Promise<number>`, `ExtractionSucceeded.lowYield`) and `apps/api/src/jobs/startExtraction.ts` (the port returns the post-cleanup count, `recordExtractionOutcome` persists the flag); 5 new cases in `apps/api/test/unit/runExtraction.spec.ts`, 2 in `apps/api/test/unit/startExtraction.spec.ts` and 3 in `apps/api/test/integration/batchCloseRemovals.spec.ts` (now 23). `T-AI-021`, `T-AI-022`. Nine mutations applied and each caught by its named test (the ratio arm dropped; the ratio comparison flipped to `>`; the threshold moved to 1.0; the zero-candidate arm dropped; the zero-image guard dropped; the cleanup count aliased to the raw count; the count accumulated as `+= 1` per image; the persistence made write-only-when-true; and `lowYield` aliased to the cross-check verdict). ⚠ **THE HEADLINE FINDING: NOTHING ANYWHERE EVER SET `lowYield`.** The flag was in the Prisma schema, in `extractionStatsSchema`, accepted by `recordExtractionOutcome`, and read by `buildReviewResponse`, `removalWithheldReason` and `planRemovals` — and no code path wrote it. Thirteen tests asserted the *behaviour given the flag* and all thirteen passed against a condition production could not reach; the safety net they described was never armed. The same dead trail left `ZERO_YIELD_IMAGE_RATIO` declared in `review.ts` and read by nobody; it now lives beside the code that raises the flag. This is the precise shape NFR-003 exists to catch, and it survived because the flag had no owner — a reminder that a well-tested consumer is not evidence of a producer. ⚠ **`0 / 0` IS `NaN`, AND `NaN >= 0.5` IS `false`.** A literal transcription of `specs/ai.md` §8.1 declares a batch that processed **no images at all** to be healthy — and in full-update that batch proposes removing the owner's entire list. `isLowYield` guards `imagesProcessed <= 0` **before** the division rather than leaning on the zero-candidate arm to cover it incidentally, because that coupling is invisible to the next reader; the pure spec pins it in both directions, including the impossible-looking "5 candidates from 0 images". ⚠ **`candidatesAfterCleanup` IS NOT DERIVABLE FROM `candidatesRaw`.** Stage 2 MERGES `ocr-only` fragments of one caption (it never drops a row), so a three-fragment read of one title is one candidate; §8.1 reads the post-cleanup number, and guessing it from raw would misjudge exactly the messiest reads — the ones the gate exists for. Stage 2 runs behind the `recordItems` port, which is why the port signature had to change rather than the orchestrator counting for itself. ⚠ **THE FLAG MUST BE WRITTEN IN BOTH DIRECTIONS.** A re-extraction (US-034) of a previously low-yield batch must CLEAR it when the new read is good; an implementation that only ever sets `true` leaves the owner permanently unable to remove anything from that batch with no way to tell why. ⚠ **`lowYield` AND `degradedExtraction` ARE INDEPENDENT** — a fully corroborated read of blank screenshots is low-yield and not degraded, and the mutant that aliases one to the other is killed in both directions. ⚠ **`T-EXT-010j` WAS WRONG AND IS CORRECTED IN PLACE**, not worked around: it asserted the cleanup count was absent from a stage-1 failure on the grounds that "stages 2-5 never ran", but stage 2 runs inside `recordItems`, so the count is a genuine measurement. Suppressing it to preserve the old wording would leave the low-yield decision with nothing to decide from. ⚠ **THE GOLDEN HALF IS NOT CLAIMED.** `specs/testing.md` L472 makes `blank-no-content-01.png` the driver for these ids and L487 warns they must be repointed at it — but no AI golden fixture set exists yet (`tests/fixtures/golden/` holds only device captures and ingest images, and no recorded LLM/OCR responses), so that half stays blocked on `TASK-010`. ⚠ **SPEC FINDING, pure-chrome reads escape detection.** `cleanup()` retains chrome-suspected rows and `imagesWithZeroCandidates` counts raw items, so a screenshot yielding only nav-bar chrome scores as a healthy image with real candidates. Implemented per §8.1 as written; the gap is reported, not silently patched, because widening it changes which batches may propose removals. `LOW_YIELD_FULL_UPDATE` exists in `apps/web/src/copy.ts` and is still unconsumed — `ReviewPage.tsx` is a stub, so the withheld-section rendering belongs to the web task, as with `T-UI-007`/`T-UI-008`. **** — deliberate. TASK-077 pairs the same detection with the per-image zero-yield *banners* (`T-AI-020`), which are review-response and `ReviewPage.tsx` rendering; the detection itself could not wait for them, because TASK-083's removal set was already live and shipping a removal proposal with the withholding condition unreachable is the mass-removal hazard this task exists to prevent. `isLowYield` is the module TASK-077 will consume, so no work is duplicated. | `T-AI-021`, `T-AI-022`, `T-AI-036` |
| `TASK-085` | `packages/domain/src/removalPatch.ts` + `packages/domain/test/removalPatch.spec.ts` (11 tests, `T-REM-014` b–l); `apps/api/src/routes/batchRemovals.ts` registered in `routes/index.ts`; `prisma/migrations/0005_removal_decision/migration.sql` (additive `CREATE TABLE` + batch FK, nothing dropped); `apps/api/test/integration/batchRemovals.spec.ts` (15 tests, `T-UI-007` a–c and `T-REM-014` a/m–w) and `apps/api/test/unit/batchRemovalsRoute.spec.ts` (8 tests, `T-REM-014` x–ae, carrying the `apps/api/src/**` coverage floor because CI job 4 runs the `unit` project with no database). Fourteen mutations applied and each caught by its named test (unknown-id refusal dropped; tick/untick swapped; counts computed from the request instead of storage; the live-set intersection dropped; the append-only 400 dropped; the in-review 409 dropped; grammar parsed BEFORE the batch read; the `ticked` default flipped in `review.ts`; the unticked set built from the ticked rows; `both-tick-and-untick` resolved instead of refused; the empty instruction accepted; within-array duplicates left un-deduplicated; the decisions read widened across every batch; and the update-then-insert collapsed to insert-only). ⚠ **THE ABSENCE OF A ROW MEANS TICKED (REQ-055), and only DEVIATIONS are stored.** The removal set is recomputed from the screenshots on every read, so a row per proposal would have to be created, reconciled and cleaned up as that set changed — and every gap in that bookkeeping presents to the owner as a removal they never ticked. `T-UI-007a` pins that a freshly proposed removal is `ticked: true` with `removal_decision` still EMPTY, so the default cannot be satisfied by a seeding step that a later refactor might drop. ⚠ **The key is `(owner, BATCH, listing)`, not `(owner, listing)`.** Keyed on the listing alone an untick becomes a standing exemption, and the NEXT full-update — with fresh evidence the title really is gone — would silently decline to propose it: a removal-shaped hole the owner is never told about, the same class of defect as product invariant 2. `T-REM-014o` walks a second batch and requires the rescued listing to arrive ticked again. ⚠ **A stored decision is NOT evidence the listing is still proposed** — `T-REM-014w`, with a stubbed twin in the unit spec. Counts are re-derived from storage and then INTERSECTED with the freshly computed removal set, because a listing the owner unticked can stop being proposed entirely (a later screenshot shows it after all), and counting that untick would report a rescue against a removal that no longer exists. ⚠ **`upsert` is forbidden in the repository and the gate caught it.** `T-SEC-021a` failed on the first full run: Prisma's unique selector cannot bind `ownerId` at the top level of a `where`, so the owner-scope checker cannot see whether the call is scoped, and TASK-017 separately requires the explicit UPDATE-then-INSERT-if-zero-rows form because `upsert()` compiles to SQL Server's `MERGE`. `setRemovalDecisions` is now `updateMany` → `create` inside the caller's transaction. ⚠ **Existence and ownership are checked BEFORE the body, and the first draft had it backwards.** Grammar-first looks safer — a malformed body then answers the same 400 whether or not the batch is real — but `T-SEC-002g` walks every id-bearing route on the shipped router with another owner's ids and requires a flat 404, and a 400 for a foreign id is a *different* answer from the one a missing id gets. That is a disclosure, and the convention is already documented at `batchCandidates.ts` L219. `T-REM-014u` and its unit-spec twin now pin the corrected order in both directions. No FK to `service_listing`: a decision must survive the removal it informs, which rewrites that listing's state in the very transaction this table feeds. `apps/api/test/integration/harness.ts` gained `DELETE FROM removal_decision` in FK order. `T-UI-007` is level `I/C` — the component half belongs to the web task that builds the removals section (`ReviewPage.tsx` is still a stub), so only the integration half is claimed here. | `T-REM-014`, `T-UI-007` |
| `TASK-086` | `apps/api/src/services/batchClose.ts` (`CloseOptions`, `RemovalPlan`, `planRemovals()`), `apps/api/src/routes/batchClose.ts` (reads the body), `apps/api/src/repository/ownerData.ts` (`listListingsForTitle`), `packages/domain/src/derive.ts` (parameters narrowed to `Pick<ServiceListing, …>`); `apps/api/test/integration/batchCloseRemovals.spec.ts` (18 tests) and eight new cases in `apps/api/test/unit/batchCloseRoutes.spec.ts` carrying the `apps/api/src/**` coverage floor. `T-REV-005` a–f, `T-REV-007` a–c. Twelve mutations applied and each caught (gate keyed on ticked instead of proposed; zero-member group dropped; a group created when removals were withheld; the partial-failure guard dropped; derived fields not recomputed; `removedByGroupId` omitted; append-only proposing removals; truthy coercion of `confirmRemovals`; derive fed only active listings; provenance losing the group id; the gate moved inside the transaction; unticked decisions ignored). ⚠ **THE GATE COUNTS PROPOSALS, NOT TICKS.** Unticking every row still requires confirmation — the owner made a decision and it must be recorded — whereas a WITHHELD section is no gate and no group at all, because the owner was never shown anything and a required confirmation for a section they cannot see would make the batch unclosable. That is the same `count: 0` vs `omitted: true` distinction the review draws (product invariant 2). ⚠ **THE GATE RUNS BEFORE THE TRANSACTION IS OPENED**, which is what makes "a refusal writes nothing" true without relying on rollback; the unit twin asserts zero transactions were even started, and the mutant that moves the gate inside is killed by it. ⚠ **Only a literal `true` confirms.** `Boolean(body.confirmRemovals)` would let `"false"`, `1` or `{}` — all of which a hand-rolled client sends by accident — authorise a destructive action; the route compares `=== true`. ⚠ **`confirmRemovals` is not the tick state.** The ticks are read from `removal_decision` inside the close, never taken from the request body: a close that trusted the body could remove rows the owner had explicitly rescued. ⚠ **THE DEFERRED WEB HALF IS NOW BUILT.** `apps/web/src/components/RemovalConfirmDialog.tsx` (**new**), `apps/web/src/pages/ReviewPage.tsx` (`confirming` state, `onApply(confirmRemovals: boolean)`), four `REMOVAL_*` constants in `apps/web/src/copy.ts`, `.removal-confirm*` in `apps/web/src/index.css`; `apps/web/test/removalConfirmDialog.spec.tsx` (14 tests) — `T-UI-008` d–o and `T-REV-007` d/e. ⚠ **WITHOUT IT THE GATE ABOVE MADE FULL-UPDATE CLOSES IMPOSSIBLE, AND NO INTEGRATION TEST COULD SEE THAT**: the server demands `confirmRemovals: true` and nothing on screen could produce it, so every full-update close could only 409. `T-UI-008a`/`b` pass either way, which is exactly why an `I/C` id needs both legs. ⚠ **THE DIALOG NAMES THE TITLES; A COUNT IS NOT A CONFIRMATION.** The one failure this screen exists to catch — a title the owner meant to rescue still ticked — is invisible behind "Remove 3 titles?", and the names and the heading count are derived from the **same** filtered array so they cannot disagree (`T-UI-008f` kills both the dropped filter and the unfiltered count). ⚠ **The client opens on PROPOSALS, matching the server's rule**, or the owner meets a 409 they cannot act on; the tick-keyed mutant is killed by `T-REV-007d`. ⚠ **Confirm stays ENABLED with nothing ticked** (§6.11, US-015 AC-5) — disabling it would force the owner to DISCARD a batch they had just reviewed to escape it (`T-REV-007e`). Mutation: 12 applied, **11 killed and the twelfth deleted** — `needsConfirmation` was `showRemovals && count > 0`, but `buildReviewResponse` already zeroes the count for an omitted or withheld section, so the conjunct was a second copy of the server's rule that no test could distinguish; it was removed rather than annotated, and `T-UI-008j` covers the withheld case through the count alone. The "no per-row remove affordance" assertion for the **list** still belongs to TASK-093/`T-REM-011`; `T-UI-008l` claims it for the dialog and `T-UI-008c` for the review body. ~~Superseded: "`T-UI-008` is level `I/C` and only the integration half is claimed — `components/RemovalConfirmDialog.tsx` remains unbuilt web work."~~ | `T-REV-005`, `T-UI-008` |
| `TASK-087` | Folded into the same change as TASK-086 (the zero-member case and the partial-failure guard are two branches of `planRemovals()` and its apply block, and separating them would have meant landing a half-built gate). `T-REM-015` a/b, `T-REV-007` a–c in `apps/api/test/integration/batchCloseRemovals.spec.ts`, plus unit twins. ⚠ **A ZERO-MEMBER GROUP IS RECORDED, NOT SKIPPED (US-015 AC-5).** "I was shown three removals and rescued all three" is a decision with the same audit weight as removing them; skipping the group makes it indistinguishable from "no removals were ever proposed", and the batch history would then show a full-update that apparently examined nothing. ⚠ **`T-REM-015` CANNOT BE PRODUCED FROM OUTSIDE THE PROCESS** and the first attempt at it passed vacuously. Pre-removing a listing to simulate the race simply takes it out of the *proposed* set — the proposal is recomputed live at close time — so the close succeeded and the test proved nothing. The only honest form is a partial `vi.mock` of `softDeleteServiceListing` returning `{ count: 0 }` on the Nth call, which is what `T-REM-015`'s own wording ("injected mid-apply failure") sanctions. The guard exists because `softDeleteServiceListing` is guarded on `state: 'active'`: a concurrent removal makes it a no-op rather than an error, so without an explicit `count` check the close would report a removal it did not perform. | `T-REM-015`, `T-REV-007` |
| `TASK-088` | Folded into the same change as TASK-086/087 — the transition is the body of the apply block the gate protects. `T-REM-012a`, `T-REM-016` a/b, `T-REM-017a`, `T-REM-018` a/b, `T-REM-019a`. ⚠ **THE TITLE'S DERIVED FIELDS MUST BE RECOMPUTED FROM ALL LISTINGS, NOT THE SURVIVING ONES.** `deriveTitleState` throws `RangeError` on an empty array (invariant I-3), so feeding it only the active listings makes a fully-removed title crash instead of computing `removed`; that is why the repository gained `listListingsForTitle` (every state) rather than relaxing `listActiveListingsForTitle`, whose name would then have been a lie at its other seven call sites. The mutant that swaps in the active-only read is killed. ⚠ **BLAST RADIUS IS ONE SERVICE.** Removing a title from Netflix must leave its Max listing active and the title itself active; `T-REM-016`/`T-REM-018` pin both halves, since a title-level state write is the natural shape for this code and is silently wrong for any multi-service title. ⚠ **Provenance carries the group id**, not just the batch id: without it a removal cannot be traced to the confirmation the owner gave, and the batch-undo work (TASK-112/114) has no unit to undo. `title.sortDateAdded` is a `Date` column while `deriveSortDateAdded` returns `'YYYY-MM-DD' \| null`, so the call site converts explicitly rather than letting Prisma coerce. `apps/api/test/integration/batchClose.spec.ts`'s pre-existing "close removes NOTHING" case was rewritten: it carried a comment scoping it to "until TASK-086", and its intent (REQ-020, removal is never a side effect) is now proven more strongly — an unconfirmed close answers 409 rather than quietly succeeding. | `T-REM-012`, `T-REM-016`, `T-REM-017`, `T-REM-018`, `T-REM-019` |
| `TASK-089` | Both halves of `T-INV-012` now exist. STATIC: `tests/infra/hardDelete.spec.ts` (`T-INV-012` a–k) scans all shipping source for a Prisma delete and refuses any not on the `SANCTIONED` allow-list, which now carries **two** documented exemptions — the pre-submit draft image (`routes/batchImages.ts::uploadedImage`, I-7) and SD-03's creates-only undo (`repository/undoDiscard.ts::title` and `::serviceListing`) — plus call-site guards `T-INV-012i` (draft check) and `T-INV-012k` (creates-only check). BEHAVIOURAL (new): `apps/api/test/integration/noHardDelete.spec.ts`, `T-INV-012` l–q, six cases driven through the real routes against a real engine. ⚠ **THE TWO HALVES DO NOT SUBSTITUTE FOR EACH OTHER, WHICH IS WHY TASK-089 NAMES BOTH.** The static scan is syntactic: a route that soft-deletes by OVERWRITING a row's identifying columns rather than flagging it contains no `delete(` at all and passes it cleanly, while the row is gone in every way the owner can perceive; and an allow-listed delete whose call site was quietly turned into a no-op keeps `T-INV-012h` green forever, since that case only proves the delete is still WRITTEN, not that it still runs. So `T-INV-012l` closes on a full-update with a confirmed removal and asserts the listing row is STILL PRESENT with `state = 'removed'`, `removedAt` set, and its **original** `titleId` and `dateAdded` — a delete-and-replace would leave a flagged row present and still be a hard delete of the original — and it compares whole-table COUNTS either side, not just the one row. `T-INV-012m` suppresses and asserts the title and its listings survive (REQ-071 keys suppression on work identity precisely so the row may go on existing; a suppress that deleted the title would make un-suppress unimplementable). `T-INV-012n` un-suppresses and asserts the suppression row survives with `active = false`, `unsuppressedAt` set and its display snapshot intact — the snapshot is what lets the suppressed view render without a Title row (US-029 AC-1). `T-INV-012o` and `T-INV-012p` are the mirror pair for the draft exemption: the row really does go on a `draft` batch, and the SAME request past draft answers 409 `BATCH_NOT_DRAFT` with the row still there. `T-INV-012q` covers SD-03's guard from the other side — a refused undo of a non-creates-only batch leaves the census byte-identical, because a guard that ran AFTER the discard would leave the owner with rows gone and a 409 saying it did not happen, and there is no soft-deleted copy behind SD-03 to recover from. Five mutations applied and each caught by its named test (the removal `updateMany` swapped for a `deleteMany`; the un-suppress `updateMany` swapped for a `deleteMany`; the sanctioned draft delete no-op'd; the draft guard disabled; the creates-only guard disabled). ⚠ File named `noHardDelete.spec.ts`, not the row's `no-hard-delete.spec.ts` — every other spec in `apps/api/test/integration/` is camelCase and no gate greps the path. | `T-INV-012` |
| `TASK-090` | `apps/api/src/services/removalGroupUndo.ts` (`undoRemovalGroup`, `toHeldBack`, private `rederive`) + `apps/api/src/routes/removalGroups.ts` (§6.26, no request body) + `listListingsInRemovalGroup`, `restoreServiceListing`, `markRemovalGroupUndone` in `ownerData.ts`, registered in `routes/index.ts`. `T-GRP-010`, `T-GRP-011`, `T-GRP-012`, `T-GRP-013`, `T-GRP-014`, `T-SEC-002` — 15 integration cases (`apps/api/test/integration/removalGroupUndo.spec.ts`) and 14 unit cases (`apps/api/test/unit/removalGroupUndo.spec.ts`). 9/9 mutants killed (suppression check skipped; partial failure tolerated; re-derive skipped; already-reversed pre-check dropped; the guarded mark ignored; the group left unreversed when everything was held back; 403 instead of 404; a placeholder name for a held-back item; `restoreServiceListing`'s `state: 'removed'` guard dropped). ⚠ **WORK-ORDER DEFECT, CORRECTED IN PLACE:** this row's epic entry cited `T-REM-020`, `T-REM-021`, which `specs/testing.md` defines as the *removed-view* tests of US-024 (claimed by TASK-095). US-017's own map is `T-GRP-010`…`T-GRP-014`, and **no backlog row cited them at all** — building to the cited ids would have shipped an undo endpoint proven by a list endpoint's tests. The epic row now cites the `T-GRP-*` set with the old ids struck through (§5's in-place rule). ⚠ **SUPPRESSION IS A NEWER DECISION THAN THE REMOVAL BEING UNDONE, AND IT WINS (AC-4).** Restoring a work the owner has since marked "not interested" reverses a decision they never asked to reverse, which is the same class of silent loss product invariant 1 exists to prevent. Held-back items are **named** with their un-suppress link — built from `suppressionIdFor`, the same function `suppressions.ts` uses, so the remedy link is one implementation rather than two strings that agree today — because "some titles were held back" with no names tells the owner nothing they can act on. `T-GRP-012e` asserts the held-back listing is not merely absent from `restoredListingIds` but that `restoreServiceListing` was never called for it. ⚠ **THE GROUP IS MARKED REVERSED EVEN WHEN EVERY MEMBER WAS HELD BACK.** AC-5 is unconditional: the owner asked for the undo and got an answer. A group left offerable would invite the same press to yield the same refusal for ever; the escape hatch after un-suppressing is §6.10's per-listing restore, which is the documented route back for an old removal (US-025), not a second undo of a group already answered. `T-GRP-013b`/`013e` pin it. ⚠ **ALL-OR-NOTHING, AND THE GUARD IS THE `updateMany` COUNT (AC-6).** `restoreServiceListing` is guarded on `state: 'removed'`, so a per-listing restore landing concurrently makes it a silent no-op rather than an error — exactly the TASK-087 trap. `count !== 1` throws `PARTIAL_FAILURE_PREVENTED` **500** with `details.applied === false` and rolls the whole transaction back, because a half-reversed group cannot be reversed again *as a group* and the owner cannot see which half landed. `T-GRP-014c` asserts the loop stops at the failure and never marks the group. ⚠ **`markRemovalGroupUndone` IS ALSO GUARDED (`undoneAt: null`), AND LOSING THAT RACE ABORTS.** The read-then-check answers the ordinary double-press; the guarded write is what makes it true when both requests read `null` before either wrote, and letting both proceed would double-apply the re-derive. ⚠ **`dateAdded` IS NEVER WRITTEN (AC-2).** The restored listing keeps its original date, so the title returns to its original sort position rather than to the top; `restoreServiceListing` takes no date argument at all, which is the structural version of that guarantee (`T-GRP-010c`, `T-GRP-010f`). ⚠ **RE-DERIVING THE TITLE IS NOT OPTIONAL.** Restoring a listing without recomputing `state`/`sortDateAdded` leaves the title `removed` while one of its listings is `active` — back on the service, still missing from the combined list, which is AC-2 failing in the one way invisible from the response body (`T-GRP-010b`, mutant M3). ⚠ **`listListingsInRemovalGroup` is deliberately NOT filtered to `state: 'removed'`**, so an already-active member is *seen* and aborts the undo rather than being silently skipped. ⚠ Test seeders must use a **well-formed** work identity: `WORK_IDENTITY_RE` is `tmdb:(movie\|tv):<digits>` or `unmatched:<16 hex>`, and `suppressionIdFor` **throws** on anything else — a made-up `unmatched:g-held-back` failed `T-GRP-012d` for a reason unrelated to what it tested. | `T-GRP-010`, `T-GRP-011`, `T-GRP-012`, `T-GRP-013`, `T-GRP-014` |
| `TASK-091` | `apps/web/src/components/BatchAppliedNotice.tsx` (**new** — `undoOffer`, `appliedSummary`, the notice), wired into `apps/web/src/pages/ListPage.tsx` behind an optional `applied` prop, plus `undoBatch` / `undoRemovalGroup` on `apiClient`. `apps/web/test/batchAppliedNotice.spec.tsx` (**new**), 17 cases `T-UX-065a`–`q`. 16/16 mutants killed by their named test: the batch undo tested before the removal undo; the offer keyed on the group id instead of the member count; an empty group id invented for a null one; the button always posting the batch undo; the in-flight control left enabled; a failed undo reporting success; the undo and the history link rendered together; the removal clause dropped from the summary; the service hardcoded; the nothing-changed sentence silenced; pluralisation forced plural; the summary demoted out of its live region; the control surviving a completed undo; the undo firing on mount; the notice hidden when the list read failed; and the unwired handler resolving silently. ⚠ **THERE ARE TWO SERVER UNDOS AND THEY ARE NOT INTERCHANGEABLE.** §6.26 puts a removal group's listings back; §6.25 discards what a **creates-only** batch created and 409s `BATCH_NOT_CREATES_ONLY` for anything else. A batch that removed anything has a non-empty `provenance.removed` and is therefore **never** `undoable` (`packages/domain/src/undo.ts`), so a notice that always offered "Undo this batch" would satisfy a naive presence assertion and then fail for every full-update close — precisely the batches US-017 exists for. `undoOffer` makes the choice once, from the summary the server just sent, so the label, the endpoint and the argument are one fact; `T-UX-065a`/`b` assert the endpoint **and the id**, never just the label. ⚠ **`T-UX-065c` IS THE CONTRAST CASE AND IT IS LOAD-BEARING**: without a batch that can offer nothing, an implementation that offers the removal undo unconditionally passes everything else in the suite. ⚠ **A ZERO-MEMBER REMOVAL GROUP IS NOT AN UNDO OPPORTUNITY** (`T-UX-065e`). Unticking every removal still records a group (US-015 AC-5, `T-REV-007`), so `removalGroupId` alone does not mean anything was removed, and offering to reverse it promises to undo a change that never happened. ⚠ **`T-UX-065q` DISTRUSTS AN INCOHERENT SERVER.** `undoable: true` alongside a non-empty removal group is contradictory, but if it is ever sent, the removal undo is the one that reverses what the owner just watched happen — so the removal branch is tested **first**, and this case is what kills the reordering mutant that is otherwise behaviourally equivalent. ⚠ **A FAILED UNDO MUST SAY THE CHANGE IS STILL APPLIED** (`T-UX-065i`). "Something went wrong" leaves the owner unable to tell whether the removal stands, and the remedy — pressing Undo again — depends on knowing that it does; the control therefore stays **enabled** after a failure rather than disappearing. ⚠ **THE NOTICE RENDERS OUTSIDE THE LIST'S LOADING AND FAILURE BRANCHES** (`T-UX-065o`). The write has already happened; withholding the undo because `GET /api/titles` failed removes the remedy at the exact moment the owner cannot see what was done. ⚠ **AN UNWIRED HANDLER REJECTS RATHER THAN RESOLVING** (`T-UX-065p`): a resolving stub would print *"those titles are back on your list"* over a list where nothing was reversed, which is the one lie this notice must never tell. ⚠ **WORK-ORDER DEFECT, CORRECTED IN PLACE:** the epic row also cited `T-UX-013`, which is US-019 AC-5's zero-match state — **TASK-039, `apps/web/test/filterBar.spec.tsx`, suffixes `a`–`e`, shipped and green**. Building under it would have marked this task `done` against tests that already passed. Struck through per §5, as `T-UI-014` was at TASK-082; this is the third stale citation found, and the pattern is always the same — grep the id across `apps/**` before starting, because `check:test-ids` proves only that an id is *defined*, never that it is *unclaimed*. ⚠ **FINDING — the copy is partly invented.** `ux-states.md` §6.13 fixes exactly one sentence, *"Added 9 titles, removed 3 from Netflix."*, and its own example summary makes the added count `listingsCreated` (9), **not** `titlesCreated` (6); the other three arithmetic cases, both undone messages and the failure message have no specified wording. ⚠ **FINDING — the container is not wired.** `applied` is an optional prop with no producer: nothing calls `POST /api/batches/:id/close` yet, because the review screen is still mounted bare in `routes.tsx`. The affordance is asserted at level C, which is what US-017 AC-1 is graded at; the close→navigate→notice path belongs to the review container task alongside the third `T-DATA-002` screen. ⚠ **FINDING — `ReviewPage.tsx` carries a local `SERVICE_LABELS` duplicate** of the domain constant this component imports from `@nextup/domain`; left in place rather than churned outside this task.  ⚠ **THE FINDING ABOVE WAS DISCHARGED — and it had sat there, correctly worded, while the feature stayed dead.** ``ReviewRoute`` and ``ListRoute`` both shipped (TASK-177/178) without closing the loop: the close ran ``.then(() => navigate('/'))``, throwing away the only copy of the §6.22 summary that will ever exist, and ``ListRoute`` passed neither ``applied`` nor either undo handler. Fixed by ``CloseBatchResult`` on ``apiClient.closeBatch`` (was ``request<unknown>``), ``toAppliedBatch`` in ``ReviewRoute``, and ``parseAppliedState`` + the two handlers in ``ListRoute``; ``apps/web/test/appliedUndoWiring.spec.tsx`` (**new**), 7 cases ``T-DATA-011a``–``g``, catalogued at ``specs/testing.md`` §36.2b. 5/5 mutants killed by their named test: the close result discarded again (killed by ``a`` **alone** — the shipped defect, reproduced exactly); ``applied`` not passed (``a``, ``d``, ``e``); the two handlers unwired (``d``/``e`` **only**, with ``a`` still green, which is what proves they are not redundant); the service read from the wrong field (``a``, and only because its assertion names the sentence rather than the box); and the history state cast instead of validated (``f``/``g``). ⚠ **THIS IS THE FIFTH INSTANCE OF "TWO GREEN SUITES, ONE DEAD FEATURE", AND THE FIRST THAT ``T-INFRA-013`` ACTIVELY MISREPORTED.** Its matcher is a bare identifier over ``apps/web/src``, so the **prop declarations** of ``undoBatch``/``undoRemovalGroup`` in ``ListPage`` and ``BatchAppliedNotice`` counted as reaching the client methods — a looseness the gate's own header documented as deliberate. The generalisable lesson, now recorded there in place: **a reachability gate proves a NAME IS MENTIONED, never that a CHAIN IS CONNECTED.** ⚠ **HISTORY STATE IS UNTRUSTED INPUT.** It survives a reload, the back button restores it, and a session restored across a deploy can hand ``/`` a shape from an older build; ``parseAppliedState`` validates every field and returns ``undefined`` rather than a default, because a notice with invented counts — or one offering an undo whose ``removalGroupId`` we do not have — is worse than no notice. ⚠ **A FAILED CLOSE NOW EXPLICITLY DOES NOT NAVIGATE** (``T-DATA-011c``): landing on an unchanged list reads as a close that succeeded and changed nothing. ⚠ **FINDING — ``ListRoute``'s ``removedCount`` comment was FALSE, corrected in place.** It claimed ``GET /api/removed`` "does not exist yet"; it shipped with TASK-099. ~~The count is still not passed, now for a cost reason rather than an availability one, and the empty-list-after-removals wording gap (US-019 AC-5) remains open and grep-able.~~ **↳ CLOSED.** The cost objection was real but self-answering: "it only matters when the unfiltered list came back empty" **is** the fetch predicate, so `ListRoute` now reads `getRemoved` **only** in that state and passes the real count. A non-empty list still issues three requests, not four. `T-DATA-002j` asserts the `all-gone` wording and the rendered number on an empty list with removals; `T-DATA-002k` asserts `getRemoved` is **not** called otherwise. Mutants: restoring `removedCount={0}` killed by `j` alone (it reproduces the shipped defect exactly); making the read unconditional killed by `k` alone. ⚠ **`ListEmptyState` was fully tested and correct the whole time** - `listEmptyKind` already had a case for this input. The defect was purely in the WIRING, which only a test rendering the container can reach; the same shape as the `undoBatch` dead call that `T-INFRA-013` was tightened for. | `T-UX-065` |
| `TASK-092` | Five integration cases in `apps/api/test/integration/batchReview.spec.ts` (now 32), `T-REV-017a`–`e`. No production change was needed — `sectionForCandidate` and `buildReviewResponse` already partitioned correctly — so this task is the **assertion** that they do, and 6/6 mutants are killed by the new cases **alone** (re-run under `-t T-REV-017` to prove the existing `T-REV-006f` was not doing the killing): already-present candidates merged into `additions`; new candidates folded into `alreadyOnYourList`; removals suppressed whenever anything was recognised; a candidate pushed into two buckets at once; an empty removal section reported as `omitted`; and the known section reporting a count while withholding its items. ⚠ **THE THING BEING ASSERTED IS THE ABSENCE OF A MERGE.** A reconciliation that quietly took the union of *what the screenshots said* and *what is on the list* would produce a correct-looking list and destroy the owner's only means of telling a genuine removal from a failed OCR read — product invariant 2, the single most important safety property in the product. Every case therefore requires the two directions of disagreement to land in **different, visible sections with honest counts**, and `T-REV-017a` requires `alreadyOnYourList` and `removals` to be non-empty **in the same response**: a review that showed only the removal would present a failed extraction as a decided fact. ⚠ **`T-REV-017d` ASSERTS THE PARTITION, NOT THE COUNTS.** It collects `candidateId` across all five candidate sections and requires the list and the `Set` of it to be the same size, because a mutant that emits one row into both `additions` and `alreadyOnYourList` leaves every individual count looking plausible while letting one confirmation both add and skip the same title. ⚠ **`T-REV-017e` PINS `count: 0` WITH `omitted: false`** — *"we looked, and there is nothing"* versus *"this question does not apply"*. Collapsing the two is exactly how a full-update comes to look like an append-only one, and `M5` is the mutant that does it. ⚠ **FINDING, reported not acted on: `T-REM-011` (TASK-093) CANNOT BE SATISFIED YET.** Its AC is *"no removals section exists in the response **or the DOM**"*, and `apps/web/src/pages/ReviewPage.tsx` is a declared stub — the same note already sits at `T-UI-008` in `batchCloseRemovals.spec.ts` L498. The response half is covered here and at `T-REV-006g`; the DOM half needs the page built first. | `T-REV-017` |
| `TASK-093` | `apps/web/test/reviewAppendOnly.spec.tsx` (**new**), 11 cases — `T-REM-011a`–`i` plus the two rendered halves of US-015, `T-UI-007d` and `T-UI-008c` — and one deletion in `apps/web/src/pages/ReviewPage.tsx`. 11/11 mutants killed **by their named test**: omitted candidate sections rendered anyway; removals ignoring `omitted`; withheld removals rendered anyway; removals never rendered at all; absence faked with a `hidden` attribute; the section rendered without its test id; the running count hard-coded to zero; the running count reading the additions number; removals rendered unticked; a per-row remove affordance added; and a `display: none` added to `.review-section`. ⚠ **"ABSENT" AND "HIDDEN" ARE INDISTINGUISHABLE IN A SCREENSHOT AND ARE NOT THE SAME PROPERTY.** A `hidden` attribute, a `display: none` rule or an `aria-hidden` wrapper all satisfy a human skim while leaving the section in the accessibility tree, in the tab order and in the serialised HTML — so a screen-reader user is offered a removal decision the mode says cannot exist. `T-REM-011d` therefore queries by **role** (which ignores nothing merely off-screen), `T-REM-011c` queries by **text** (so a section that lost its `data-testid` cannot slip past), `T-REM-011e` asserts no `hidden`/`aria-hidden`/inline `display: none` anywhere in the tree, and `T-REM-011i` reads the **stylesheet as a file** — because a `display: none` on `.review-section` would leave every DOM assertion passing while hiding the section in the mode that needs it most. ⚠ **EVERY ABSENCE CASE HAS A POSITIVE TWIN ON THE SAME FIXTURE.** The append-only and full-update fixtures differ **only in `mode`**, so a page that never rendered removals at all would pass every absence assertion here; `T-REM-011b` requires the same data to produce the section, its label, its row and its count. ⚠ **`withheld` IS NOT `omitted`** (`T-REM-011h`): a low-yield full update proposes nothing *because the read was too thin to trust*, and the owner must be **told** — the section is absent but the banner explains it, and `alreadyOnYourList` is **still present**, which is product invariant 2. ⚠ **DEFECT FOUND BY MUTATION, FIXED HERE:** `ReviewPage` computed `const removalCount = sections.removals.omitted ? 0 : sections.removals.count` — a mutant that deleted the guard **survived**, because `buildReviewResponse` already reports `count: 0` whenever removals are omitted or withheld. It was not a safeguard but a **second implementation of the server's rule that no test could distinguish from the first**, and it would have gone on agreeing after the server's rule changed. Deleted; the bar now renders the server's number, and `T-REM-011g` uses a fixture with a **non-zero additions count** so that a bar printing the wrong section's number cannot read correctly by coincidence. ⚠ **`T-UI-008c` IS THE HALF INTEGRATION CANNOT SEE.** `T-UI-008a`/`b` prove the API needs only one group confirmation; they cannot see a per-row *remove* button, because the API never hears about an affordance until it fires — and a second, unconfirmed path to a destructive outcome is exactly what REQ-020 forbids. The case asserts no button exists inside the removals section **and** that every button on the screen is inside the sticky action bar. ⚠ **`T-UI-007d` IS THE `I/C` HALF THAT WAS MISSING:** `T-UI-007a`–`c` pin `ticked: true` in the response; this pins that the checkbox paints **checked** on first paint. Rendering them unticked would make "rescue everything" — a choice the owner never made — look like a batch that had already been reviewed. ⚠ **THE RESPONSE HALF IS NOT RESTATED HERE.** `T-REV-006g`, `T-UI-006a`/`d` already own it in `apps/api/test/integration/batchReview.spec.ts`; `T-REM-011`'s AC is *"the response **or the DOM**"* and this task is the DOM. | `T-REM-011` |
| `TASK-094` | `tests/e2e/journey.spec.ts` — the canned single-batch API stub was replaced by a **stateful in-memory backend** (a WORKS catalog, three ordered `UploadBatch` plans with distinct per-batch dates, a mutable listing/suppression model, and faithful `reconcile`/`reviewResponse`/`titlesResponse`/`removedResponse`/`serviceStateResponse`/`batchStatusResponse`/`closeBatch` builders behind a single `page.route('**/api/**')` router) so the one `T-E2E-001` test drives the real SPA through steps 1–5 instead of asserting against fixtures. Test title renamed to `T-E2E-001: a first full update, a reconcile with removals, a suppression, and a reappearance` (only `tests/e2e/journey.spec.ts` cites it). Step 5 = a second Netflix **full update** that drops one title (arrival) and adds one (sinners): the review shows the removal ticked, the removal-confirm dialog gates the close, and after apply the removed **log** preserves arrival's *original* date-added. 3/3 load-bearing branches mutation-tested and each killed by its named assertion, restored by edit (never `git checkout`), re-verified with a clean rebuild + green run: **M1** `ReviewPage.showRemovals` → `false` killed the "removals section visible / arrival ticked" assertion; **M2** `RemovedPage` `removed-date-added` rendering `removedAt` instead of `dateAdded` killed "original date preserved in the removal log"; **M3** `BatchAppliedNotice.undoOffer` returning `batch` instead of `removal-group` killed the "Undo the removals" affordance assertion. ⚠ **FINDING, reported not fixed:** `buildReviewResponse` keeps a **count-0 full-update removals section present**, but the e2e stub *omits* it (`omitted = appendOnly | `T-E2E-001` |
| `TASK-095` | `apps/api/src/routes/removed.ts` (§6.9) + `removedQuery.ts` (query parsing and a dedicated cursor codec) + `listRemovedView`, `countRemovalsForWorks` and `findActiveSuppressedWorks` in `ownerData.ts`, registered in `routes/index.ts`. `T-REM-020`, `T-REM-021`, `T-REM-022`, `T-REM-006` — 18 integration cases (`apps/api/test/integration/removed.spec.ts`) and 33 unit cases across `apps/api/test/unit/routes/removed.spec.ts` and `removedRoute.spec.ts`. 15/15 mutants killed. ⚠ **THE MOST TEMPTING "IMPROVEMENT" TO THIS VIEW DESTROYS THE ONLY THING IT CARRIES.** Collapsing three removals of one work into one row is what the combined list does, so a reviewer who did not know `specs/data-model.md` §11 would read it as consistency — and it erases the owner's only record that a title keeps coming back (US-024 AC-6, invariant 7). One item per removed **listing**, never per work; `T-REM-006` seeds three removals and asserts three rows. ⚠ **ORDINAL 1 IS THE OLDEST REMOVAL, AND THE PAGE IS NEWEST-FIRST — THE TWO ORDERINGS ARE DELIBERATELY OPPOSED.** Numbering from the newest renumbers every earlier row the next time the work is removed, and an ordinal that changes retroactively records nothing. ⚠ **RANKING IS NEVER GIVEN THE CALLER'S FILTERS.** `countRemovalsForWorks` deliberately takes no `q`/`service`: filtering to Max would otherwise renumber a work removed twice from Netflix and once from Max as "removal 1 of 1". `T-REM-022b` filters by service and asserts the ordinals are unchanged; `T-REM-022h` asserts at the call boundary that the filter never reaches that read. ⚠ **SEARCH MUST LOOK AT `normalised_text` AS WELL AS `tmdb_name`.** The pre-existing `searchRemovedListings` matched `tmdb_name` only, which passes a search test written against matched rows and silently hides every row the extraction could not identify — the set an owner is most likely to come here looking for (§11 rule 1, `T-REM-021b`). The term is parameterised and `ESCAPE`d; the mutant that drops `escapeLikeTerm` is killed by `T-REM-021d`, where a bare `%` would otherwise match the whole log and read as a search that had simply found everything. ⚠ **THE CURSOR IS NOT `ListCursor` AND MUST NOT BE MADE TO REUSE IT.** `ListCursor` carries a `YYYY-MM-DD` date; the removed view orders by `datetime2`, and one full-update close removes a whole page of listings at the same instant. A date-precision key is ambiguous across every removal that day, and a keyset predicate over an ambiguous key **skips rows** — which the owner reads as the log having lost their history. `T-API-017e` refuses a date-precision cursor outright. ⚠ **ORDINALS ARE NOT COMPUTED WITH A WINDOW FUNCTION.** `ROW_NUMBER() OVER (PARTITION BY work_identity)` must be evaluated over the owner's entire removed history before `TOP (n)` applies, turning the keyset seek `T-PERF-001` asserts into a full scan of a table that grows for ever (REQ-028, NFR-018). Two bounded reads over the page's works instead. ⚠ **`restorable` is `!suppressed` and nothing else.** A duplicate active title does **not** make a row unrestorable — §6.10 answers `DUPLICATE_WORK_IDENTITY` with a *confirmable* 409, so folding it in here would grey out a control that works. Suppression is different in kind: §6.10 refuses it until the work is un-suppressed. ⚠ **No lazy metadata refresh on this route**, unlike §6.4's list and detail: a removed row is a historical record, and spending the page's latency budget refreshing metadata for titles already off the list is a poor trade. | `T-REM-006`, `T-REM-021`, `T-REM-022` |
| `TASK-096` | `apps/web/src/pages/RemovedPage.tsx` (the stub replaced), `apps/web/src/copy.ts` (six `REMOVED_*` constants quoted from `ux-states.md` §7.2/§7.3), `apps/web/src/lib/apiClient.ts` (`RemovedItem`/`RemovedResponse` types only — no new method, so `T-DATA-003a`'s surface enumeration is untouched), `apps/web/src/index.css` (`.removed-*`), `packages/domain/src/copy.ts` (`removedOnLabel`, and `dateAddedLabel` refactored onto a shared `formatIsoDay`); `apps/web/test/removedPage.spec.tsx` (17 tests) — `T-UI-009` a–g, `T-UI-011` a/b, `T-UX-071` a–d, `T-UX-072` a–d. Mutation: 18 applied, **18 killed**. ⚠ **THE LOG IS NEVER DE-DUPLICATED, AND THAT IS THE WHOLE TASK.** Every other list in this product collapses to one row per work, so a `RemovedPage` that did the same would look consistent, satisfy a naive "renders the removed titles" test, and silently destroy the earlier removals of a title that has come and gone more than once (product invariant 7, US-024 AC-6). `T-UI-009a`–`c` therefore fixture ONE work removed THREE times and require three rows, three ordinals **and three distinct date pairs** — the third is what catches a render that keeps the rows but reads every field from one item. ⚠ **`T-UI-009g` EXISTS BECAUSE A MUTANT SURVIVED.** Keying the list on `workIdentity` renders three correct-looking rows today, because the content is prop-driven; all three then share one React identity, and TASK-099's per-row restore state would be carried to the wrong row once the list changes. The duplicate-key error is the only observable symptom before then, so that is what the case asserts — a latent defect is still a defect. ⚠ **A FAILED LOAD IS NOT AN EMPTY LOG (AC-8).** An empty removal history reads as *"nothing has ever been removed"*, so letting a fetch failure render it makes the product assert something false about the owner's data in the one place they come to check nothing was lost; `T-UX-072b` goes further and requires the error to win **even when rows are already held**, because a stale list under a failed refresh is a list the owner cannot tell is stale. ⚠ **The two EMPTY states are distinct** (§7.2 vs §7.3) and `T-UX-071b` is the discriminating case: identical empty `items`, only the applied query differs, so a page keyed on `items.length` alone tells an owner who mistyped that their history is gone. ⚠ **`removedOnLabel` carries no "to nextup" marker and `dateAddedLabel` still does.** The asymmetry is deliberate — the added date is unknowable for the streaming service and would otherwise read as theirs (`T-LIST-018`), whereas the removal is unambiguously an event in nextup. Both now share one formatter, so the two dates shown side by side cannot disagree about padding or month casing. ⚠ **FINDING — a spec collision, reported not silently resolved.** `specs/testing.md` US-024 AC-8 assigns `T-UX-072` to *"load failure renders an error, not an empty view"*, while `ux-states.md` §7.3 assigns the same id to the *no-search-results* empty state — and §7's state table has **no load-failure row at all**. `specs/testing.md` carries the authoritative AC→test mapping (NFR-003), so `T-UX-072` is the load failure here and the no-search-results state sits under `T-UX-071`, whose own wording (*"distinct from a no-search-results state"*) demands that comparison anyway. `ux-states.md` §7 needs a load-failure row added; that is a spec change and is left to the owner. Restore is **not** in scope (TASK-098/099); `restorable` and `suppressed` are carried on the row for it. | `T-UI-009`, `T-UI-011` |
| `TASK-097` | **Already delivered in full by TASK-047** — verified rather than assumed. `apps/api/test/integration/queryPlan.spec.ts` plants `SCALE_ROWS = 20_000` removed listings (§9) and asserts scale-invariance as `T-PERF-001a`–`i`. ⚠ **`a` is a NON-VACUITY guard** — it asserts the fixture really holds 20,000 rows, which is the case that matters: a scale test whose fixture silently failed to plant its rows passes every plan assertion trivially and reports the opposite of the truth. `b` seeks the filtered index rather than scanning, `c` bounds rows-read by page size rather than table size, `d` proves a later keyset page costs the same as the first, and `e` bans `OFFSET`. `f`–`i` cover the deliberately non-index-backed `LIKE` search, including metacharacter escaping and the escape character being escaped first. Nothing to add; the row is closed against the existing suite rather than duplicated. | `T-PERF-001` |
| `TASK-098` | `apps/api/src/routes/listings.ts` (new) — `POST /api/listings/:listingId/restore` (`api.md` §6.10, US-025), registered in `routes/index.ts` next to the removed view it complements, with the supporting query in `repository/ownerData.ts`. Pre-conditions are **ordered**: existence (404 `NOT_FOUND`) → 409 `LISTING_NOT_REMOVED` → 409 `WORK_SUPPRESSED` → 409 `DUPLICATE_WORK_IDENTITY`; `confirmDuplicate: true` reuses fix-match's `duplicateAckSeq` pattern so a restored listing can coexist with an already-active title without tripping the filtered unique index. Tests `T-RES-010/010a/010b`, `T-RES-013`, `T-RES-014` (`test/unit/routes/listingsRoute.spec.ts`, `test/integration/restoreListing.spec.ts`). ⚠ **Recovered from the abandoned `lane-f` branch (PR #90).** That PR went green and then sat open and unmerged: the lane was stood down on the mistaken belief that it had produced nothing, when it had in fact delivered a working endpoint. **Only the API half was ported.** `main` had meanwhile grown its own TASK-096 removal-history page (PR #91, `0da235a`) with copy sourced from named constants, so taking the lane's web half wholesale would have been a divergent second copy of an existing page — and its restore copy contradicted `ux-states.md` §7.7 (it carried **no `role="status"` naming the original date**, which is what makes US-025 AC-2 visible) and §3.6 (it said *"Remove that first, then restore"* where the spec says *"You marked '{name}' as not interested. Stop ignoring it first?"*). That half was rebuilt against the spec as TASK-099 instead. **Restore stays an EXPLICIT user action** (product invariant 7): nothing here is reachable from reconciliation. | `T-REM-010`, `T-RES-013`, `T-RES-014` |
| `TASK-099` | The restore UI on `/removed` — `RestoreControl` in `apps/web/src/pages/RemovedPage.tsx` as an explicit state machine (`idle` → `submitting` → `success` / `confirm-duplicate` / `unsuppress-first` / `already-active`), `restoreListing` + `RestoreResponse` in `apiClient.ts` (and its `T-DATA-003a` invocations entry), ten spec-quoted copy constants, one `.removed-row__restoring` rule, and twelve cases in `apps/web/test/removedPage.spec.tsx`. Written **against `ux-states.md` §7.5–7.10 rather than ported** from `lane-f` (see TASK-098). ⚠ **The `role="status"` names the ORIGINAL DATE** — *"'{name}' is back on your {service} list, with its original date (4 Jan 2026)."* — because §7.7 is how **US-025 AC-2 becomes visible at all**; a success toast that omits it looks correct and silently drops the acceptance criterion. `T-UX-075a` fails if the date is dropped. ⚠ **The `WORK_SUPPRESSED` branch calls unsuppress and THEN retries** (§3.6, `T-UX-035b`), and **"Keep both" retries with `confirmDuplicate: true`** (§3.5, `T-UX-034b`) — both mutation-proven, since either one silently degrades to "the button does nothing" without failing a naive render test. Restore remains an explicit action; no path auto-restores. Ids `T-RES-016a`, `T-UX-034a/b/c`, `T-UX-035a/b/c`, `T-UX-074a`, `T-UX-075a/b/c`, `T-UX-076a`; the five `T-UX-*` ids were defined only in `ux-states.md` and are imported into `specs/testing.md` §12.3 so the backlog may cite them. ⚠ **Known gap:** after every row is dismissed by a restore the list renders with no rows but does **not** show the §7.2 "Nothing has been removed yet" empty state — items still exist server-side, and claiming otherwise would be false. ⚠ **COMPLETED LATER: THE RESTORE UI WAS FINISHED AND UNREACHABLE — THE FOURTH INSTANCE OF THE SAME DEFECT CLASS, AND THE FIRST ONE A GATE FOUND INSTEAD OF A HUMAN.** `RemovedPage` (398 lines, including this `RestoreControl`) was mounted by `routes.tsx` **bare, with no props**: `items` defaulted to `[]`, so `/removed` told the owner that nothing had ever been removed — against a fully working `GET /api/removed` — and `onRestore` was `undefined` on every row, so nothing built here could be reached at all. `apiClient.getRemoved` **did not exist**. Twelve `removedPage.spec.tsx` cases and the whole server-side suite stayed green throughout, because each injects `items` by hand: that measures rendering and says nothing about whether anything ever fetches. ⚠ **`T-INFRA-013b` IS WHAT SURFACED IT, ONE PR AFTER SHIPPING.** The gate had baselined `restoreListing` as unreached with the note *"the restore UI is TASK-099 and the removed view it hangs off is unbuilt"* — the gate was right and that diagnosis was wrong; the view was built, and nothing joined the three finished pieces. The baseline entry is now **discharged** (`T-INFRA-013c` fails if a wired method stays baselined), leaving `getTitle` and `removeBatchImage`. Now delivered: `apps/web/src/containers/RemovedRoute.tsx` (**new**, mirroring `SuppressedRoute`), `apiClient.getRemoved` (§6.9, plus its `T-DATA-003a` invocations entry), and the one-line `routes.tsx` change. `apps/web/test/removedRoute.spec.tsx`, 8 cases, `T-DATA-002v`–`ac` (catalogued in `specs/testing.md` §36.2a). **Every case drives the container, and `T-DATA-002ac` drives the ROUTE TABLE itself** — a container can be correct in every respect and reachable by nobody, which is exactly what shipped, and `ac` is the only case in the block that fails on a one-line revert of `Component:` to the bare page. 3/3 mutants killed: reverting the route table failed `ac` **alone** (the other seven passed — proof that container-only assertions cannot see this defect); dropping `onRestore` failed `T-DATA-002z`, reproducing the shipped behaviour exactly; and pinning `loadFailed` to `false` failed `T-DATA-002w`/`x`. ⚠ **`T-DATA-002w` IS THE MOST IMPORTANT CASE IN THE BLOCK**: a failed read must render the failure state and **never** an empty log, because on this one screen an empty view asserts something false about the owner's data (the same property `T-UX-072b` protects at the page level, now protected at the fetch level too). ⚠ The retry matcher was tightened from `/retry\|try again/i` to the imported `RETRY_LABEL` — three different retry labels exist in `copy.ts` (`Retry`, `REVIEW_RETRY_LABEL`, `STATUS_RETRY_LABEL`) and a regex spanning them would pass against the wrong screen's button. ⚠ **The known gap recorded above still stands** and is unrelated to this fix. | `T-RES-016`, `T-UX-014` |
| `TASK-100` | `apps/api/test/integration/reappearance.spec.ts` (new, 5 cases) — `T-REAP-010`, `T-REAP-011`, `T-REAP-012`, `T-REAP-013`, `T-REAP-014`. Mutation: 5 applied, **5 killed**, each by its own named test. ⚠ **NO PRODUCTION CODE CHANGED — THE BEHAVIOUR WAS ALREADY CORRECT, AND THAT IS WHY THIS ROW MATTERS.** `services/batchClose.ts` already inserts a new title when `existing === null | `T-REAP-010`, `T-REAP-011`, `T-REAP-012`, `T-REAP-013`, `T-REAP-014` |
| `TASK-101` | `T-SUP-001`, `T-SUP-010`, `T-SUP-012`, `T-SUP-013`, `T-SUP-014` | `T-SUP-001`, `T-SUP-010`, `T-SUP-012`, `T-SUP-013`, `T-SUP-014` |
| `TASK-102` | `components/SuppressDialog.tsx` — `T-UX-085` (a–f), `T-UX-022` (a–l). The row is reported `pending` while the request is in flight and `suppressed` only once the server has persisted it, so a rejected request cannot leave a hidden row behind — `T-UX-085a` asserts `suppressed` is never reported **at all** on the failure path, which an optimistic-hide-then-reconcile implementation would not satisfy even though it ends on `present`. Undo goes back through the `suppressionId` the server returned (`supp:<workIdentity>`), never a row-scoped key (REQ-071); an idempotent 200 offers Close only, because nothing changed. Five mutations caught: optimistic hide before persistence (`T-UX-085a/e`, `T-UX-022d`), undo failure showing the row again (`T-UX-022f`), an idempotent 200 rendered as a fresh hide (`T-UX-022g`), the same 200 still offering Undo (`T-UX-022g`), and the undo affordance removed (`T-UX-022b/c/d/e/f/k`). Four invented copy constants live in the component, not `copy.ts`, each with a ⚠ FINDING note — see the spec defects below. ⚠ **COMPLETED LATER: THE DIALOG WAS FINISHED, CORRECT AND UNREACHABLE.** TASK-102 built `SuppressDialog` and TASK-101 built `FixMatchDialog`; neither was ever mounted. `ListPage` rendered `<TitleList items={items} />` with **no callbacks**, so `TitleRow`'s `⋮` and "Find a match" — present, focusable, correctly labelled, 44 px — optional-chained to `undefined` on every press. Worse, **`apiClient.fixMatch` did not exist at all**, so US-030 AC-1 (`must`) had no reachable implementation on any path, while its named tests `T-FIX-010` (server integration) and `T-UI-020` (the isolated dialog) both passed. Now delivered: `apps/web/src/components/RowMenu.tsx` (**new**, `specs/ui.md` §2.3), `apiClient.fixMatch` (§6.5), the `ListPage` menu/dialog state, and the four `ListRoute` call-throughs. `apps/web/test/rowMenu.spec.tsx`, 10 cases, `T-UX-030a`–`j` (`ux-states.md` §3.1 — an id defined in the specs and implemented by no test anywhere). **Every case drives `ListPage`, never a component in isolation: a test that mounts the component under test can never discover that nothing mounts it.** 3/3 mutants killed — reverting `ListPage` to the unwired list failed **8 of 9** cases, reproducing the shipped defect exactly; hiding rows on `pending` instead of `suppressed` failed `T-UX-030j`; and pointing the dialog at `items[0]` instead of the row the menu was opened from failed `T-UX-030g`. ⚠ **`T-UX-030j` HAD TO BE STRENGTHENED MID-MUTATION AND THE REASON GENERALISES.** Its first form asserted only that a *failed* suppress left the row visible — and the optimistic-hide mutant **survived**, because the row is restored when the error arrives, so an end-state assertion sees nothing wrong. The mutant makes the row vanish mid-request and stay vanished for as long as a slow or hanging request takes. The case now holds the promise open and asserts the row is present **while in flight**. ⚠ **A PASSING TEST WAS CODIFYING THE DEFECT AND HAS BEEN CORRECTED, NOT DELETED.** `T-UI-010k` in `apps/web/test/titleRow.spec.tsx` clicked the `⋮` on an unwired `ListPage`, asserted the row survived, and described this as correct "as the read-only list does" — but `specs/ui.md` §2.2 describes no read-only list and `ListPage` was the only consumer. `TitleRow` now renders the `⋮` only when `onOpenMenu` is defined and "Find a match" only when `onFixMatch` is defined, and the case asserts the row renders safely **and offers no dead affordance** (`T-UX-030h` asserts the same property from the page). This is a deliberate behaviour change to a shipped component, surfaced here rather than buried: an a11y or tap-target sweep counts an inert button as happily as a working one. ⚠ **TWO EXISTING INVARIANT TESTS CAUGHT REAL DEFECTS IN THIS WORK, WHICH IS THEM EARNING THEIR KEEP:** `T-DATA-003a` (enumerated, not sampled) failed because the new `fixMatch` sent no `credentials: 'same-origin'` entry in its enumeration, and `T-CSS-001a` failed on `row-menu` having no rule in `index.css`. ⚠ **`TmdbSearchResult.mediaType` NARROWED from `string` to `'movie' \| 'tv'`** — the server drops anything else (`tmdbClient.ts` L381; TMDB search also returns `person`), so the client type was wider than anything that can arrive. ⚠ **`confirmDuplicate` is always sent explicitly** so US-030 AC-4's warning cannot be bypassed by omission. ⚠ **FINDING: `T-API-010` CANNOT SEE THIS DEFECT CLASS AND IS NOT AT FAULT.** Client→server parity is one-directional **by design** (`GET /api/images/:imageId` is reached from an `<img>` href, so a bidirectional sweep would fail on correct code) — a **missing client method** and an **unmounted component** are both asserted by nobody. This is the third instance: `GET /api/batches/:batchId` (TASK-076), `FixMatchDialog`, and `apiClient.fixMatch`. ⚠ Orphan baseline ratcheted **48 → 26** in the same change: 22 ids listed by the gate itself as now-owned were pruned. ⚠ **THE DEFECT CLASS IS NOW GATED, NOT JUST FIXED** — `tests/infra/webReachability.spec.ts`, `T-INFRA-013a`–`d`. Three instances shipped and all three were found by hand; the fourth will fail CI. `d` (every `components/`/`pages/` module imported by another `src` module) ships with **no baseline at all**, because it was RED before this change and green after — an unmounted screen is a feature the owner cannot reach and there is no acceptable number of those. `b` carries a **ratcheted baseline of three**, and the gate found two of them that nobody had recorded: **`getTitle`** (the `/#title-<titleId>` deep link resolves against the already-loaded list, so nothing fetches a single title — the one previously-known instance), **`removeBatchImage`** (the upload screen's per-image remove control is not built), and **`restoreListing`** (TASK-099's restore UI, hanging off the unbuilt removed view). `c` fails if a baseline entry is ever wired without being removed, so closing a gap cannot silently loosen the ratchet. ⚠ **BOTH MATCHERS ARE DELIBERATELY LOOSE** — any reference counts, not just a call — because a reachability gate that produces one false positive gets a baseline entry added to silence it and then protects nothing; `undoBatch` and `undoRemovalGroup` are reached **only as prop references** today, and the loose matcher is what keeps them honestly out of the baseline. 3/3 mutants killed: an unimported component under `components/` (fails `d`), an unreferenced client method (fails `b`), and a baseline entry that is in fact wired (fails `c`). **A43 follow-up (`T-INFRA-013e`):** the loose matcher for `b`/`c` then cost exactly what it was warned it might — `undoBatch`/`undoRemovalGroup` appeared only in the PROP DECLARATIONS of `ListPage` and `BatchAppliedNotice`, satisfying a bare-identifier match while nothing called them, so `BatchAppliedNotice` (US-017 AC-1, `must`) shipped never having rendered. `b`/`c` now require a call form `` `\.method\s*\(` ``; `d` stays loose, where a false positive is still the worse failure. ⚠ **Verified against the real defect rather than assumed:** with `ListRoute`'s `client.undoBatch(batchId)` replaced by a no-op, the old matcher stayed GREEN and the new one reports `['undoBatch']` — the false negative reproduced and then closed. The tightening revealed **no** further gaps, so every other client method is genuinely invoked. `e` is the gate's own regression test, asserting a prop declaration, a destructuring binding, an `on`-prefixed JSX attribute and a type member all fail to match.**`T-INFRA-014` (`tests/infra/specPaths.spec.ts`, `a`-`e`) closes the FOURTH disguise of the same class:** not a wrong assertion but a stale INSTRUCTION - a spec naming a source path whose file has moved, which an agent following it literally turns into a duplicate module that passes its own tests. It found `specs/ai.md` §2.1c naming ~~`apps/api/src/extraction/crossCheck.ts`~~ (actual: `packages/domain/src/extraction/crossCheck.ts`), then six more across `specs/api.md`, `specs/testing.md`, `docs/backlog.md` and `docs/status.md` - including a `pixelGuard.ts` divergence that `specs/testing.md` had **already flagged** with *"should be settled before TASK-145 is built"*, and TASK-145 was then built anyway. All seven corrected in place. ⚠ **Mutation-testing the gate found a hole in the gate:** blinding the detector (`elsewhere.length === 0 \|\| true`) left `a`-`d` **all green**, so `e` was added as a positive control that drives the detector over synthetic text; a dropped `CODE_ROOT` likewise stayed green until `a` was made per-root. 3/3 mutants killed after strengthening. | `T-UX-022`, `T-UX-085` |
| `TASK-103` | `apps/api/test/integration/suppressionLifecycle.spec.ts` (10 tests), `T-SUP-003`. Seven mutations applied and each caught by its named test (the close-time gate deleted; the gate no longer skipping the write; the point read widened to ignore `active: false`; the gate branching on the `tmdb:` prefix; the review filter matching by prefix instead of by identity; the review filter dropped entirely; and `suppressedGated` ceasing to count the in-transaction gate). ⚠ **THE GATE ITSELF WAS ALREADY BUILT** — in the repository read (`batchReview.ts` `loadReviewCandidates`) and again inside the close transaction (`batchClose.ts`) — but the property TASK-103 names had never been asserted end to end, and mutation testing shows why that mattered. **Deleting the in-transaction gate outright left every black-box case green.** The close re-reads the review at close time, and that read already filters suppressed identities out, so in a single-request world the second gate never fires: it is not dead code — the window between `loadReviewCandidates` and `runInTransaction` is real — but it is microseconds wide and cannot be opened from outside the process. The honest form is the same partial `vi.mock` injection `T-REM-015` uses: `listActiveSuppressions` is made to return `[]` for the LOAD only, exactly as it would have moments before the owner pressed the button, while the real point read inside the transaction sees the row that is genuinely in the store. Nothing else is stubbed. Without that seam, four of the seven mutants survived. ⚠ **THIS MUST BE AN INTEGRATION TEST, AND THE INVARIANT IS THE REASON.** Suppression is keyed on canonical WORK IDENTITY rather than a row id precisely BECAUSE a reappearing work becomes a brand-new row (product invariant 7) — the title the owner pressed "not interested" on no longer exists by the time the gate has to fire, so the store is the only thing that can join the two. A stubbed repository would be asked for a suppression by whatever key the handler chose and would agree, which is the exact defect the file exists to make impossible: a row-scoped gate appears to work right up until the first reappearance and then silently stops. ⚠ **THE GATE IS A POINT READ, AND OVER-MATCHING IS THE INVISIBLE FAILURE.** A prefix scan or a `startsWith` presents as titles the owner never suppressed quietly failing to appear, and there is no record anywhere of a row that was never created; the neighbour case pins that a second, unsuppressed candidate in the same batch still lands. ⚠ **NO BRANCH BY MATCH STATE** (US-028 AC-6′): a text-derived `unmatched:` identity gates identically at BOTH gates, which are separate pieces of code that could drift apart. Instability is a reason to caveat the suppression in the UI, never a reason for the gate to treat it as a different kind of key — a branch there would mean the one class of work the owner most wants gone is the one that keeps coming back. ⚠ **THE GATE REFUSES TO CREATE; IT NEVER REMOVES OR TIDIES.** The old removed Title and ServiceListing rows are asserted byte-identical after a gated close (REQ-028, invariant 7 — the removed view is a historical log, not a recycle bin), a suppressed work's listings on OTHER services are left alone, and the un-suppress case proves the suppression is what stopped the write rather than some unrelated failure, then requires the re-upload to create a NEW title row rather than resurrect the old one. `T-SUP-003` was until now one of the two probe ids in `T-STATUS-001p` (an id mentioned but not delivered); delivering it correctly failed that gate, and the probe has been MOVED to `T-IMG-017`, never relaxed — `T-PERF-001` was tried first and rejected by the same assertion, which is the gate working. | `T-SUP-003` |
| `TASK-104` | `apps/web/src/lib/apiClient.ts` (`SuppressionItem.identityStability` narrowed to the closed pair), four new component cases in `apps/web/test/suppressedPage.spec.tsx` and two new integration cases in `apps/api/test/integration/suppressionLifecycle.spec.ts` (now 17). `T-SUP-006` (b–g; `a` already existed). Six mutations applied and each caught by its named test (the stability pinned to `stable`; pinned to `text-derived`; inverted; the caveat rendered on every row; the caveat keyed on a missing poster instead of on the stability; and the caveat never rendered). ⚠ **THE STORE AND THE PAGE WERE ALREADY BUILT — THE DEFECT WAS IN THE CONTRACT BETWEEN THEM.** `apps/web/src/lib/apiClient.ts` typed `identityStability` as a free `string`, so the component fixture used `'matched'`, a value no server can produce (`specs/api.md` §6.7 sends `stable` or `text-derived`, derived by `identityStabilityOf`). The negative case therefore passed because ANY value other than `'text-derived'` hides the caveat: it asserted the fallback branch, never the contract, and the real negative value was never once exercised. This is the same shape as the vacuous safety assertion TASK-072 found — a test that is green for a reason unrelated to the thing it claims. The type is now the closed pair and the case is corrected **in place** rather than deleted. ⚠ **THE CAVEAT IS KEYED ON THE STABILITY, NOT ON SPARSE METADATA.** The rows most likely to be unmatched are also the rows most likely to have no poster and no year, so a caveat keyed on either would look right on every realistic fixture while being wrong in principle — and would then appear on matched rows whose metadata simply had not loaded yet. It also attaches **per row**: a page-level banner would tell the owner their matched suppressions are unreliable when they are not. ⚠ **STABILITY IS DERIVED ON EVERY READ, NEVER FROZEN ONTO THE ROW.** Fix-match (`T-FIX-005`) can migrate a suppression from a text-derived identity to a TMDB one, and a stored copy would go on telling the owner their now-stable suppression is unreliable. ⚠ **AN UNMATCHED ROW STAYS FULLY ACTIONABLE.** OQ-015 was closed by making unmatched works suppressible; a caveat shipped alongside a disabled control would re-open it in practice, leaving the owner able to suppress but never to undo. ⚠ **SPEC/LEDGER FINDING, reported not acted on:** the epic row for this task says store `identityStability: 'unstable'`, but the value the API defines and sends is **`text-derived`** (`specs/api.md` L888) — `unstable` appears nowhere in the code or the specs. Implemented to `specs/api.md`; the backlog's epic prose is the stale copy. | `T-SUP-006` |
| `TASK-105` | Five new cases in `apps/api/test/integration/suppressionLifecycle.spec.ts` (now 15). `T-SUP-016` (a–e); `T-SUP-004` (a–d) already delivered by TASK-083. Three mutations applied and each caught by its named test (the candidate suppression filter dropped; the same filter widened so one suppression hides every work; and lifted suppressions still filtering the review). ⚠ **THE REVIEW IS RECOMPUTED ON EVERY READ, AND THIS IS THE CASE THAT MAKES THAT MATTER.** A review frozen when the batch entered review would keep rendering a work the owner has just said they never want to see again, and the only way out would be to close a batch they no longer agree with. `T-SUP-016a` walks the same open batch across the suppression, and `T-SUP-016d` walks it back across the un-suppression — which is also what proves the disappearance is the suppression rather than the candidate having been consumed. ⚠ **THE REMOVAL SET IS COMPUTED FROM ACTIVE LISTINGS, NOT FROM CANDIDATES**, so the candidate filter alone does not deliver REQ-073: a suppressed work still holding a listing would go on being proposed for removal in every full-update, asking the owner over and over to confirm the removal of something they already said they never want to hear about again. `T-SUP-016e` drives the section in both directions and pins `count: 0` rather than `omitted` — product invariant 2's distinction, the section was shown and was empty. ⚠ **THE CANDIDATE ROW IS NOT DELETED, ONLY UN-RENDERED** (REQ-012). Suppression is a rendering decision about a work, not a licence to destroy the record of what a screenshot said; "interested again" has to be able to show the candidate exactly as it was read, and a deleted row cannot come back. ⚠ **FINDING, recorded rather than acted on: the suppression filter on the classification index is REDUNDANT BY CONSTRUCTION.** Mutation showed it is unobservable — the index is keyed on work identity, `classifyWorkIdentity` only ever asks it about a candidate's own identity, and every suppressed identity is already absent from `candidates`, so no lookup can reach a suppressed entry. It is kept, with a comment at the site, because the redundancy is the point: it makes the index correct on its own terms rather than correct only while the candidate filter stays exactly as wide as it is today. ⚠ Its survival under mutation must **not** be read as evidence the candidate filter is optional — that one is load-bearing and pinned here. Two fixture traps worth remembering: `alreadyOnYourList` and `removals` are both **omitted in append-only mode**, so an open-review case has to use a `full-update` batch or it asserts against an empty section for the wrong reason; and already-present items carry `rawText`, not `name`. | `T-SUP-004`, `T-SUP-016` |
| `TASK-106` | `GET /api/suppressions` + `POST /api/suppressions/:suppressionId/unsuppress` in `apps/api/src/routes/suppressions.ts` (§6.7/§6.8) + `findSuppression`, `listActiveSuppressions` ordering, `identityStabilityOf`, `toSuppressionItem`. `T-SUP-020a`-`h`, `T-SUP-021a`-`i` (`apps/api/test/unit/suppressionsListRoute.spec.ts`, 17 cases); `T-SUP-020i`-`l`, `T-SUP-021j`-`n` (`apps/api/test/integration/suppressions.spec.ts`, 9 added cases). ⚠ **`restoredAnything` is a CONSTANT `false`, not a count, and must never become one** — un-suppression lifts a filter and restores nothing (product invariant 7). Computing it from the update count would make it true one day and silently turn an honest sentence in `ui.md` §7 into a false one; `T-SUP-021d` pins it across both counts. ⚠ `findSuppression` is deliberately **not** filtered on `active`: a second press from a stale page must be 200, not a 404 for a record that plainly exists (`T-SUP-021e`/`f`). The route resolves the path id to a **work identity** and deactivates on that (`T-SUP-021c`, invariant 1) — passing the path string through would answer 200 having changed nothing. Items are shaped field by field, never spread, so `migratedFrom` (a fix-matched title's previous identity) cannot leak (`T-SUP-020d`, asserted against the raw body). `T-SUP-020j` proves the suppressed view renders after the Title row itself is gone, which is the entire reason `displaySnapshot` is frozen at suppress time. | `T-SUP-020`, `T-SUP-021` |
| `TASK-107` | `apps/web/src/containers/SuppressedRoute.tsx` (**new**) + the one line in `apps/web/src/routes.tsx` that mounts it; `apps/web/test/suppressedRoute.spec.tsx`, 12 cases, `T-DATA-002j`–`u`. 9/9 mutants killed (route table reverted to the bare page; `items` pinned to `[]`; `loading` pinned false; `loadFailed` pinned false; `onRetry` dropped; the refusal branch deleted; `onUnsuppress` stubbed to resolve; a failed un-suppression swallowed as success; and a refetch added after a successful un-suppression). ⚠ **THE PAGE WAS COMPLETE, CORRECT AND ALREADY SHIPPED — `/not-interested` RENDERED AN EMPTY LIST ANYWAY.** `SuppressedPage` is fully prop-driven with `items = []` as its default, and `routes.tsx` mounted it with **no props at all**, so the screen told an owner with suppressions that they had none. That is indistinguishable from data loss, and it was permanently green: all 20 cases in `suppressedPage.spec.tsx` inject `items` by hand, which measures rendering and says nothing about whether anything ever fetches. This is the identical defect class TASK-177 fixed for `ListPage` — **containers fetch, pages render** — and `T-DATA-002t` asserts the route-table line directly, because asserting the container's behaviour alone would not catch a one-line revert. ⚠ **FINDING, reported not acted on: TASK-177 IS MARKED `done` BUT DID NOT WIRE THE SCREEN IT NAMED.** Its epic row reads *"wire the read screens (list, removed, suppressed, history, provenance, rating)"*; `apps/web/src/containers/` contained exactly one file, `ListRoute.tsx`, and **`suppressed` was a finished page it never connected**. The remaining screens are a *different* and lesser problem: `RemovedPage`, `BatchHistoryPage`, `BatchStatusPage` and `ReviewPage` are declared **stubs** (`RemovedPage` is four lines and says `STUB` in its header), so they are honestly unbuilt rather than dishonestly blank, and they belong to their own later tasks (TASK-093 and neighbours) — the client has no `getRemoved`/`getBatches`/`getBatch` method to wire them to yet. `RatingLookupPage` calls `apiClient` itself, on demand. ⚠ **AND `T-DATA-002` IS DEFINED AS *"EVERY data screen actually issues a request on mount"* WHILE COVERING ONE.** The id's own wording is the gate that should have caught the suppressed screen; it was satisfied by a single-screen suite, and is now satisfied by two. ⚠ **UN-SUPPRESSION IS A MUTATION AND STAYS IN THE EVENT HANDLER** (REQ-102): React 19 double-invokes effects under `<StrictMode>`, so the same call placed in one would restore a title the owner never asked to restore. `T-DATA-002q` renders under `StrictMode` and requires `unsuppress` to be absent from the call log. ⚠ **A SUCCESSFUL UN-SUPPRESSION DELIBERATELY DOES NOT REFETCH**, and `T-DATA-002s` pins the absence: the page already drops the row locally, so the only visible effect of a refetch is to remove a row twice — while a refetch that *fails* would replace a screen that just worked with an error state about something that already succeeded. ⚠ **A 403 IS THE WHOLE SCREEN, NOT THE FAILURE STATE** (`T-DATA-002o`): the owner is authenticated, so the retry the failure state offers could never succeed. ⚠ **A FAILED READ MUST NOT RENDER AN EMPTY LIST** (`T-DATA-002m`) — the empty list here reads as *"you have never marked anything not interested"*, the exact sentence this screen must never impersonate. ⚠ **FIXTURE TRAP: `SuppressionItem` NESTS `name`/`releaseYear` UNDER `displaySnapshot`.** The first draft of the fixture invented a flat shape and reached it with `as SuppressionItem`; the cast silenced the compiler and every row threw at runtime. The cast is gone and the fixture is now type-checked against the real contract. ⚠ **LOCAL-ONLY FLAKE, not a regression:** running `--project unit --project infra --project meta --project golden` in one invocation fails 4 HEIC-decode cases (`T-IMG-015h`, `T-IMG-023g` and two neighbours) under memory contention; each project passes in isolation (1178, 383, 20) and CI runs them as separate jobs. Same family as the coverage-only flake recorded on TASK-073. | `T-DATA-002`, `T-SUP-006` |
| `TASK-108` | `tests/e2e/journey.spec.ts` — extends the same `T-E2E-001` journey (see TASK-094) through steps 6–7 on the stateful backend. Step 6 = the owner suppresses a **present** title (dune): the row's `row-menu` → `row-menu-suppress` → page-level `SuppressDialog` (h2 *Not interested*, confirm *Not interested*) → success → the row leaves the list, hidden **both** client-side (`ListPage.visible`) and by the server `titlesResponse` filter. Step 7 = a third, **append-only** batch in which arrival (previously **removed**) and dune (previously **suppressed**) both reappear in the screenshots: arrival returns as a brand-new addition **dated today** while dune stays absent — the contrast that expresses REQ-071 end-to-end (suppression is keyed on canonical work identity, so it survives arrival's reappearance-as-new-row without leaking dune back). 2/2 load-bearing branches mutation-tested and each killed by its named assertion, then restored + reverified green: **M4** `ListPage.visible` no longer filtering `rowStates[...] === 'suppressed'` killed the step-6 client-hide assertion; **M5** the stub `titlesResponse` dropping its server-side suppression filter killed the step-6 suppression-**persistence** assertion (proving the test observes the *server* honouring suppression, not only optimistic client hiding). ⚠ **FINDING (a) — RESOLVED IN FAVOUR OF THE SPEC.** The "Removal 1 of 1" ordinal chip did not render for a singleton: `RemovedPage.removalOrdinalLabel` returned `null` when a work's total removals were `<= 1`, so arrival's single removal showed no ordinal. ~~the journey asserts `removed-ordinal` count **0** and this is reported, not patched (RemovedPage is another lane's file / a spec mismatch)~~ — escalating rather than working around it was the right call, but the conclusion that the specs were "silent on the singleton" was too generous to the implementation: **`specs/testing.md` §5 step 7 names "Removal 1 of 1" outright**, `ui.md` lists the chip as a per-**row** element and `ux-states.md` §7.5 says "One row per removed listing, with ordinal chips". The function's own doc comment cited "§7.5" for a rule §7.5 does not contain. Now fixed; the journey asserts the chip's **text**. ⚠ **FINDING (b): the REQ-071 work-identity invariant is fundamentally a backend property owned by `T-SUP-003` (integration).** The e2e can only express it *observably* via the step-7 arrival-vs-dune contrast; a row-id-keyed suppression would still pass a naive "dune is hidden" check, so the honest journey-level guard is the reappearance contrast, and the identity-keying itself remains `T-SUP-003`'s to prove. ⚠ Full Playwright suite **86/86** green on both `chromium` and `mobile-safari` after a clean `npm run build -w @nextup/web`; `T-PASTE-010` regression guard untouched and passing. **R2 integrity (follow-up):** added a **proves-vs-stub-enforced boundary** header to the file naming which properties are genuinely proven (real-SPA routing, review sections, removal-confirm gate, rendered dates, client-side hide) and which are only **enforced by the stub and NOT proven here** — each mapped to the integration test that truly owns it (`T-SUP-003` for REQ-071 identity keying; reconcile-by-absence and the removed-log dating per `specs/testing.md` §6). Written as a warning to the next person: deleting the server-side suppression filter in `apps/api` would keep every e2e green while the owner's "not interested" silently breaks (invariant 1). Part-3 ordinal resolved as a **spec-vs-impl conflict** (finding (a)) after reading `specs/ui.md` / `ux-states.md` §7.5 / `testing.md` §5 — absence assertion (`removed-ordinal` count 0) kept and the journey **not** bent to force a second removal. Mutation **M(b)** re-run post-boundary-doc: deleting the stub `titlesResponse` suppression filter still fails step 6/7, confirming the documentation weakened no assertion — killed, restored by edit. | `T-E2E-001` |
| `TASK-109` | `apps/api/src/routes/fixMatch.ts` (§6.5) + `findActiveTitleByWorkIdentity` in `ownerData.ts`, registered in `routes/index.ts` with a per-request TMDB client. `T-FIX-010`, `T-FIX-002`, `T-FIX-003`, `T-FIX-004`, `T-FIX-006` — 14 integration cases (`apps/api/test/integration/fixMatch.spec.ts`) and 16 unit cases (`apps/api/test/unit/routes/fixMatch.spec.ts`). 9/9 mutants killed. ⚠ **THE OBVIOUS IMPLEMENTATION SILENTLY DESTROYS DATA THE OWNER CANNOT GET BACK.** Delete the wrongly-matched title, insert a right one: it passes "is it matched now?" and loses every `dateAdded` — which came off a screenshot the owner may no longer have — and with them the row's position in the default sort (REQ-038, invariant 6). So this handler UPDATES one row's identity columns and touches no listing at all, `T-FIX-002` compares the **stored** listing rows before and after rather than the response (a handler that deleted and re-created them could still echo the ids it was given), and `T-FIX-003` compares the rendered ordering of three titles across the call. ⚠ **`sortDateAdded` IS NOT RECOMPUTED, AND THE ABSENCE IS THE ASSERTION.** It is derived from listings' `dateAdded` and no `dateAdded` changes, so recomputing can only reproduce the same value or a wrong one; `T-FIX-002b` reads the `updateTitle` argument back and requires `sortDateAdded`, `id`, `createdAt` and `state` to be **absent** from it, so a later refactor that widens the write fails there rather than quietly moving a row. ⚠ **GATE ORDER IS LOAD-BEARING: suppression, then duplicate, then TMDB.** Reversed, an owner fix-matching onto a work they marked "not interested" is told it is a duplicate — true, but not the reason they cannot do it, and it points them at the wrong remedy; both gates are also network-free, so a TMDB outage must not stop either answering. `T-FIX-006` drives a case where both conditions hold at once. ⚠ **BOTH GATES ARE SKIPPED WHEN THE IDENTITY DOES NOT CHANGE.** The only active suppression that can hold an unchanged identity is this title's own, and the only active title holding it is this one — refusing there would make a metadata-only re-match impossible for a suppressed or already-matched row (`T-FIX-004b`). ⚠ **`confirmDuplicate` MUST BE WRITTEN, NOT MERELY HONOURED.** `title_one_active_per_work` is unique on `(owner, work_identity, duplicate_ack_seq)`, so the second active row is legal only once that column differs from `''` — the title's own id is used. The mutant writing `''` is killed by `T-FIX-004`. A non-boolean `confirmDuplicate` is **refused, never coerced**: `"true"` from a client that stringifies its body would create a duplicate while the client believed it had asked a question. ⚠ **`rawExtractedText` MUST be cleared — it is a CHECK, not a convention.** `title_match_coherent` requires a matched row to carry a tmdb id and no extracted text; the mutant leaving it in place fails at the database. ⚠ **The stored IMDb rating is cleared too.** It belongs to the work the row used to be; left in place the owner sees one work's name beside another work's score, and `imdbRatingFetchedAt` keeps it there for the whole 14-day horizon. ⚠ **SPEC FINDING, reported not acted on: `specs/data-model.md` §6.3 step 7 is not implementable as written.** It asks for `provenance.modified` entries with `batchId: null`, but `batch_change.batch_id` is `NOT NULL` with a foreign key to `upload_batch` — and it contradicts `packages/domain/src/provenance.ts`, whose header states that fix-match, suppress, un-suppress and restore are **deliberately invisible** to batch provenance so a batch undo never reverses an owner decision made afterwards (US-031 AC-5). The provenance module's reading is the one implemented; no `batch_change` row is written. | `T-FIX-002`, `T-FIX-003`, `T-FIX-010` |
| `TASK-110` | `migrateSuppression` in `ownerData.ts` + the SD-06 arm of `fixMatch.ts`; `suppressionMigrated: { from, to }` on the 200, `null` when nothing moved. `T-FIX-005` — 3 integration cases and 2 unit cases; 3 of the 9 mutants above are aimed here and all are killed. ⚠ **A SUPPRESSION THAT DOES NOT MOVE IS A SILENT RE-ADMISSION.** Suppression is keyed on work identity (REQ-071, invariant 1) and fix-match **replaces** that identity, so leaving the suppression behind makes the work the owner rejected visible again on the next render with nothing anywhere to say why — the REQ-071 hole reached from a direction no existing test covered. `T-FIX-005` asserts the work stays hidden from `GET /api/titles` after the move, which is what distinguishes a migration from a suppression that merely still exists. ⚠ **REACTIVATE BEFORE CREATE, and the id is why.** The suppression id is `supp:<workIdentity>` and therefore deterministic: at most one row can ever exist per identity, so a target the owner suppressed and later lifted already has one and a bare `create` collides on the primary key — failing the whole fix-match for a case that is entirely normal. A dedicated case seeds exactly that state. ⚠ **The old row is DEACTIVATED, never deleted** (REQ-028): the record that the owner made the decision, and when, survives the move, and `migratedFrom` on the new row is the only breadcrumb back to it — `Title` carries no `previousWorkIdentity` column. ⚠ **The display snapshot is taken from the NEW work.** The suppressed view renders without a title row, so a snapshot left at the old OCR text would be the only name the owner ever sees for that decision. ⚠ **The migration is always reported, never silent** (`ui.md` §9.4, ux-states §3.7): `FIXMATCH_SUPPRESSION_MIGRATED` already exists in `apps/web/src/copy.ts`, and the API half delivered here is what lets the page state it. | `T-FIX-005` |
| `TASK-111` | `apps/web/src/components/FixMatchDialog.tsx` — `T-UI-020` (11 cases) and `T-UX-033` (5 cases) in `apps/web/test/fixMatchDialog.spec.tsx`. The dialog accepts `searchTmdb` and `fixMatch` as props (dependency injection, identical pattern to `SuppressDialog`) so it carries no direct fetch calls and `T-DATA-001` stays clean. States covered: idle, searching, results, no-results, confirming, submitting, tmdb-unavailable (§3.4), duplicate-409 (§3.5), suppressed-409 (§3.6), success with/without suppression migration (§3.7). `T-UX-033a` pins the verbatim `TMDB_UNAVAILABLE_MESSAGE` ("Couldn't reach TMDB. Try again in a moment. Nothing has changed.") which lives in the component rather than `copy.ts` because `specs/ui.md` §9 has no row for it. `T-UI-020i` asserts `FIXMATCH_SUPPRESSION_MIGRATED` is shown byte-equal to the `copy.ts` constant. The error branch detects `TMDB_UNAVAILABLE` by `code`, not by status — `status === 502` is redundant because the server always sets the code, and a redundant branch produces a surviving mutant rather than an honest finding. 10/10 mutants killed. | `T-UI-020`, `T-UX-033` |
| `TASK-112` | Creates-only batch undo (SD-03). `T-UNDO-001` (14 unit cases over the pure predicate and plan), `T-UNDO-002`/`T-UNDO-003` (15 integration cases driven through the REAL close route so the provenance undo reads is the provenance close writes), a no-store twin of the route for the coverage gate, plus `T-UNDO-008` (a batch that created nothing), `T-UNDO-009` (injected failure mid-undo) and `T-UNDO-010` (the discard module directly, which `T-INV-023b` requires because `apps/api/src/repository/**` is excluded from the unit coverage thresholds). 8/8 mutants killed. ⚠ **`fk_change_listing` MADE THE FEATURE NOT WORK AT ALL, and the fix is an ordering constraint.** `batch_change.title_id`, `batch_change.listing_id` and `extraction_candidate.resolved_title_id` are plain foreign keys onto the rows SD-03 destroys — only `service_listing → title` cascades — so every discard failed outright. The rows are DETACHED (those columns nulled) before the delete, never deleted: purging provenance would violate REQ-028 and would need a third `T-INV-012` entry. ⚠ **The detach is NOT scoped to the undone batch.** A later batch can reference a title this one created (it added a second service), and scoping the detach to this batch's own rows passes every single-batch test and fails the first time an owner undoes an older import — the same reason the listing count is READ from the title rather than inferred from the plan. ⚠ **The discards live in their own module, `undoDiscard.ts`.** The `T-INV-012` exemption is keyed on file **and** model, so granting `ownerData.ts::title` would pre-authorise every future `title.delete(...)` in that 1,300-line repository; a dedicated file keeps the blast radius the size of the exemption, and `T-INV-012k` now asserts the creates-only gate precedes the discard at its call site. ⚠ **Gate order is the contract**: `BATCH_ALREADY_UNDONE` is checked BEFORE `BATCH_NOT_APPLIED`, because `undone` is not `applied` and the generic check placed first reports "that batch was never applied" — false and unactionable. ⚠ **Creates-only is "modified and removed are both empty", NOT "created is non-empty"** — an import where the owner discarded every read is undoable (US-032 AC-5); the mutant asserting otherwise is killed. ⚠ **`T-UNDO-002c` was VACUOUS TWICE before it killed its mutant**, and both failures are instructive: a survivor dated in the past never has its `sortDateAdded` moved by a listing added today, so there is nothing to put back; and a survivor whose only listing is `removed` is the REAPPEARANCE path (invariant L1/A33), where close writes a brand-new title row and the old one is never touched — so both assertions passed against an arbitrary one of two rows. The live version uses an active survivor with a future-dated listing and asserts the title count is 1. The §8.4 refusal ENUMERATION is TASK-114 and `later-owner-edits` is TASK-113; until then a refusal carries `reason: 'modified-or-removed'` and `truncated: false` so the later task has a marker to find rather than a bare 409. | `T-UNDO-001`, `T-UNDO-002`, `T-UNDO-003` |
| `TASK-113` | `detectLaterOwnerEdits` in `packages/domain/src/undo.ts`, gated in `undoBatch` between `isCreatesOnly` and `planCreatesOnlyUndo`. ⚠ **This could not be a provenance predicate.** Suppress and un-suppress write no `batch_change` row at all (US-031 AC-5, proven by `T-PROV-013`) and fix-match is an out-of-batch edit, so a batch whose created titles the owner has since re-decided still reads as perfectly creates-only — and SD-03 then DISCARDS them, a hard delete of a decision the batch has no record of and no way to restore. The evidence therefore comes from the CURRENT rows: an ACTIVE suppression on a created work, or a created title whose current `workIdentity` is **outside the set of identities the batch's CONFIRMED candidates resolved**. ⚠ **THE OBVIOUS PER-TITLE JOIN DOES NOT EXIST AT RUNTIME AND ONLY THE INTEGRATION TEST FOUND THAT** — `extraction_candidate.resolved_title_id` is declared, FK'd, indexed and detached on discard, but nothing ever WRITES it, so it is null for every real row; the first detector joined on it, the no-store twin passed all eight cases because the fixture hand-fed the column, and `T-UNDO-004b` returned 200 instead of 409 against a real database. Recorded as a finding in `specs/testing.md` §22a. The set comparison is complete rather than approximate because the work-identity unique index refuses a fix-match onto an identity a confirmed sibling candidate already holds a title for; restricting the set to CONFIRMED is what closes the remaining hole, since a DISCARDED candidate's identity is a reachable target (`T-UNDO-014i`). The suppression side needs no timestamp — a suppressed work is filtered out BEFORE any Title is created (REQ-071), so it can only have arrived afterwards. `matchState` cannot substitute for the identity comparison: close writes `matched` for every matched creation and fix-match writes the same value. Fix-match is reported in preference to suppression because SD-06 MIGRATES the suppression onto the new identity, so reporting both would describe one owner action as two. No new error code — §8.4 keeps `BATCH_NOT_CREATES_ONLY` and varies `details.reason`, and `UndoRefusalReason` already carried the literal, so `enums.ts` was untouched. 11 mutations run: all killed by the named test, comment-only control survived; forcing the detector to fire on every title left `T-UNDO-014g` green, which is the proof that `BATCH_ALREADY_UNDONE` genuinely outranks it. ⚠ `T-UNDO-004` is CI-verified only — no local Docker. | `T-UNDO-004`, `T-UNDO-013`, `T-UNDO-014` |
| `TASK-114` | `buildRefusalDetails` in `apps/api/src/services/batchUndo.ts` — the 409 carries `created`/`modified`/`removed` in full, each entry with `currentState`, `remedy` and `remedyHref`, plus `truncated: false`. `T-UNDO-006`, `T-UNDO-007` (`apps/api/test/integration/batchUndo.spec.ts`) and the unit cases in `apps/api/test/unit/batchUndoRoute.spec.ts`. ⚠ **`truncated` IS PINNED `false`, NOT COMPUTED, AND THAT IS THE POINT.** A refusal the owner cannot act on item-by-item is a dead end — they are told the undo is impossible and given no way to reach the thing blocking it — so a page/limit here would be a silent regression of US-033 AC-2/AC-5 rather than an optimisation. Any future paging must change the field, the panel and the named tests together. ⚠ **NOTHING IS WRITTEN ON A REFUSAL.** The enumeration is built from reads only, so an owner who triggers it repeatedly cannot mutate their own data by asking a question. ⚠ **THE `removed` ENTRY'S `currentState` IS THE LISTING'S, NOT THE TITLE'S** (`batchUndo.ts:416–420`): its remedy is `restore`, which acts on the listing, so reporting the title's state would show a state the remedy cannot change. ⚠ **`later-owner-edits` (TASK-113) IS DELIBERATELY ABSENT**; a refused batch reports `modified-or-removed` or, with no `batch_change` rows at all, `provenance-unavailable` (US-033 AC-7, `T-UNDO-007`) — refused, never no-op undone, because "no provenance" and "nothing to reverse" are indistinguishable from the data and only one of them is safe. | `T-UNDO-006`, `T-UNDO-007` |
| `TASK-115` | `T-UNDO-006` (`apps/api/test/integration/batchUndo.spec.ts:684`) seeds **134 created + 133 modified + 133 removed = 400 distinct titles** in one batch and asserts `truncated: false` with `seen.size === 400`. ⚠ **THE SIZE IS THE ASSERTION.** A 3-title fixture passes against an implementation that pages at 50, at 100, or at any limit a future "performance fix" introduces; only a fixture larger than any plausible limit can fail it. ⚠ **DISTINCTNESS IS ASSERTED VIA A `Set`, NOT A LENGTH** — a response that repeated one id 400 times would satisfy a count and tell the owner nothing. | `T-UNDO-006` |
| `TASK-116` | `apps/web/src/components/UndoRefusalPanel.tsx` + `apps/web/src/lib/undoRefusal.ts` (`isUndoRefusal` guard, `parseUndoRefusalDetails`), reached from `BatchHistoryRoute` when `undoBatch` answers 409. `UNDO_REFUSAL_*` copy in `copy.ts`; `T-UX-097` (18 cases, `apps/web/test/undoRefusalPanel.spec.tsx`). 4/4 mutants killed (toast-instead-of-panel → `T-UX-097a`; truncate a group at 50 with no **Show all** → `T-UX-097e`; drop the state chip on a since-removed title → `T-UX-097f`; inline the copy → `T-UX-097b`). ⚠ **IT IS A FULL-SCREEN PANEL, NEVER A TOAST, AND `T-UX-097a` EXISTS TO KEEP IT THAT WAY.** The refusal enumerates up to 400 actionable items (TASK-115); a toast dismisses itself and takes the only list of blockers with it, leaving the owner with a button that refuses and no route to the cause. ⚠ **PAGINATION IS CLIENT-SIDE AT 50 PER GROUP WITH AN UNCONDITIONAL "Show all"** — the server's payload stays complete (TASK-114), so the cap is a rendering concession that the owner can always defeat; a cap without the escape hatch would re-introduce truncation in the UI after `truncated: false` promised otherwise. ⚠ **REMEDIES CALL THE REAL `apiClient` METHODS AND REUSE THE EXISTING DIALOGS** rather than re-implementing suppress/fix-match/restore, so the panel cannot drift from the behaviour those flows already have tested. ⚠ **THE UNDO CONTROL WAS GATED ON `status === 'applied' && undoneAt === null`, NOT ON AN `undoable` FLAG — spec finding, reported not fixed:** the `BatchHistoryItem` DTO from `GET /batches` carries no `undoable` field, so `specs/ux-states.md` §9.3's "undoable batches show *Undo this batch*" is approximated. The approximation errs towards showing the control, which keeps the refusal reachable. ⚠ **`specs/ux-states.md` LICENSES NO WORDING** for the panel title, the provenance-unavailable title, or the state-chip labels; those `UNDO_REFUSAL_*` values are invented and annotated as such in `copy.ts`. ⚠ **FOLLOW-ON DEFECT FOUND IN REVIEW AND FIXED SEPARATELY (PR #157):** the unclassified-failure banner shipped its retry as an inert `<span>` — a dead control one element away from the dead control this epic removed. `apps/web/test/undoOutcomes.spec.tsx` now asserts the retry resolves via `getByRole('button')`, and drives a **two-batch** history failing on the second card so a retry hard-coded to the first cannot pass. Those cases carry ux-state ids rather than `specs/testing.md` §9 ids because §9.6/§9.7/§9.10 are `ux-states.md` rows with no PRD acceptance criterion behind them; they are pinned by `tests/meta/uxStateCoverage.spec.ts`, not by the AC map. | `T-UX-097` |
| `TASK-117` | `apps/api/src/services/batchReextract.ts` (new) and one route in `apps/api/src/routes/batches.ts` — `POST /api/batches/:batchId/re-extract` (`specs/api.md` §6.24); `apps/api/test/integration/reextract.spec.ts` (12 cases) — `T-REX-010`/`010a`/`010b`, `T-REX-011`, `T-REX-012`/`012a`, `T-REX-013`, `T-REX-014`/`014a`/`014b`/`014c`, `T-SUP-017`. Mutation: 6 applied, **6 killed**. **No migration and no schema change** — `derivedFromBatchId` and `retainUntil` already existed. ⚠ **FINDING — THIS ROW'S OWN "DONE WHEN" NAMED THE WRONG TESTS, and that is the most useful thing in this entry.** It cited `T-BATCH-010` and `T-RET-014`: the first is US-011's *"POST /api/batches without `service` → 400"*, the second is US-035/A45's *"30-day retention is identical for pasted images"*. Neither is a US-034 test, both are already green elsewhere, and an implementer who trusted the column would have closed re-extraction by running two passing tests that say nothing about it. `specs/testing.md` carries the authoritative AC→test mapping (NFR-003) and maps US-034 to `T-REX-010`–`T-REX-014` + `T-SUP-017`; the column is corrected in place with the superseded citation struck through beside it. ⚠ **RE-EXTRACTION DERIVES A NEW BATCH; IT DOES NOT RE-RUN THE OLD ONE (US-034 AC-3).** The confusable neighbour is `extraction-failed → submitted` RETRY (§6.16), which re-enters the *same* batch. Retry is for a batch that produced nothing; re-extraction is for one that produced results the owner has read and judged wrong — so re-running in place would overwrite candidates the owner is looking at and, where the original was applied, attach a second result set to a batch whose provenance rows (REQ-068) describe the first. `T-REX-012` snapshots the source's **whole row** rather than chosen fields, because the failure it guards is a well-meaning "tidy the source on the way past". ⚠ **RETENTION IS COPIED, NEVER RESTAMPED — the single most consequential line in the feature.** An image belongs to exactly one batch, so the derived batch needs its own `uploadedImage` rows; giving those rows today's `uploadedAt`/`retainUntil` is the natural thing to write and it silently converts NFR-019's flat 30 days into *"30 days after you last re-extracted"*, so an owner who re-extracts monthly retains screenshots indefinitely. `retainUntil` is documented WRITE-ONCE in the schema and this is one of only two paths that could break it; the restamp mutant was killed by `T-REX-012a`. ⚠ **ONE PURGED IMAGE REFUSES THE WHOLE RE-EXTRACTION, and that is correct rather than strict.** Re-reading only the survivors would build a `full-update` batch from a partial view of the service, and full-update reconciles by absence — so titles whose only evidence was the purged screenshot would be **removed**. That is the "a failed extraction must never be misread as a removal" invariant arriving by a different road. ⚠ **THE REFUSAL ORDER IS A PRODUCT DECISION (`T-REX-014b`).** `IMAGES_PURGED` (410) is checked *before* `OPEN_BATCH_EXISTS` (409): answering the 409 first looks defensible and sends the owner to discard work in progress, after which they hit the permanent 410 anyway, having lost the batch for nothing. ⚠ **THE SOURCE IS NOT EXEMPT FROM THE ONE-OPEN-BATCH RULE (`T-REX-014c`).** Reading §6.24's *"if **another** batch is open"* as an exemption is the tempting mistake: deriving from a still-open batch leaves two open at once, which product invariant 3 forbids. The intended flow is discard-then-re-extract — and that is exactly why discard **retains** images, as `services/batchLifecycle.ts` already said in as many words. ⚠ **`T-SUP-017` IS ASSERTED AS AN ABSENCE, DELIBERATELY.** The suppression gate lives in the close transaction and applies to a derived batch **by construction**, since it closes through the same path; the way that breaks is not a missing check here but a re-extraction that bypasses close, so the case pins the derived batch to a reviewable, unapplied state. A second copy of the check on this path would be worse than none — REQ-071 keys suppression on **work identity**, not row id, and a duplicate is a second place to get that wrong. ⚠ **`T-REX-012a` WAS VACUOUS ON ITS FIRST RUN** and now carries an anti-vacuity guard: a refused request leaves `batchId` undefined, and a Prisma `findMany` with an undefined filter returns **every** row — so the retention and blob-path assertions read the *source* image and passed. `tools/check-mutating-routes.mjs` already listed this route in the REQ-041 closed enumeration; its `why` is corrected from "re-runs extraction over an existing batch" to what the route actually does. Six now-owned `T-REX-*`/`T-SUP-017` ids are pruned from `BASELINE_ORPHANS`. ⚠ **THE FIRST PUSH FAILED CI ON COVERAGE, AND THE REASON GENERALISES TO EVERY FUTURE API SERVICE.** `npm run coverage` runs `--project unit --project web` only, so a service proven *exclusively* by integration tests reads as uncovered: `batchReextract.ts` landed with every branch exercised and still dropped `apps/api/src/**` branches to **84.55%** against an 85% floor. The fix is `apps/api/test/unit/routes/reextractRoute.spec.ts` (10 cases, the same pattern as `listingsRoute.spec.ts`, which exists for this identical reason) — **not** a lowered threshold. **Run `npm run coverage` locally before pushing any new `apps/api/src/**` file; the local gate order does not catch this.** The unit suite is not redundant with the integration one: a mocked store agrees with whatever the code asked of it, so `T-REX-012`/`012a` stay integration-only, while the mock makes the refusal *order* directly assertable (`T-REX-014d` sets both conditions true at once and pins which wins). ⚠ **AND IT CAUGHT A THIRD VACUOUS TEST — the boundary case cannot be driven over HTTP.** `T-REX-013b` asserts `isPurged` uses `<=`, not `<`; but the route defaults `now` to `new Date()` *inside* the request, which is strictly later than any `retainUntil` a test can build beforehand, so both operators refuse and the first draft went green against the mutant. It is now driven against `reextractBatch(ownerId, batchId, now)` directly with the two instants equal, plus `T-REX-013c` one millisecond earlier so that a `return true` mutant cannot satisfy it either. | `T-REX-010`, `T-REX-011`, `T-REX-012`, `T-REX-013`, `T-REX-014`, `T-SUP-017` |
| `TASK-118` | Delivered as part of the TASK-052/TASK-014 image work and verified against the tree rather than re-implemented: `retainUntil` is stamped at ingest (`apps/api/src/images/ingest.ts:267`, `now + IMAGE_RETENTION_DAYS`) and persisted on the row (`:289`); availability is derived, not stored, by `isExpired(retainUntil, now)` (`apps/api/src/routes/images.ts:79`) which the serve route consults at `:99` and answers 410 `IMAGE_EXPIRED` at `:100`; and a live in-retention row whose blob has already been reaped answers the SAME 410 rather than a 500 (`apps/api/src/storage/blobStore.ts:170`). Covered by `T-IMG-004` a–d and `T-IMG-005` a–b in `apps/api/test/unit/imagesRoute.spec.ts`, and `T-IMG-004` e–g and `T-IMG-005c` in `apps/api/test/integration/imageServe.spec.ts`. ⚠ **AVAILABILITY IS DERIVED FROM `retainUntil` AT READ TIME, AND MUST NOT BECOME A STORED FLAG OR A SWEEP.** A stored `expired` column needs something to set it, and the only somethings are a scheduler or a job — both prohibited outright by product invariant 5 and `T-CI-005`; the 30-day blob purge is Azure Blob **lifecycle management**, which is storage-side configuration and not a process this application runs. ⚠ **THE ROW SURVIVES THE EXPIRY** (`T-IMG-004f`, `T-IMG-004d`): only the bytes go, so the batch, its provenance and the owner's history stay whole — expiry is a change in what can be *served*, never a deletion, and REQ-028 applies to the row exactly as before. ⚠ **THE TWO 410 PATHS ARE DELIBERATELY INDISTINGUISHABLE TO THE OWNER** (`T-IMG-005b`): "past `retainUntil`" and "blob already gone" are the same fact from the owner's side — the screenshot is no longer available — and splitting them would leak the purge schedule while giving the owner two messages for one situation. ⚠ Retention uses `IMAGE_RETENTION_DAYS` (NFR-019) and must never be conflated with `TMDB_METADATA_MAX_AGE_DAYS` (NFR-014); `T-INV-008` enforces the separation. No code change was needed; this row corrects a stale ledger entry. | `T-IMG-004`, `T-IMG-005` |
| `TASK-119` | `lifecyclePolicyViolations()` in `tools/check-infra.mjs`, ten `T-INFRA-004` cases in `tests/infra/infra.spec.ts` against the **compiled ARM**, and `apps/api/test/integration/retentionPurge.spec.ts` (10 cases, `T-RET-011` a–c, `T-RET-012` a–c, `T-RET-013` a–d). Mutation: **5 applied, 5 killed**, each by the intended test. ⚠ **THE LIFECYCLE RULE IS THE ONLY THING IN THE SYSTEM THAT DELETES A SCREENSHOT, AND ITS FAILURE IS SILENT IN BOTH DIRECTIONS.** No application code participates (US-035 AC-3): the API stops *serving* at `retainUntil`, which `T-IMG-004` already proved, but the bytes go away because of Azure's rule and nothing else. So a rule that is missing, disabled, or filtered to a prefix matching no blob **retains the owner's screenshots forever while every existing 410 test still passes** — and a smaller day count destroys uploads the owner may not have reviewed yet, which REQ-028 makes unrecoverable. Nothing outside `T-INFRA-004` would ever notice either. It pairs with `T-INFRA-002`: a perfectly correct rule **plus** blob soft delete still retains the bytes past 30 days, so neither half is sufficient alone. ⚠ **`T-INFRA-004e` PASSED VACUOUSLY ON ITS FIRST WRITING AND THE MUTANT IS WHAT EXPOSED IT.** It removed the `managementPolicies` resource by filtering `template.resources` — but storage deploys through a **module**, so the top-level filter matched nothing, the template was unchanged, and "no violations" was read as "the mutant was caught". Now removed by walking nested module templates. **Any ARM assertion written against `t.resources` alone is asserting a template that has almost nothing in it.** ⚠ **THE MUTATION THAT WOULD PASS REVIEW IS `tierToCool`, NOT A DELETED RULE.** Swapping `delete` for a tiering action costs *less*, leaves the rule reading as `enabled: true`, and retains every screenshot indefinitely in cool storage. NFR-019 is a privacy commitment, not a storage-cost decision, so `lifecyclePolicyViolations` rejects any `baseBlob` action other than `delete` by name rather than merely checking the day count. `T-INFRA-004f` also asserts **both** directions of a changed window (7 and 365), because they fail differently and only one of them looks like a mistake. ⚠ **THE PURGE IS SIMULATED BY REMOVING THE BLOB, AND THAT IS FAITHFUL, NOT A SHORTCUT.** Azurite implements no lifecycle rules at all (`specs/testing.md` §3.4), and the real rule's entire effect on this system is that the bytes stop being there — it writes nothing back, calls nothing, and the application is never told. `T-IMG-005c` reproduces it the same way. ⚠ **THE STEADY STATE OF A YEAR-OLD ACCOUNT IS: EVERY ROW PRESENT, EVERY BLOB ABSENT** — and that is why an integration suite is worth writing for a feature that runs no code. A join, a lazy `store.get`, or an availability check added to a list or batch read would work perfectly for thirty days in development and then break the owner's history **oldest-capture-first**, the captures least likely to be looked at before the damage became normal. `T-RET-011b` reads the batch and the list back through the real routes *after* the purge for exactly that reason. ⚠ **`T-RET-011a` COMPARES EVERY COLUMN, NOT ROW COUNTS.** A purge handler that "tidied up" by nulling `blobPath`, zeroing `byteSize` or stamping a `purgedAt` keeps every count identical while destroying the record of what was captured — REQ-028 keeps the **record**, not merely the row. `T-RET-011c` pins 410-not-404 because the distinction is what the owner is owed: 404 means *there was never such an image*, 410 means *this happened and the screenshot expired*. ⚠ **`T-RET-012c` WAS UNMUTATABLE AS FIRST WRITTEN AND WAS STRENGTHENED.** Asserting "`retainUntil` is unchanged across the purge" is a claim about code that does not exist, so **no mutation of the shipped system could fail it**. It now drives `GET /api/images/:id` twice after the purge — the one place that *discovers* a blob is gone, and therefore where a "tidy the row while we are here" would plausibly be written. Verified: a route that restamps `retainUntil` on a missing blob now fails it, where before it passed. **An assertion that no mutant can break is not yet a test.** ⚠ **`T-RET-012b` IS A SOURCE ASSERTION WITH A DELIBERATE ALLOW-LIST OF ONE.** `apps/api/src/**` may delete a blob in exactly one place — the owner removing an image from a batch they have not submitted (`batchImages.ts`, §6.13), an explicit user action. `T-CI-005` catches a **scheduler**; this catches the deletion even if it were hung off a request instead, which is the shape an "expire old images" sweep would most likely take once someone notices the application knows `retainUntil` and wonders who acts on it. ⚠ **`T-RET-013b` MOVES THE BATCH TO `submitted` FIRST, ON PURPOSE.** A batch left in `draft` is refused *before* any image is read, so the test would have passed for entirely the wrong reason. The stub extractor **rejects** rather than returning an empty result, so if the purge had not taken effect the batch would land on `EXTRACTOR_ERROR` and the assertion would fail — and `T-RET-013d` closes the same gap from the other side by proving a batch whose bytes are still present does **not** report `IMAGES_PURGED`. The message assertion is not cosmetic: a batch that failed because its screenshots aged out is not a fault the owner can retry their way out of, and "something went wrong, try again" sends them round a loop that can never succeed. ⚠ **FINDING (second occurrence).** `T-RET-011`/`012`/`013` and `T-INFRA-004` were all defined in `specs/testing.md` and unimplemented, yet **none was in `BASELINE_ORPHANS`** — the same gap already noted for `T-EXPORT-001` in TASK-131. The baseline is therefore not a complete census of defined-but-unimplemented ids, and its 48 entries should not be read as one. | `T-INFRA-004`, `T-RET-011`, `T-RET-012`, `T-RET-013` |
| `TASK-120` | `T-UI-022` (`apps/web/test/aboutPage.spec.tsx`, 11 cases a–k). **`T-ATTR-004` was already shipped** by TASK-026/046 in `tests/e2e/attribution.spec.ts`; the only unimplemented half of this row was the `/about` copy assertion. `AboutPage.tsx` and all four copy constants already existed too — what did not exist was anything asserting the page says what the backend does. **US-035's entire test set is integration retention/purge (`T-RET-011/012/013`, `T-INFRA-004`); not one of them looks at the page that tells the owner what the behaviour is.** A correct backend behind a page that says nothing fails the user-visible half of REQ-028 while every backend test stays green. Byte-equality throughout, not `toContain`: "kept for about a month" passes a substring check and is a different promise. `T-UI-022f` is the invariant-8 guard — merging `IMAGE_RETENTION_DAYS` (30) with `TMDB_METADATA_MAX_AGE_DAYS` (183) here reads as a perfectly plausible English sentence, so nothing else would notice. Mutation-tested: 5 mutants, 5 killed — `title=` attribute added (killed by `j`), never-delete copy replaced with a duplicate of the analytics copy (killed by `c`, `d`, `j`, `k`), 30→183 (killed by `a`, `b`, `f`), a 90-day TTL added to the never-delete promise (killed by `h`), an "except anonymous aggregate counts" hedge added to the no-analytics promise (killed by `i`). Noted: `f`/`g` are partly redundant with `a`'s byte-equality against the spec wording, and deliberately so — `a` is updated whenever `specs/ui.md` §8 is, whereas `f`/`g` are invariant guards that would then still fail. | `T-ATTR-004`, `T-UI-022` |
| `TASK-121` | `fdd5519` — `tools/check-mutating-routes.mjs`, `T-MUT-001` (a–j), `T-MUT-002` (a–f). The 18 mutating routes each map to one of the eight REQ-041 owner-initiated operations or carry an explicit non-list-state reason; mutation-proven with an unregistered `POST /titles/:id/auto-confirm` and a `jobs/reconcile.ts` calling a guarded operation. | `T-MUT-001`, `T-MUT-002` |
| `TASK-122` | `dde3dc5` — `tools/check-outbound-hosts.mjs`, `T-SEC-031` (a–i) and the outbound half of `T-SEC-009` (`k`). Mutation-proven by widening the list, shrinking it, and planting an unlisted host in source. The telemetry and streaming hostnames the spec needs are assembled from fragments rather than added to another gate's exemption list. **Amended by Epic M:** the list is now **four** destinations (TMDB, Azure OpenAI, Azure AI Vision, OMDb) and each entry declares a `sends` class, with `T-SEC-031u`/`v` pinning `image-bytes` to the two extractors — the count was never the T18 guarantee. ~~Superseded: "Exactly three destinations: TMDB, Azure OpenAI, Azure AI Vision."~~ | `T-SEC-009`, `T-SEC-031` |
| `TASK-123` | `tests/e2e/a11y.spec.ts` now covers the viewport suite named by US-037: `T-A11Y-001` checks every route at 320 px, the closed list tap targets, the R7 upload affordance cluster (`Paste screenshot`, `PASTE_IOS_HINT` on touch viewports, `Choose files`, and drag target) and the newly wired open row menu items; `T-A11Y-002` proves the 1024 px file-selection journey can create, attach and submit a batch; `T-A11Y-013` renders a long removal history and measures every restore button at ≥44 px; `T-A11Y-015` exercises `/`, `/upload` and `/removed` at the deliberate 280 px floor. The R7 regression guard measures rendered boxes with `boundingBox()` and reaches the paste button by real `Tab` presses; it also asserts file selection remains visible beside paste, not displaced by it. Mutation/break checks used DOM/style injection and each failed the intended case: removing `paste-hint` failed `T-A11Y-001d`, removing `Choose files` failed `T-A11Y-001d`, collapsing row-menu items to 20 px failed `T-A11Y-001e`, and forcing `documentElement.minWidth = 400px` failed `T-A11Y-015a`. ⚠ **FINDING CONFIRMED AND FIXED ON MAIN BY COORDINATOR BEFORE THIS LANE CLOSED:** the first red run showed `PASTE_IOS_HINT` absent at 320 px because the promised touch inference did not exist; `T-A11Y-001d` is the exact case that caught it. The row-menu coordinator note did invalidate a closed-list-only sweep, so `T-A11Y-001e` was added after rebasing onto `origin/main` to cover the open menu state introduced by PR #114. | `T-A11Y-001`, `T-A11Y-002`, `T-A11Y-013`, `T-A11Y-015` |
| `TASK-124` | `T-A11Y-012c` sweeps all ten routes. Found two real defects: the review route rendered a blank page for a malformed stub (so `T-A11Y-001c` had been passing on it vacuously), and the paste button failed contrast at 3.68:1 on WebKit. | `T-A11Y-012` |
| `TASK-125` | `apps/web/src/lib/useOnline.ts` (the single source of connectivity truth), `components/OfflineBanner.tsx` mounted once in `AppShell`, and per-control disabling with a visible `OFFLINE_DISABLED_REASON` on upload, review and list. `T-UX-023` (a–d) and `T-UX-024` in `tests/e2e/offline.spec.ts`, driven through Playwright's real `context.setOffline` rather than a stubbed flag. ⚠ **THE BANNER IS MOUNTED ONCE, IN THE SHELL, AND THE PER-SURFACE WORK IS THE *CONTROLS*.** A per-page banner is the tempting shape and it produces two banners the moment any surface nests another; more importantly it does nothing about the actual hazard, which is a mutating control that still looks pressable offline and fails as an opaque network error. ⚠ **`STATUS_OFFLINE` (§5.8) AND `OFFLINE_BANNER` (§11) MUST NEVER BE MERGED** — one is a batch's *status*, the other is the browser's connectivity; a single constant makes an extraction that failed while the owner was online read as "you are offline". ⚠ **THE HOOK RE-READS `navigator.onLine` ON MOUNT, NOT ONLY ON THE EVENT.** A surface mounted while already offline never receives an `offline` event, so an event-only hook renders every control enabled for a session that began disconnected — the exact case the feature exists for. ⚠ **THE `connectivity` INJECTION SEAM WAS DELETED, NOT BASELINED.** An optional prop no caller supplies is a defect when it *gates behaviour* the product would otherwise never reach (`T-INFRA-013f`); Playwright's `context.setOffline` drives `navigator.onLine` directly, so the seam was pure duplication. Closing it also widened the gate's `componentFiles()` filter to include `containers/`, which surfaced a third instance the old filter could never see — `App.tsx` mounts route containers dynamically (`element={<Component />}`), so the gate never sees a literal tag for them. ⚠ **`RowMenu` FOCUSES ITS FIRST ITEM ON OPEN; OFFLINE THAT ITEM IS DISABLED AND `focus()` IS A NO-OP**, stranding a keyboard user on `<body>` — the fallback is load-bearing, not cosmetic. ⚠ **`context.setOffline(true)` ALSO BLOCKS `page.goto`** in e2e, so the navigation must precede the disconnect. `/removed` (§7.11), `/suppressed` (§8.7) and `/batches` (§9.11) were deliberately deferred at the time because those files were held by other lanes; they are tracked as follow-on work on those surfaces. | `T-UX-023`, `T-UX-024` |
| `TASK-127` | **`ahead-of:TASK-126`** — the declared dependency is ordering, not a blocker: TASK-126 is the AC↔test mapping gate whose remaining half is the **rental** mapping, an owner decision that must not be built in v1, while the coverage floors are an independent mechanism that neither reads nor feeds it. **The thresholds were already in `vitest.config.ts` at the §1 numbers; what was missing was proof they are ENFORCED, and a guard on the numbers themselves.** Verified enforcement live rather than assumed: with `packages/domain/src/**` statements raised to an impossible `100`, `npm run coverage` exits **1** with `ERROR: Coverage for statements (98.83%) does not meet "packages/domain/src/**" threshold (100%)`; restored, exits 0. The 93.16 %/88.31 % overall figure confirms the floors are not vacuous. ⚠ **Gap found and closed:** `T-INV-023a–c` guarded the coverage **exclusion list** but nothing guarded the threshold **numbers** — an agent hitting a red gate could edit `95` down to `70` and CI would stay green, which makes the standing "never lower a threshold" rule a prohibition with no enforcement (`T-INFRA-016` restated). Added **`T-INV-023d`** (`tests/infra/coverageExclusions.spec.ts`): it parses the §1 table in `specs/testing.md` **and** the `thresholds` block in `vitest.config.ts` and fails unless they agree, so the spec stays the source of truth and lowering a floor requires a visible spec edit. Deliberately not hardcoded. Both parsers carry a **positive control** asserting exactly 3 rows — which immediately earned its place by catching a real bug in this very test: the first config parser sliced to the first `},`, stopping at the end of the first entry and yielding an empty set that would have compared `{}` to `{}` and passed vacuously had the spec side broken too; replaced with a brace matcher. Mutation: `apps/web/src/**` statements `70` → `40` fails `T-INV-023d`; restored by `edit`, `git diff --stat` clean. | _no test id declared_ |
| `TASK-128` | `c64b7b5` — `tools/egress-guard.mjs`, `T-CI-007` (a–n). Patches `fetch`, `http.request` and `https.request`; loopback and the compose service names stay reachable; mutation-proven with a `fetch` and a raw `https.request` to an external host. ⚠ Installed per-suite, not globally: global wiring needs `setupFiles` in `vitest.config.ts`, which no lane may edit — see the finding below. | `T-CI-007` |
| `TASK-129` | `T-PERF-002` (`tests/e2e/reviewPerformance.spec.ts`, cases a–f, both the `chromium` and `mobile-safari` projects). `CandidateList` already virtualised above 100 (SD-11c); what did not exist was any proof, **and it could not have existed anywhere but here** — `@tanstack/react-virtual` windows on measured layout and jsdom reports every element as 0 × 0, so `CandidateList` deliberately falls back to a plain list below the threshold and its windowed branch has no component-test coverage at all. **The headline finding is a false claim I wrote and a mutant then disproved:** the first draft asserted the summary count (500) alongside a small mounted-row count and claimed that pinned "a window OF 500". It does not. `section.items.slice(0, 200)` leaves the summary reading 500, leaves the list windowed, scrolls, recycles rows — and passed every test in the file. A windowed list and a **silently truncated** one are indistinguishable from the top of the list, and truncation is far worse: it hides candidates from a review pass, the one screen where a missing row becomes a lost title. Only reaching the **last** candidate separates them (`T-PERF-002f`). ⚠ Truncation *below* the threshold (`slice(0, 20)`) is caught by a/b/e too, but only incidentally — it drops the windowing altogether — and that coincidence is exactly what made the original claim look true. **Two harness traps, both of which fail with a message that reads like a real product defect:** (1) `node.scrollTop = node.scrollHeight` once is not enough and fails on *correct* code — the virtualiser starts from an estimated row height and re-measures as rows mount, so the bottom moves under the scroll; wrapped in `toPass` and re-scrolled. Do **not** "simplify" it to `scrollTop = MAX_SAFE_INTEGER`: tried and re-verified, it passes on Chromium and **fails on WebKit**, so it breaks only the `mobile-safari` project — the one representing the device this product is designed for. (2) A candidate card renders its title **twice** (resolved match name **and** raw extracted text), so a bare `getByText` resolves to two elements and fails Playwright strict mode with a not-found-shaped message; `.first()`, scoped to `review-additions`. **⚠ THE MOST EXPENSIVE MISTAKE, AND IT WILL RECUR: Playwright's `webServer` runs `vite preview`, which serves the PREBUILT `apps/web/dist`, not the source.** Restoring a mutant and re-running re-tests the mutant. `T-PERF-002f` was consequently declared flaky, "fixed" twice, and blamed on worker contention — it was correct the whole time and the stale `slice(0, 200)` build was still being served. `npm run build --workspace apps/web` after **every** source change; also note `--workers=1` is the CI setting and a multi-worker local run can pass what fails serially. Written into the file header. Mutation-tested: virtualisation disabled (`VIRTUALISE_ABOVE = 100_000`) killed by `a`, `b`, `e`; `slice(0, 20)` killed by `a`, `b`, `e`; `slice(0, 200)` killed by `f` **alone**. ⚠ Recorded honestly: `T-PERF-002c` (no 320 px horizontal scroll) and `T-PERF-002d` (the action bar still responds) **survive the unwindowed mutant** — 500 cards mount fast enough on a CI-class machine that a timing assertion cannot separate them. They assert real, distinct properties, but they are **not** part of the virtualisation proof and must not be read as such; `a`, `b`, `e` and `f` are. | `T-PERF-002` |
| `TASK-130` | **`T-E2E-001` steps 8–10 (attribution, a11y, 320×640 re-run) added to `tests/e2e/journey.spec.ts`.** Steps 1–7 (from #163/#166) were a single inline `test`; steps 8–10 required driving attribution/axe/overflow checks through **journey states a static route sweep cannot reach**, so the body was refactored into one shared `async function runOwnerJourney(page, opts: JourneyOptions)` with two thin `test()` wrappers — `T-E2E-001a` (default viewport, `axeJourneyStates:true`) and `T-E2E-001b` (`page.setViewportSize(320×640)`, `narrow:true`). ⚠ **One body, two viewport configs — NOT two copies.** Copying the ~350-line journey would reproduce exactly the second-implementation hazard #166 spent a PR bounding; the two would diverge and only one would be maintained. Three nested helpers woven through the flow: `attribution(label)` (step 8 — `TMDB_DISCLAIMER` visible + logo `naturalWidth>0`), `axeState(label)` (step 9 — `AxeBuilder` wcag tags, **filtered to `serious`/`critical` only**, never widened), `noOverflow(label)` (step 10 — `scrollWidth-clientWidth<=1`, a no-op unless `opts.narrow`). ⚠ **Two `test()` calls could not share the id `T-E2E-001`** — `nextup/test-id-naming` (`T-META-004`) dedups on the full id string; the `a`/`b` sub-ids follow the repo convention (`T-A11Y-001a…e`, `T-ATTR-002a…c`) and both resolve to base `T-E2E-001`, which `check:test-ids`/`check:orphans` confirm is defined in `specs/testing.md` §5 and still cited by the backlog. **`-g "T-E2E-001"` still selects both.** **ALREADY COVERED ELSEWHERE vs BUILT HERE (stated plainly, as instructed):** step 8's *route-level* obligation (US-011 AC-5) is already fully proven on all 9 routes in initial/empty/API-failure/320px states by **`T-ATTR-002/003/004`** (`tests/e2e/attribution.spec.ts`); step 9's *static* per-route serious/critical sweep at 320px is **`T-A11Y-012c`** and no-horizontal-scroll is **`T-A11Y-001c/e`** (`tests/e2e/a11y.spec.ts`). The journey's **additive** contribution is only what those static sweeps structurally cannot see: attribution + axe + overflow in **populated** lists and **interaction-gated** states (removal-confirm dialog open, suppress dialog open, row menu open), plus the **entire value loop driven end-to-end at 320×640** (step 10's real requirement — a dialog whose buttons fall outside the viewport only appears when you *drive* the flow, not when you measure `scrollWidth` on a static page). This split is documented in the file header. **Ordinal conflict — REPORTED, then DISCHARGED BY MAIN during rebase:** `specs/testing.md` §5 step 7 names "Removal 1 of 1" and `specs/ui.md` §6 lists the ordinal chip **unconditionally** per removed row ("Removal 2 of 3"), but `apps/web/src/pages/RemovedPage.tsx` `removalOrdinalLabel` returned `null` when `removalTotalForWork <= 1`, so a singleton rendered no ordinal — an **implementation defect in `RemovedPage`** (nothing in `ux-states.md` §7.5 authorizes the suppression). I first reported it (not patched — `apps/web/src` is another lane's) and pinned the shipped behaviour with a `removed-ordinal` **absence** assertion. **Another lane fixed `RemovedPage` on `main` in favour of the spec** (it now renders the chip whenever total ≥ 1); on rebasing onto `origin/main` I adopted that resolution — step 7 now asserts `getByTestId('removed-ordinal').toHaveText('Removal 1 of 1')`, verified against the rebased `RemovedPage`. The finding is closed, not carried. **MUTATION TABLE (all client-side via `page.addStyleTag`/`insertAdjacentHTML`, since `apps/web/src` is read-only for this lane — faithfully reproduces the production defect shape while staying in-lane; each injected then restored via `edit`, `git diff --stat` clean between):** (a) hide `.tmdb-attribution` before `/removed` → **step 8 fails on /removed** (disclaimer "hidden"); (b) inject an alt-less `<img>` on a journey surface → **step 9 fails** on axe `image-alt` (critical); (c) inject a 2000px-wide div into the flow → **step 10 narrow (`T-E2E-001b`) fails**, wide (`T-E2E-001a`) passes; (d) ungate `noOverflow` + inject a **360px** element → **both narrow(320) runs fail while both default runs pass** (chromium 1280, mobile-safari 390), proving the viewport genuinely varies and not merely the assertion gating. All four killed by the intended case; all restored; journey 4/4 green after restore. **No surviving mutants.** **No shared-file change needed** — T-INFRA-016 (#169) already wired `typecheck:e2e`, so `npm run typecheck` is a live drift gate on this file (verified). **Verification:** journey 4/4 at `--workers=1` (the CI config); full suite **88 passed** at `--workers=1`; `typecheck`/`prettier --check`/`lint`/`check:test-locations`/`check:orphans`/`check:test-ids`/`check:status` all clean. ⚠ **Local flake, environmental not a regression:** default multi-worker Playwright against a single `vite preview` intermittently times out 1–2 heavy tests (incl. untouched `mobile-safari` journey `a`, whose axe scans are WebKit-slow) from proxy contention; `--workers=1` (CI's setting) is consistently clean. ⚠ **Rebuild `apps/web/dist` before every e2e run — `vite preview` serves the prebuilt bundle** (the recurring TASK-129 trap). | `T-E2E-001` |
| `TASK-131` | `scripts/export-owner-data.ts` (new) + `scripts/tsconfig.json` (new project, referenced from the root solution so CI typechecks it), `docs/restore.md` (new runbook), `apps/api/test/integration/exportOwnerData.spec.ts` (10 cases, `T-EXPORT-001` a–j), a `build:scripts` npm script, and three `.gitignore` patterns. Mutation: 8 applied, **6 killed, 2 survived and both were worth more than the kills.** ⚠ **THE TABLE LIST IS DERIVED FROM PRISMA'S DMMF, NEVER WRITTEN DOWN, and that is the single design decision this task turns on.** A hand-maintained array is how a backup tool fails silently: it keeps succeeding, keeps reporting a row count, and keeps writing an artefact missing whichever table was added last — and the omission is discovered during a restore, the one moment it cannot be fixed. `T-EXPORT-001a` compares the exported table set to the model set and `T-EXPORT-001b` asserts the source names no table at all, so adding a model to `prisma/schema.prisma` is covered **by construction**. ⚠ **`T-EXPORT-001b` WAS NEARLY USELESS AND ONLY A MUTANT SHOWED IT.** It rejected physical table names (`upload_batch`) but not Prisma MODEL names (`UploadBatch`) — and the model name is the form anyone would actually hard-code, since `ownerModels()` returns models. The hand-coded-list mutant walked straight past it and was caught only by `001a`, which fails on *any* omission, so `001b` was contributing nothing. It now rejects both spellings. ⚠ **THE PITR WINDOW IS 7 DAYS, NOT 35 (A40).** The Rev-3 plan assumed PostgreSQL Flexible Server; we run Azure SQL **Basic** (`infra/sqldb.bicep`, `retentionDays: 7`). Because REQ-028 makes the store append-mostly and irreplaceable, damage unnoticed for a week is **unrecoverable via PITR** — which is why the manual export is the **primary** line of defence, not a safety net. `T-EXPORT-001i` fails if the runbook ever drifts back to a 35-day claim, because that would leave the owner believing they have five times the recovery window they have. ⚠ **THE RUNBOOK'S OWN COMMAND DID NOT RUN, and `T-EXPORT-001j` exists because of it.** `npm run export:owner-data -- --owner … --out …` fails: npm parses `--owner`/`--out` as its own config, warns `Unknown cli config` rather than erroring, and forwards the bare values as positionals — so the script dies with `Unknown argument: o_xxxx`, which reads as a bug in the script rather than in the instructions. Split into `npm run build:scripts` + a direct `node` invocation, and the test pins the working form. A runbook is executed by a person under pressure who has never run it before; a command that fails on line one is worse than no runbook. ⚠ **A MISTYPED OWNER ID IS THE LIKELIEST FAILURE AND DOES NOT LOOK LIKE ONE.** Owner ids are opaque 16-hex hashes; mistype one and every query matches nothing, the script succeeds, and it writes a well-formed artefact with the right shape, the right table list and zero rows — indistinguishable from a real backup until the day it is restored from. A zero-row export is therefore **refused** unless `--allow-empty` is passed (`T-EXPORT-001f`). The write also uses `flag: 'wx'`: the only thing worse than no backup is overwriting a good one with a bad one, and this tool is reached for precisely when something has already gone wrong. ⚠ **`JSON.stringify` THROWS ON `BigInt`** and this schema has four (`batch_change.id`, `candidate_source_image.id`, `uploaded_image.byteSize`/`.uploadedByteSize`). Unhandled it produces no artefact at all, and only once there is real image data — never on an empty developer database, always on the owner's. Encoded as strings with a per-table `fieldTypes` map carried alongside, because `"12345"` is otherwise ambiguous with a `String` id and guessing wrong at restore time is a silent corruption. ⚠ **THE ARTEFACT IS A COMPLETE COPY OF THE OWNER'S DATA AND THIS REPOSITORY IS PUBLIC** — the runbook says write it outside the working tree, and `.gitignore` gains `*.owner-export.json`, `owner-export-*.json` and `/exports/` as the backstop for the run that uses `--out .` out of habit. ⚠ **NOTHING HERE IS SCHEDULED AND NOTHING MAY BECOME SCHEDULED.** `T-CI-005` scans `apps/api/src/**`, **not** `scripts/**`, so a timer added to this file would slip past it — `T-EXPORT-001h` closes that gap directly, and `T-EXPORT-001g` asserts the script only ever calls `findMany` (no `create`/`update`/`delete`/`$transaction`/`executeRaw`). The weekly cadence lives in the owner's calendar, which is the right place for a habit a machine must not own. **There is deliberately no import script** (`docs/restore.md` §3.4): a one-command "restore my data" against a live database is a delete-and-replace with a friendly name on it. ⚠ **TWO SURVIVING MUTANTS, BOTH INSTRUCTIVE.** (1) Deleting the explicit `bigint` branch in `encodeValue` changed nothing — the trailing `String(value)` fallback already covers it, so that branch is defensive redundancy rather than the load-bearing line; the property is still guarded, because a pass-through `encodeValue` fails `001c` with the real `Do not know how to serialize a BigInt`. (2) Dropping `orderBy` entirely also survived: SQL Server returns small tables in clustered-index order, which for these models **is** key order, so two runs agreed. That mutant is equivalent *today* and stops being equivalent the moment a parallel plan or a different index is chosen — order is not guaranteed without `ORDER BY`. `001e` was rewritten to assert ascending key order **positively** rather than merely that two runs agree, which kills the non-equivalent `desc` and wrong-field mutants. ⚠ **The script uses the PLAIN Prisma client on `DATABASE_URL`, not the app's driver adapter** — the same operator-credential path `prisma migrate deploy` already takes, for the reason `apps/api/src/db/connection.ts` gives: it never runs inside the container, so the running app still holds no database credential (`specs/security.md` §7). Nothing in `apps/api/src/**` imports it. | `T-CI-005`, `T-EXPORT-001` |
| `TASK-132` | Verified against the delivered repo, not the authoring tree. **AC-6′/AC-7 recorded in PRD traceability** as new `docs/PRD.md` §A.9, with the provenance §A.1 cannot carry (it traces requirements to stories, not criteria), AC-7 traced to REQ-071 via SD-06, and the counting rule that AC-6′ **replaces** AC-6 rather than adding to it. **"62 in v1" → 59**: the two files the task names (`Context/requirements.md`, `Context/mvp-definition.md`) **do not exist in this repo** — `Context/` is an authoring tree, like `artifacts/`. The one live stale headline in the delivered repo was `docs/BRD.md` §Scope authority, corrected in place with the superseded figure struck through beneath it. `docs/PRD.md` §A.1/§A.5 already said 59. ⚠ `docs/review-report.md:141` asserted "No stale '62 in v1' survives ✅" while `BRD.md` still carried one — the claim was true of the authoring tree only, and is true here only as of this commit. | _no test id declared_ |
| `TASK-133` | **First of three artifacts delivered: `docs/runbooks/rollback.md`.** It was the urgent third — `.github/workflows/deploy.yml` and `infra/aca.bicep` both cited the file by path and it did not exist, so the pipeline pointed an operator at nothing mid-incident. **Rehearsed end-to-end on `ca-nextup-staging`** (`minReplicas = 0`, free, unwatched); production state untouched. Two verified traps drove the content: (1) `az containerapp revision list` **without `--all` shows only ACTIVE revisions** — prod has 13 revisions and lists 1, so the obvious command implies rollback is impossible; (2) **shifting traffic to a DEACTIVATED revision exits 0, prints the new weight table, and then serves 404.** Since the deploy workflow deactivates the superseded revision every time (it must — `minReplicas = 1` bills every active revision for ever), **the rollback target is always inactive**, so the one-command rollback is *always* wrong and fails silently. Correct order is `revision activate` → verify `Healthy` → shift traffic → verify against the FQDN → deactivate the bad one last. The disproven one-command claim was also corrected in place in `deploy.yml`. **Still owed: the incident playbook and the config checklist** (`NEXTUP_MAX_DECODE_PIXELS` pairing warning, the R7 HTTPS/clipboard functional dependency and its silent symptom, the one-value `NEXTUP_EXTRACTOR=azure-vision-read` revert, and the NFR-012a no-downgrade note). **↳ Completed 2026-08-20 (lane D): the incident playbook and the config checklist now exist**, both verified read-only against the live **serving** revision (`ca-nextup-prod--0000031`) rather than inferred from Bicep. `docs/runbooks/config-checklist.md` carries every real setting, the `NEXTUP_MAX_DECODE_PIXELS`/memory pairing rule with both permitted triples and why a byte ceiling cannot substitute for a pixel guard, the one-value `NEXTUP_EXTRACTOR=azure-vision-read` revert with the `NFR-012a` no-downgrade rule, and **R7's HTTPS/clipboard FUNCTIONAL dependency with the symptom named** — *the "Paste screenshot" button is **missing**: not disabled, not erroring, absent; no console error and no failed request* — plus why it is quiet: file selection, drag-and-drop **and desktop Ctrl/Cmd+V all keep working**, because the `paste` event's `clipboardData` is **not** gated on a secure context and only `navigator.clipboard.read()` is, so the loss lands entirely on **iOS**, the one platform where the button is the only paste route. `docs/runbooks/incident-playbook.md` routes by symptom and **links** the memory runbook without duplicating it, stating that an OOM is a decided, priced, pre-authorised remedy (`A43`) and **not** an incident to debug — and that the opposite case, a whole batch failing together, is a real `REQ-080`/`REQ-081` isolation bug that must not be papered over by up-sizing. ⚠ **Three findings while verifying.** (1) **`NEXTUP_AOAI_MODEL` does not exist** — named in this row and in `specs/ai.md` L180, read by no code and set nowhere; the model is pinned by `NEXTUP_AOAI_DEPLOYMENT`, so setting it would silently do nothing. (2) **`.env.example` instructed the reader to create a credential the architecture forbids** (`GHCR_PULL_TOKEN`, described as a fine-grained PAT — which cannot authenticate to ghcr.io at all); `specs/security.md` L405 had already deleted it at R8 and `infra/aca.bicep` has no `registries` block on purpose. Corrected in place, together with `security.md`'s leaked-secret procedure, which still told an operator to rotate that non-existent secret. (3) 🛑 **Production is deployed but NOT configured** — `infra/aca.bicep` sets six env vars and the deployment matches it exactly, so `DATABASE_URL`, `NEXTUP_ALLOWED_SUBJECTS`, `TMDB_API_KEY` and the storage settings are **not wired at all** (a template gap, not drift; the only secret is `entra-client-secret`). Expected while `TASK-141`/`TASK-134` are owner-gated, but **invisible from outside**: Easy Auth answers first, so a fully-configured and an entirely-unconfigured app return an identical, correct-looking `401` — which is precisely why a green `T-SMOKE-*` run does not mean the app works. Recorded as §0 of the checklist; `infra/**` is frozen this round, so not fixed. | _no test id declared_ |
| `TASK-141` | **Managed identity for the app; SQL login for migrations only.** Three R4 assumptions were disproven by execution. **(1) The M0 smoke migration was already satisfied** — `prisma migrate deploy (prod)` is step 8 of `.github/workflows/deploy.yml` and has been applying all three `prisma/migrations/**` against the real **Azure SQL Basic** `nextup` database on every deploy. The gating deliverable was green before the task started; nobody had checked. **(2) MI is impossible on Prisma's built-in `sqlserver` connector.** That connector is the Rust `tiberius` driver: its URL grammar has no `authentication=` parameter and Entra auth for it is unimplemented (prisma/prisma#12562, #13853, #7673, all open). Passing a token as `password=` does not work either. The `specs/security.md` §8 row asserting "the `sqlserver` provider needs no extra npm package" and that "`mssql`/`tedious` is pulled in transitively by Prisma" was **wrong in both halves** and is corrected in place. The working path is **`@prisma/adapter-mssql`**, published at our exact pinned `6.19.3`, with driver adapters **GA** (verified in the installed `.prisma/client` typings — no preview flag). **(3) The token-refresh trap is avoided by not writing one.** `azure-active-directory-default` hands the *credential* to tedious, which acquires a token during each connection's LOGIN, so there is no lifetime for the app to cache or guess; `pool.min = 0` lets idle connections drain so reconnection re-authenticates. The naive alternative `azure-active-directory-access-token` takes a token *string* — it works for about an hour after each deploy and then fails at a time nobody is deploying, which is exactly the failure R4 predicted. `T-SEC-035p`/`T-SEC-035t` assert against it. **The credential split:** the container holds **zero** database credentials (`DATABASE_URL` is plain, credential-free config, and the auth mode is *derived* from that absence, so adding `user=` would silently switch auth back with every test still passing); the SQL admin login is a **deploy-time** GitHub secret that never enters the container and carries the DDL rights the app must not have. ⚠ **Id collision resolved:** R4 cited `T-SEC-028`, which §9 L744 had already defined as "every domain type declares required `ownerId`" and which is implemented under that meaning — reusing it would have silently retired one assertion with the suite still green. The TASK-141 sense is **`T-SEC-035`** (23 assertions, `specs/testing.md` §34). ⚠ **Coverage gate caught a real placement error:** `connection.ts` was first written into `apps/api/src/repository/**`, which is coverage-excluded because it is measured by the integration suite — `T-INV-023b` refused it, and since `T-INV-023c` holds that exclusion list closed, the file moved to `apps/api/src/db/connection.ts` where the unit thresholds measure it. 🙋 **One owner action remains and cannot be automated:** `CREATE USER … FROM EXTERNAL PROVIDER` requires an Entra-authenticated session, and the pipeline authenticates to SQL with a SQL login. The `WITH SID = 0x…, TYPE = E` workaround does work from a SQL login but needs the identity's **application** id hand-converted to little-endian bytes (ARM reports only the *principal* id, which is a different guid) — and a wrong SID **succeeds**, failing only later as a login error that names the wrong cause. Deliberately not used. See `docs/runbooks/database-access.md` and `infra/sql/grant-app-identity.sql`. | `T-SEC-028`, `T-SEC-035`, `T-SEC-035t` |
| `TASK-142` | `T-INFRA-009` (`infra/budget.bicep`, `tests/infra/infra.spec.ts`). **Deployed live**: `az deployment sub create` succeeded, `az consumption budget list` returns `nextup-monthly / 13.0 / Monthly`, alerts at 100% and 150% to the owner by email. Email-only by design — no action group, webhook or automated remediation (TASK-142, `REQ-028`). `tools/check-infra.mjs` now drift-gates two templates. Also `T-SEC-034` (`tools/check-audit.mjs`): the production audit gate now suppresses by named, self-deleting exception after GHSA-ggr8-5vv4-36mx turned `main` red with an unfixable advisory whose npm-suggested "fix" is a prisma **downgrade**. See `specs/testing.md` §31. | _no test id declared_ |
| `TASK-143` | **Phase 1 (sweep) reported, Phase 2 (apply) done.** The discriminator that made the triage work: **supersede-by-banner is legitimate for narrative, illegitimate for an instruction a machine executes top-to-bottom** (the F-001 rule). `docs/architecture.md`'s PostgreSQL text sits under a correctly-ordered R4-above-R3 banner over *prose* → **sanctioned, not a defect**, and was left alone. **The one real F-001 violation: `specs/data-model.md` §15** declared itself "(REVISION 3 — **AUTHORITATIVE**)" with a banner reading "**this chapter wins**", while containing executable PostgreSQL DDL (`CREATE EXTENSION pg_trgm`, `jsonb`, `23505`, B1ms) — and supersession was declared **only from §16, 400 lines below it**. Corrected in place with the superseded heading/banner struck through beneath; **the chapter body is deliberately still unedited**, and §16's now-false "retained, unedited" claim was corrected with it. Also applied: **§11's removed-view search** still cited the `pg_trgm` GIN index as current (now §16.6 `LIKE N'%'+@term+N'%'`, with the three consequences the R3 wording dropped — not index-backed, no fuzzy/typo tolerance, `@term` must be `ESCAPE`d); **OQ-025** still claimed **35-day PITR** as current when Azure SQL Basic gives **7** — the R3 *narrowing* of that risk was justified by a window that no longer exists, so severity was re-widened; **I-8** still read "No Cosmos container… carries a TTL"; §5.4's banner routed readers into the superseded §15.4; `api.md`'s cursor cited §15.6; `security.md` said "On PostgreSQL, `owner_id` is a column" and cited §15.9. **Diagram finding — the ERD self-declares "a mismatch is a blocking review finding":** it said **nine tables**, but §16 has **ten** — R4 added `candidate_source_image` (the many-to-many join that intra-batch overlap collapse requires, SD-02) and the count was carried over from §15 unrecounted. Corrected, and the three physical tables the ERD deliberately does not draw (`batch_change`, `removal_group`, `candidate_source_image`) are now listed so the "names must match" rule is satisfiable. ⚠ **My first diagram sweep passed vacuously** — the task names `diagrams/`, but the tree is `docs/diagrams/`, so the grep matched nothing and "clean" meant "absent"; re-run correctly. **The R3→R4 follow-up was actioned, not deferred:** `ColdStartNotice` → **`SlowResponseNotice`**, copy → *"Still working…"*, changed from "consider" to **binding** — with `minReplicas = 1` there is no cold start, so *"Waking things up…"* named a cause that cannot occur and would misdirect anyone debugging a genuinely slow request. No code carried the old name (the component is unbuilt), so this is a spec-only rename with nothing to migrate; `specs/ux-states.md` §2.1 was the one other naming site and was updated with it. **Verified clean:** all three invariants express as filtered unique indexes in §16.4; `2627`/`2601` consistent across `testing.md`/`api.md`/`security.md`; `mssql/server:2022` consistent; every surviving `23505` is inside a banner, struck through, or explicitly contrastive. 🛑 **Queued for the orchestrator (contended, not edited):** `specs/testing.md` L1439 (`23505`) and L1507 (`postgres:16-alpine`). | _no test id declared_ |
| `TASK-144` | `eb07409` — a CI grep gate over `prisma/migrations/**` | `T-MIG-001` |
| `TASK-145` | **Both halves delivered.** Pixel guard: `packages/domain/src/pixelGuard.ts` (pure verdict table, `specs/api.md` §5.0.1), `apps/api/src/images/readDimensions.ts` (PNG IHDR / JPEG SOFn / HEIF `ispe`, header-only, ≤64 KiB, no decoder), `apps/api/src/images/decodeGuard.ts` (`assertDecodable`, the entry point TASK-149 names) and `maxDecodePixels()` in `apps/api/src/config.ts` (request-time read) — `T-IMG-017a`-`l`, `T-IMG-022a`-`d`, `T-IMG-025a`-`g`, 23 unit cases, six of six mutants caught. Module-naming divergence between `specs/api.md` §5.0/§5.0.1/§5.0.3 and the backlog **resolved rather than guessed** (`specs/testing.md` §26.3a). Serial processing: `apps/api/test/unit/serialImageProcessing.spec.ts` — `T-IMG-026a`-`g`, 7 unit cases, two of two mutants caught (a `Promise.all` prefetch and a dropped `image.decode.end` sentinel). The load-bearing claim is asserted as buffer **overlap**, not as a megabyte ceiling, because serial peak RSS varied 65–160 MiB across identical runs and a tuned ceiling would be a GC race; `T-IMG-026g` keeps a coarse 384 MiB cross-check sized from measurement. Three findings in `specs/testing.md` §26.3(0a–0c): an RSS assertion over unwritten buffers passes against everything; the lifetime instrument's release point is `reportProgress`, not `recordItems`, and got this wrong in the safe-looking direction; and a `loadImageBytes` failure is batch-fatal **by design** while only the decode step is contained. §26.2(a) and (b) were both stale and are corrected in place. | `T-IMG-017a`, `T-IMG-022a`, `T-IMG-025a`, `T-IMG-026a`, `T-IMG-026g`, `T-INFRA-005` |
| `TASK-146` | `docs/ghcr-pat.md` written. **No PAT is needed at all** — a fine-grained PAT cannot authenticate to `ghcr.io` and a classic one is account-wide, so the package is public and CI pushes with `GITHUB_TOKEN` (R8). Closed out with TASK-007: the `deploy.yml` link and the pre-push image secret-scan are in place, and the visibility flip is confirmed by the property that actually matters — an **unauthenticated** manifest GET against `ghcr.io/saquibrashid/nextup` returns 200, and both live Container Apps pull it with no `registries` block and no credential. | _no test id declared_ |
| `TASK-147` | HEIC decode chain (`heic-convert` → `heic-decode` → `libheif-js`, LGPL-3.0, decode-only) + `sharp` installed; `T-DEP-001` runtime allow-list, `T-DEP-002` encoder ban and `T-DEP-003` prebuilt-only added to `tools/check-deps.mjs`; `T-LICENSE-001` now green against the REAL tree, completing TASK-153's other half. Three corrections found at implementation: (a) `sharp@0.34` carried high-severity libvips CVEs and would have been blocked by `npm audit` in CI — pinned `^0.35.3`, 0 vulnerabilities; (b) `sharp` is Apache-2.0 with LGPL-3.0-or-later libvips binaries, **not MIT** as the spec claimed — corrected in `specs/security.md` §8 and the TASK-147 row; (c) **the runtime container stage installed with `--omit=optional`, which excludes all 25 sharp platform binaries** — proven by building the image both ways (`require('sharp')` threw "Could not load the sharp module using the linuxmusl-x64 runtime"), fixed with a `COPY --from=build` and guarded by `T-INFRA-007`. | _no test id declared_ |
| `TASK-148` | `apps/api/src/images/sniffFormat.ts` + `T-IMG-024a`-`p` (16 unit cases). `UPLOAD_FORMATS`, `uploadedFormat` and the format error codes already existed from TASK-012, so this task is the sniffer itself. Two findings recorded in `specs/testing.md` §25.3: (a) the spec and the backlog name different module paths for this file AND for the TASK-145 pixel guard - the guard divergence is still unbuilt and should be settled before TASK-149 imports it; (b) a printable-ASCII guard in the brand reader was proven unfalsifiable by mutation and removed rather than left under a test that could not fail. The two ids the Done-when column originally named are integration properties of the upload endpoint and are already owned by TASK-050 - struck through, not relocated. | `T-IMG-024a` |
| `TASK-149` | `apps/api/src/images/transcode.ts` + `apps/api/test/unit/transcode.spec.ts` (21 cases), claims table in `specs/testing.md` §29. `transcodeHeicToPng` calls `assertDecodable` as its first statement, decodes with `heic-convert` to **lossless PNG**, maps a catchable WASM allocation failure to `IMAGE_DECODE_OOM` (503) and everything else to `IMAGE_DECODE_FAILED` (415), and re-asserts the header dimensions against the decoded raster. Wired to the route by `DEFAULT_STAGES`; `UNBUILT_STAGES` survives as an alias so §28's citation resolves. Three findings in §29.3: (a) `ispe` ignores `irot`, so a correct decode legitimately **transposes** the dimensions - reading §5.1 step 4 literally would reject ordinary rotated camera-roll uploads, the exact case A42 exists to support; (b) the stored ceiling is not the upload ceiling - a lossless PNG transcode of a compliant 10 MiB HEIC was **unrepresentable in its own schema**, and the domain schema case had encoded the bug, so `MAX_STORED_IMAGE_BYTES` was separated and that case corrected in place; (c) wiring a stage that can genuinely fail turned a per-file failure into a **whole-request** failure - `ingestOne` now catches `AppError` only (REQ-080/081, `T-IMG-023k`/`l`). The real-fixture legs of `T-IMG-013/015/016` stay with **TASK-151**: `T-DEP-002` forbids a HEIC encoder in the tree, so nothing here can generate HEIC bytes and a committed fixture is the only route. | `T-IMG-013`, `T-IMG-015`, `T-IMG-016`, `T-IMG-023` |
| `TASK-150` | `stripAllMetadata()` in `apps/api/src/images/transcode.ts`, wired into `DEFAULT_STAGES.stripMetadata` so it runs for **every** accepted image from **every** source, outside the HEIC condition — never selected by `ingestSource`. `apps/api/test/unit/stripMetadata.spec.ts` (10 cases) plus an integration case that uploads a GPS-bearing JPEG over HTTP and re-reads the **stored blob out of Azurite**, so the claim is about what landed in the store rather than what a seam returned; it uses the **upload** path deliberately, because WebKit strips EXIF on clipboard read but not on file upload and a pasted fixture would pass whatever our code did. Removal is **structural** — whole JPEG segments and whole PNG chunks are copied or dropped, never re-encoded — so surviving CRCs stay valid and no pixel changes; a JPEG re-encode to launder metadata would be lossy, which NFR-012a forbids. `APP0` (JFIF) and `APP2` (ICC) are kept on purpose: the ICC profile decides how the image renders and identifies nobody. Unparseable streams fail closed. Six mutations verified, each caught by its named case. Claims, non-claims and two findings in `specs/testing.md` §30; §28.2 and §29.2 corrected in place now that REQ-078 is discharged. §30.2 records honestly that the **real-HEIC-with-GPS** leg named in `specs/security.md` §4.2 stays with **TASK-151** — the encoder ban means nothing in this tree can generate HEIC bytes, so a committed fixture is the only route. Finding: the integration JPEG fixture was a 29-byte header stub that stopped mid-`SOF0` and the strip correctly refused it as truncated; the fixture was wrong, not the refusal. | `T-SEC-032`, `T-SEC-033` |
| `TASK-151` | `tests/fixtures/golden/ingest/` (owner's real iPhone HEIC + JPEG screenshot, control photos with and without GPS, clipboard blob) and `tests/fixtures/golden/images/`, with the `golden` project green at **20/20**. Three findings from lane E, all acted on: (a) **the earlier GPS assertions were unsound** — they matched EXIF tag `0x8825` as a two-byte *substring*, which occurs 33× LE and 38× BE by coincidence in a 1.8 MB HEIC; replaced everywhere by a structural TIFF/IFD walk and PNG chunk-table read (`exifProbe.ts`), exact in both directions. (b) **`T-SEC-033f` is vacuous w.r.t. the metadata strip** — the HEIC transcode decodes to a raster and re-encodes, so the stored blob is clean even if `stripAllMetadata` is deleted; `T-SEC-033g` was added to prove the strip deliberately (strip → identity, blob still clean), and `T-SEC-032m` covers the owner's **JPEG** screenshot, which is stored without re-encode and is therefore the only case where the strip genuinely is what removes APP1 (`specs/testing.md` §29.2). (c) the committed HEIC carried **real lat/lon into a public repo** and was redacted in place by `redact-gps.mjs` — only the two ref characters and six lat/lon rationals overwritten with a public landmark, no offset or length changed, so container, EXIF layout, IFD0, device model, XMP, raster and the other 11 GPS tags are byte-identical to what the iPhone wrote. ⚠ Also falsified a **spec** premise: invariant 11 / ASM-058 reason from "iOS screenshots are normally PNG", but the owner's own screenshot is **JPEG** with an EXIF block and no GPS. The conclusion (accept all three, sniff by magic bytes) is unchanged and strengthened; the stated rationale was wrong and is corrected at ASM-058. | `T-IMG-013`, `T-IMG-015`, `T-IMG-016`, `T-IMG-023`, `T-PASTE-006`, `T-SEC-032`, `T-SEC-033` |
| `TASK-152` | `components/RejectionList.tsx` (new) + `ImageDropzone` wiring — `T-UI-004` (f–h), `T-UX-042` (g–m). The inline rejection list moved out of `ImageDropzone` into one component that takes client and server refusals together, and is given **no** `ingestSource` prop — so `ux-states.md` §4.18 ("no separate copy, no separate code path, no exemption") is enforced by the type, not by discipline; `T-UX-042m` renders the same file via the input and via drop and compares `textContent`. Server `fileName` and `message` render **verbatim** (compared with `===`, not `toHaveTextContent`, whose whitespace normalisation would pass against a re-wrapped message) — the guard message interpolates live container memory, so a client-composed copy states the wrong limit the instant the container is up-sized. The up-size link is gated on a two-code set: `T-UX-042i`/`T-UX-042j` are a pair proving it appears for `IMAGE_TOO_LARGE_TO_DECODE` and **never** for `IMAGE_DECODE_FAILED`. `T-UI-004g` asserts the format copy cannot be phrased to disown HEIC and `T-UI-004h` proves an oversized HEIC fails the **ceiling**, not the format check. Eight mutations caught, including message recomposed, local label substituted for the synthesised name, remedy always/never offered, server list dropped, rejections hidden behind the accepted list, and the lenient unknown-type rule removed. The four `PASTE_*` messages remain TASK-161. | `T-UI-004`, `T-UX-042` |
| `TASK-153` | `d6796b3` — owner approved the LGPL-3.0 obligation; gate proven on synthetic fixtures | `T-LICENSE-001` |
| `TASK-154` | `apps/api/test/integration/ingestGuard.spec.ts` (9 tests) — `T-IMG-018` (c–h) and `T-IMG-019` (a–c) asserted end to end through the real route, real Azurite and a real database. The isolation itself was already implemented by TASK-050/149; this task PROVES it and found one real defect doing so: `OOM_PATTERNS` in `apps/api/src/images/transcode.ts` did not match Emscripten's `abort(OOM)` / `Aborted(OOM)` shape, so a genuine out-of-memory decode was classified `IMAGE_DECODE_FAILED` (415, no runbook) instead of `IMAGE_DECODE_OOM` (503) — fixed here, and `specs/testing.md` §AC-10/P1 requires exactly that case. The double is `heic-convert` itself, not `transcodeHeicToPng` and not `IngestStages`, so the real OOM-vs-corrupt classification is under test rather than stubbed out. `T-IMG-018h` asserts NEGATIVELY that no compensating-cleanup path exists (orphan blobs are left to the `NFR-019` purge) and was mutation-verified. Per the task's own scope note this does NOT depend on TASK-072. | _no test id declared_ |
| `TASK-155` | Both halves shipped and mutation-tested (8 mutants, all killed). Server: new `apps/api/src/images/decodeErrorMessages.ts` carries the three `api.md` §5.2.4 messages with live interpolation, deliberately NOT templated from a shared body — `imageDecodeFailedMessage` takes no `env` parameter, so the memory sentence structurally cannot leak into the corrupt-file path. `ingest.ts` re-composes the transcode-rejection message so the file name reaches the owner. Client: `RejectionList.tsx` now renders all five `ui.md` §3.2a elements in order; `MEMORY_CODES` deliberately excludes `IMAGE_DECODE_FAILED`. Container memory is **derived** from `maxDecodePixels` via `COMPUTE_TIERS`/`containerMemoryGiB` in `apps/api/src/config.ts` rather than separately configured — a third setting would let the diagnostic confidently state the wrong memory after an up-size, and REQ-079 makes the two one setting. Tests: `T-IMG-020c`–`i` (`apps/api/test/unit/decodeErrorMessages.spec.ts`, asserting on `outcome.rejected[].message` via `ingestFiles`, not on the builders alone) and `T-UI-013a`–`j` (`apps/web/test/decodeDiagnostic.spec.tsx` + `T-UI-013j` in `mutatingFlows.spec.tsx`). **Two of my own assertions were initially too weak and were caught by mutation testing:** (1) a bare `toContain('1.0 GiB')` passed via the *remedy* text while the *diagnosis* stayed hard-coded — now asserts `'in a 0.5 GiB container'`; generalisable rule: when a message contains a value twice for different reasons, a substring assertion proves nothing. (2) the facts-line-scoping test passed vacuously because the OOM/corrupt fixtures carried no `details` — both now carry full dimensions, so only the code-scoping can suppress the line. **Spec conflict, reported not edited:** `api.md` §5.2.4 and ADR-0008 R2.3 write `runbooks/scale-up-memory.md`; the backlog row requires `docs/runbooks/scale-up-memory.md`. The backlog is the work order and the repo-rooted path is the one that resolves, so the longer form wins; it contains the shorter as a substring, so `T-IMG-020` holds either way. | `T-API-002`, `T-UI-013` |
| `TASK-156` | Runbook verified against the **live** deployment (read-only: `az containerapp list/show`, `az containerapp ingress traffic show`, `az containerapp revision show`, `az monitor metrics alert list`, and `az deployment group what-if` — **no up-size was applied**; prod remains `0.25 vCPU / 0.5 GiB` with `NEXTUP_MAX_DECODE_PIXELS=25000000`). Ten inaccuracies corrected in place with the superseded text struck through beneath (F-001 rule). Three were load-bearing: **(1)** every command named `nextup` / `rg-nextup`, neither of which exists — the real names are `ca-nextup-prod` / `nextup-rg`; **(2)** prod runs `activeRevisionsMode: 'Multiple'` with traffic pinned to a **named** revision, so the documented one-command remedy builds a revision that serves **nobody** — a traffic-shift step is now §2b; **(3)** §3a's "authoritative check" read the **app-level** template, which reflects the newest revision regardless of traffic and so reports success on the exact failure mode in (2) — now checks the **serving** revision. Both backlog-mandated gaps closed: the `nextup-prod-memory-pressure` 400→800 MiB step (§2d, with its §5 revert) and the explicit **not-owner-approved** caveat on the §7 escalation. `README.md` now links `docs/runbooks/`; `infra/aca.bicep` already links the runbook (lines 103, 112). | `T-INFRA-005` |
| `TASK-157` | `packages/domain/src/logEvents.ts` (the ONE spelling of both event names, in the domain so `tests/infra` can import it without pulling in Node-only API code) + `apps/api/src/log.ts` (a stdout sink, **no logging library** — a dependency here would be a telemetry SDK by another name, `NFR-005`) + the sentinel emission in `apps/api/src/images/ingest.ts`, and `infra/alerts.bicep` with three rules — `T-IMG-021` (a–k), `T-INFRA-012` (a–i). ⚠ **Item (h) is discharged and the answer changed a design decision:** ACA publishes 31 metrics and **none is OOM-distinct**, so the sentinel is the PRIMARY signal, not a convenience — written up as `specs/testing.md` §31.6 and a dated **ADR-0003 addendum**. ⚠ **The backlog row's `RestartCount` `Total` aggregation was WRONG and is corrected to `Maximum` split by `podName`:** it is a running per-pod counter, so a `Total`-over-window rule fires forever on a healthy app and gets muted. Three Bicep traps found and guarded: a `'''…'''` multi-line string is **verbatim and does not interpolate** (`${containerAppName}` reached the deployed KQL; `T-INFRA-012h` now fails on any surviving `${`), an interpolated variable compiles to `format()` so the runbook path had to be spelled out **literally** in every description for `T-INFRA-012f` to prove the owner's email names the remedy, and a CRLF-authored template compiles to a **different** artifact than CI's LF checkout. Six mutants killed. Also found: `apps/api/test/**` is **not typechecked**, so four suites silently omitted the newly-required `IngestContext.correlationId`. | `T-CI-005` |
| `TASK-158` | `packages/domain/src/pastedFileName.ts` + `T-PASTE-005a`-`s` (19 unit cases). `INGEST_SOURCES`/`IngestSource` already existed from TASK-012, so this task is the synthesiser alone. The INTEGRATION half of `T-PASTE-005` (round-trip of `ingestSource`, server-assigned `seqInBatch`, `blobPath` free of any client name) belongs to TASK-050 — see `specs/testing.md` §27.2. | `T-PASTE-005` |
| `TASK-159` | `components/PasteCapture.tsx` (renders `null`, mounted by `ImageDropzone` so the listener lives exactly as long as the attach area) — `T-PASTE-001` (a–l). Handler order is normative and asserted step by step: editable target → return with no `preventDefault()`; zero images → return with no `preventDefault()`; otherwise `preventDefault()` and deliver **every** image. `navigator.clipboard.read` is asserted **never** called on this path, and no `contenteditable` node is added to the DOM. Four mutations caught: guard removed (`T-PASTE-001b`), `removeEventListener` deleted (`T-PASTE-001d`), zero-image guard neutered (`T-PASTE-001c/g/h`), clipboard truncated to one image (`T-PASTE-001e/f`). | `T-PASTE-001` |
| `TASK-160` | `components/PasteButton.tsx` + `lib/useHeldImages.ts` — `T-PASTE-002` (a–j), `T-PASTE-009` (a–c). `navigator.clipboard.read()` is the **first statement** of the click handler, asserted by a mutation that merely wraps it in `setTimeout`. The §4.0a pre-batch hold was extracted to `useHeldImages` and wired to **both** primitives — a hold behind the button alone would leave Ctrl/Cmd+V silently lossy on the platform where paste is used most. Feature detection returns `null` (not disabled), which is every `http://` origin. Four further mutations caught: `read()` deferred (`T-PASTE-002a`), feature detect short-circuited (`T-PASTE-009a/b`), held images dropped (`T-PASTE-002c/d/e`), empty clipboard conflated with no-image (`T-PASTE-002g`). The four `PASTE_*` rejection messages are **TASK-161** and are deliberately not rendered here; `onPasteFailed` + `classifyRejection()` are the seam. New copy constant `PASTE_HELD_BODY` quoted from `ux-states.md` §4.0a, which `ui.md` §9 has no row for. **A43 follow-up (`T-PASTE-002k`–`n`):** the iOS hint rendered on `touch === true` and NOTHING in `apps/web/src` ever passed `touch` — the prop comments in `PasteButton` and `ImageDropzone` both claimed it was "otherwise inferred from the viewport" and that inference had never been written, so the hint was live only under test. Found by lane-viewport at 320 px. Fixed with `isTouchDevice()` (exported from `ImageDropzone`, keyed on `matchMedia('(pointer: coarse)')` and NOT a width breakpoint) and `const showHint = touch ?? isTouchDevice()`. ⚠ The whole class was invisible because every existing hint case passed `touch` explicitly, including `T-PASTE-002i`, which was NAMED for the touch viewport it did not exercise: a component test that supplies the prop under test can never see a caller that never supplies it. 4/4 mutants killed (`touch === true` by `k`/`n`; `\|\|` for `??` by `m`; `isTouchDevice` always-true by `l`; the `matchMedia` guard removed, by the whole file, jsdom having no `matchMedia`). | `T-PASTE-002`, `T-PASTE-009` |
| `TASK-161` | `components/PasteButton.tsx` + `apps/web/test/pasteCapture.spec.tsx` — `T-PASTE-008` now covers the four clipboard-rejection states as distinct owner-visible outcomes and the re-offer invariant after each one. `NotAllowedError` renders `PASTE_DENIED_BODY`; an empty `read()` result renders `PASTE_EMPTY_BODY`; items with no `image/png` render `PASTE_NOT_IMAGE_BODY`; and a bare `DOMException` renders `PASTE_ABANDONED_BODY`. The same suite asserts the failure is an alert, the paste button is enabled again, no status/spinner/loading copy survives the settled promise, a later successful paste clears the rejection, and a failed paste does not auto-retry or disturb an already-attached upload. Mutations applied before PR: `NotAllowedError` was classified as `abandoned` and `T-PASTE-008a` failed on the denied copy; zero clipboard items were classified as `not-image` and `T-PASTE-008b` failed on the empty-copy assertion; the catch path issued an automatic second `clipboard.read()` and `T-PASTE-008h` failed on the one-read assertion; a permanent pending status was rendered after rejection and `T-PASTE-008e` failed on the no-surviving-spinner/status assertion. No shared-file stop was hit. Stale-doc finding retained from prior paste work: `specs/ux-states.md` supplies `PASTE_HELD_BODY` copy but `specs/ui.md` §9 has no row for it; this task did not edit specs. | `T-PASTE-008` |
| `TASK-162` | `components/ImageDropzone.tsx` + `apps/web/test/pasteCapture.spec.tsx` — `T-PASTE-004` makes the whole attach area the drop target: `dragover` prevents the browser default, flips the visible label to `DROPZONE_ACTIVE_LABEL`, exposes a non-colour-only state hook, `drop` accepts two PNGs through the same `addFiles` path with `ingestSource: 'drop'`, and file and folder refusals render by name in the standard rejection list. `T-UI-014` now asserts the completed three-affordance surface: the paste button, labelled file input and drop target are simultaneously present; the idle copy names paste, choose-files and drag routes while still enumerating PNG/JPEG/HEIC and the 10 MB / 40-image ceilings; the paste/file controls keep their tap targets; the drop target has a keyboard focus stop and accessible name; touch viewports render `PASTE_IOS_HINT`; and successful attachment is announced through the polite live totals. Mutations applied before PR: changing dropped files to report `upload` killed `T-PASTE-004a`; removing the active drag label/state killed `T-PASTE-004b`; dropping folder detection killed `T-PASTE-004e`; removing the drop target focus stop killed `T-UI-014c`; and deleting HEIC/ceiling wording from `DROPZONE_IDLE_LABEL` killed `T-UI-014b`. No shared-file stop was hit. Spec finding: `FOLDER_REJECTION` is required by the "folder refused by name" assertion but `specs/ui.md` §9 has no exact copy row, so the UI uses the nearest matching rejection-copy pattern rather than editing specs in this lane. | `T-PASTE-004`, `T-UI-014` |
| `TASK-163` | `apps/api/test/integration/ingestSources.spec.ts` — **25 tests, verified green in CI on `main`** (run `33300991288`, job `5 · test:int`, `36 files / 550 tests passed`), against a real SQL Server and a real Azurite, offline against `tests/fixtures/golden/ingest/`. `T-PASTE-003` (a–b: three pastes → ONE batch, ordinals `01`/`02`/`03` in receipt order; paste, drop and upload land in the SAME batch), `T-PASTE-005` (t–w: a paste ignores the client filename, an upload keeps the device name, `ingestSource` is read from the FIELD and never inferred from a filename prefix, an unknown source is refused), `T-PASTE-006` (a–c: PDF bytes declaring `image/png` → 415, a PNG declaring `application/octet-stream` → accepted — `Blob.type` decides nothing), `T-PASTE-007` (a–e: 48 MP pasted PNG refused before any decode, 11 MiB paste → 413 by the ingest ceiling, the 41st image refused by either route, `image.decode.begin` carries `ingestSource`, plus the per-request file ceiling and empty request), `T-RET-014a` (a pasted row's `retainUntil` is stamped once and purged by the same rule), and `T-IMG-002/006/010/012`, `T-SEC-003`, `T-SEC-032`, `T-IMG-023` alongside. ⚠ **THE LEDGER, NOT THE WORK, WAS THE GAP.** The suite has been merged and green for some time while this row read `todo`, so `docs/status.md` listed TASK-163 under **"Ready to start"** and advertised finished work to lane agents as startable — the exact duplicate-work hazard `docs/parallel-execution-plan.md` exists to prevent. The row is corrected here rather than the gate loosened. ⚠ **Lane finding, retained:** reusing an existing test id for a different assertion preserves id-count gates while silently deleting coverage; the displaced request-file-ceiling and empty-request assertions were restored as `T-PASTE-007d`/`e`. The 500-instead-of-413 multer defect this lane found is recorded on `TASK-050`, where the production fix belonged; the lane hard-stopped rather than reaching into `apps/api/src/**`, which is the discipline working. | `T-PASTE-003`, `T-PASTE-006`, `T-PASTE-007`, `T-PASTE-007d`, `T-RET-014` |
| `TASK-164` | `tests/e2e/uploadPathRegression.spec.ts` — `T-PASTE-010` (a–e), **10 passing** (5 cases × `chromium` + `mobile-safari`). The mandated never-re-point header comment is present and explains why this is not a duplicate of `T-E2E-001`. Asserts the file input still exists on `/upload`, is `multiple`, is focusable and carries a ≥44 px visible label; that `accept` still admits **PNG, JPEG and HEIC**, token by token so a narrowing names the format it dropped; that the paste button and the file input **coexist** (with a `navigator.clipboard` shim, or the coexistence claim would pass vacuously by paste simply being absent — invariant 19); that a HEIC selection declaring `application/octet-stream` is accepted and **actually transmitted**; and that the complete attach → submit → review → close journey still passes when driven through the input. **Done-when mutation requirement discharged locally before this row was written:** narrowing `IMAGE_ACCEPT_ATTRIBUTE` to `image/png,image/jpeg` failed `T-PASTE-010b` alone; renaming the input's `data-testid` (i.e. removing the input the suite can reach) failed **all five**; both restored with `git --no-pager diff --stat` empty and 10/10 green again. ⚠ **SCOPE HONESTY, AND THE ONE DONE-WHEN CLAUSE THIS FILE CANNOT SATISFY:** the third named mutation, "removing the HEIC transcode", is **not observable from here**. The e2e web server is `vite preview` — a static SPA with every `/api/**` call stubbed — so no server-side HEIC→PNG transcode exists in this process to remove. That half is owned by `T-IMG-013` (integration). The spec file says so in its header rather than asserting a stubbed response and calling it a transcode, which would be a green test for a property the product was never observed to have. Recorded as a divergence, not silently closed. ⚠ Same ledger-drift as TASK-163: the file was merged and green while this row read `todo`. | `T-E2E-001`, `T-PASTE-010` |
| `TASK-166` | Shipped in `9b9e988` (lane B) and **completed here**: `components/SortControl.tsx` is mounted in `ListPage` and `ListRoute` forwards `params.toString()` verbatim to `GET /api/titles`, so the URL is what the server sorts by — and the session-persisted direction was **never written into it**. With `asc` remembered and no `dir` in the address bar, the button read "Oldest first" while the API, seeing no `dir`, returned its `desc` default: a newest-first list under an oldest-first label, silently, with no error anywhere. ⚠ That is exactly the US-020 AC-6 path the persistence exists for ("survives navigating away and back"), so the half that appeared to work was the wrong half, and `T-UI-024j` passed against it because it only asserts `readSortDir`'s return value. Fixed with a `replace: true` reconciliation (a correction of the address the owner already arrived at, not a navigation they performed — pushing would make Back need two presses) and covered by new cases **`T-UI-024o`–`q`**: the direction reaches the query string, the other filters survive it, and the `desc` default is deliberately not written since an absent `dir` already means `desc` (`api.md` §6.2). Both mutants killed. This control is `must` per `A47`: it is the sole escape hatch for the knowingly-accepted newest-first-vs-SUC-003 trade-off. | `T-UI-024` |
| `TASK-167` | this commit — the ledger, the gate and `docs/status.md` | `T-STATUS-001` |
| `TASK-169` | `fc85ae4` — OMDb client | `T-OMDB-001`, `T-OMDB-007` |
| `TASK-170` | `fc85ae4` — `external_ids` read first; `T-IMDB-009` (renamed from the already-taken `T-TMDB-011`) | `T-IMDB-009` |
| `TASK-171` | `fc85ae4` — migration `0004`, the `sends` classification on the outbound gate, `T-CI-005` raised 2→3 | `T-INV-008`, `T-MIG-001`, `T-MUT-001f`, `T-SEC-031u` |
| `TASK-172` | this branch — service, job and the closed two-column writer | `T-IMDB-001`, `T-IMDB-007` |
| `TASK-173` | this branch — `GET /api/imdb/lookup` | `T-IMDB-006` |
| `TASK-174` | this branch — row rating, `/rating` screen, OMDb footer line | `T-ATTR-006`, `T-IMDB-008`, `T-UI-023a` |
| `TASK-175` | this branch — `lib/apiClient.ts`; `T-DATA-001` caught the one real offender (the `/rating` page's own `fetch`, written before the client existed and unaware of the 401 rule) | `T-DATA-001`, `T-DATA-003`, `T-DATA-004`, `T-DATA-005`, `T-DATA-006`, `T-DATA-010` |
| `TASK-176` | `T-DATA-007` | `T-DATA-007` |
| `TASK-177` | `T-DATA-002` | `T-DATA-002` |
| `TASK-178` | `T-DATA-008`, `T-DATA-009`, `T-UX-043`, `T-UX-045`, `T-UX-046`, `T-UX-047`, `T-UX-048`, `T-UX-067`, `T-UX-066` | `T-DATA-008`, `T-DATA-008f`, `T-DATA-009`, `T-DATA-009a`, `T-DATA-009f`, `T-REV-005`, `T-UX-043`, `T-UX-045`, `T-UX-046`, `T-UX-047`, `T-UX-048`, `T-UX-066`, `T-UX-067` |
| `TASK-179` | `T-CSS-002`, `T-CSS-003`, `T-CSS-004` | `T-CSS-002`, `T-CSS-003`, `T-CSS-004` |
| `TASK-180` | `T-CSS-001`, `T-CSS-005` | `T-CSS-001`, `T-CSS-005` |
| `TASK-181` | `T-BOUND-001` (a–f). Found as a field defect three times over and each time written off as a bad test stub: a 200 of the wrong SHAPE throws mid-render, React unmounts the tree, and the owner gets a white page. ⚠ **Every existing sweep stayed green on it** — `role="main"` exists for the instant before the throw propagates — so `T-A11Y-001c` and `expectStyledAndRendered` could not have been strengthened into this. Three mutations, each killed by the intended case and no other: dropping `resetKey` killed `d` **alone** (the latch — React never clears boundary state, so the apology would render over a route that is fine); inverting the render guard killed `f` (the discriminator; `a`–`e` all pass against a boundary that apologises unconditionally, which would replace all ten routes); removing the wrapper from `AppShell` killed `a` + `c` + `d`. ⚠ The boundary wraps the `<Outlet />`, not the root: a root boundary takes the nav down with the crashed screen and strands the owner on the one page that does not work. | `T-A11Y-001c`, `T-BOUND-001` |
